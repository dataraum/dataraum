# DAT-725 graph-close — owner ledger

Objective: Every remaining operating-model structure — temporal coverage/calendar, additivity
projection + units, metric DAG + parameters, validity filters, generated validations (YAML
retired), typed cycle direction — lives as typed, enforced graph structure over `current_*`,
eval green on the rebaselined bands: v0.3.4-ready.

Scorecard: SIGNED OFF (lead, 2026-07-22) with two amendments: band 6 added (second
dataset + vertical, LAST lane); eval-repo strategy = wait for the lead-side rewiring,
fallback = second checkout (see Decisions). Bands:
  1. **Wave-0**: DAT-858 all 9 validation SQLs EXPLAIN-bind clean, every table assessed
     (scoped generation probe, not a sweep) · DAT-859 undetermined temporal_behavior never
     silently "flow" — loud abstain/flag, proven by tests.
  2. **Temporal track (P5·P6·P7)**: every new edge MATCH-returns-rows on the finance corpus;
     coverage per (relation × time column) + exactly one anchor; rolls_up_to + period roll-up
     via bounded recursive CTE; 2-axis additivity projected onto the graph; measured_in +
     fx conversion path; metric DAG walkable; days_in_period as parameter node.
     Gate: sweep #1 (clean + clean-flat) — new oracle sections green, no regression vs baseline.
  3. **Conventions track (DAT-789·P8·P10)**: conventions seeded→DB, YAML retires; posted-only
     scope composed by default with explicit visible opt-out; NINE validations regenerate
     verdict-identical + new enumerated checks; validations YAML deleted.
     Gate: sweep #2 — parity oracle green, no regression.
  4. **Cycle direction (DAT-856)**: family+direction seed→DB, save-enforced, honest rendering
     (look_cycle); eval grades detected-but-undirected as its own state; AP recall green or
     named-undetermined. Gate: sweep #3 (detection-v1).
  5. **Close**: gap tickets 787/788/793 landed; final 3-strategy sweep green on the 9465db0
     bands; structural invariants hard; no new failures vs baseline; documented flappy set;
     no truth patches (DAT-685).
  6. **Second dataset + vertical (LAST lane, lead-added 2026-07-22)**: a non-finance dataset
     with its OWN declared vertical runs the full path — concepts/conventions/cycle-families
     seed from the new vertical, validations GENERATE from edges (no hand-authored YAML for
     it), graph structures instantiate, zero finance-vocabulary leakage. Existing bands don't
     grade this → the lane brief defines done explicitly (oracle/smoke spec) before the lane
     opens. Gate: sweep on the new corpus (+1 run vs the original grant).
  Oracle-first: each band's eval sections land in dataraum-eval BEFORE that band's gate run
  (DAT-736 is continuous, owns them). Invariants: abstention primitive reused (no parallel
  shapes) · edge-gate rule · two-layer enforcement · no corpus names/numbers in code · PGQ-only.

Non-goals: DAT-857 edge-valued additivity (→ DAT-671, next week) · P12/P13/P14 · DAT-739 ·
backlog DAT-280/620/622/679/795/799 (lead-ruled out) · the broad multi-dataset widening
program (eval project follow-up) — band 6's single second dataset is IN · DAT-853's
cockpit-polish parks · DAT-747 only if a lane naturally absorbs it, else stays parked.

e2e budget: 5 sweeps + 1 lead-authorized contingency (band-6 second-dataset run added by
lead 2026-07-22), at track gates (~5 USD/run, Console API) · spent: 0. **Deviation from skill default (lead-ruled 2026-07-22):** no sweep per
integration — per-integration gate is LLM-free (full CI + MATCH-returns-rows + oracle/lint
over captured dumps); real sweeps only at the 4 gates above.
Owner model: Fable, ≤30% of this week's budget, spend reported at every checkpoint; never
switch mid-slice (fallback if allowance dry: fresh session, owner on Opus, tighter cadence).

## NEXT ACTION
**SWEEP-FIX WAVE IN FLIGHT (2026-07-23).** Landed on epic: DAT-866 fix (`1eabf5d00`,
senior-reviewed clean, degrade paths pinned `0cb8ba624`), stray `--help` removed,
**DAT-865a INTEGRATED (`a80923212`)** — typed validation→convention dependency
(relevant_conventions declared at induction, membership-validated + canonicalized at
save, targets-push ∪ declared-pull at bind, loud missing-id warning; prompt v3.0.0;
vocabulary verdict: sign_natural_balance was expressible all along, the missing piece
was the routing edge). Union gates: engine 2978P + ruff(src+tests)+mypy, cockpit 1953P
all four gates, zero dump drift. **DAT-867 INTEGRATED** — folded dim-member axes:
identity = COALESCE(bm.conformed_group, bm.signature) (conformed when judged, else
fact-scoped — unconformed same-name columns stay DISTINCT); referenced leg byte-identical;
both reviewers APPROVE; union gates engine 2979P + zero drift (dump regenerated from
source post-merge per lesson). SWEEP-INTERPRETATION NOTE (lane-flagged): folded members
only surface for filter columns landing in folded bus_matrix CELLS (needs grain-verified
EnrichedView + discovered fold hierarchy) — if sweep #1 folded count stays 0, question
the cell-routing gate, not the SQL. **TEACH-SURFACE INTEGRATED (`8c0763432` + `558127280`)** — teach_validation shadow
detection reads the typed validations view (source='seed', superseded_at IS NULL); typed
tolerance/guidance wire shape; owner reconciliation: zod schema carries
relevant_conventions (verbatim passthrough verified). SCOPE NARROWED (lane-escalated,
reviewer-verified): teach_metric/teach_cycle STAY fs-based — engine lacks a typed metric
description/DAG-body home and cycleFamilies ≠ cycle_types identity space; frame.ts needs
cross-vertical pre-binding YAML reads → readShippedValidations (fs) stays for frame only.
PARKED: typed cycle_types vocabulary + metric DAG-body home (closeout triage). Band 3
YAML deletion now unblocked cockpit-side (engine seed path remains, parity-gated).
Cockpit 1959P all gates. **865b INTEGRATED** — CTC through the witness pool, provenance
(seed 0.95 | generated 0.5) reliability; lone generated → investigate at BOTH grains
(senior review caught the table-grain object + a dup-columns_used double-count — both
fixed+pinned); engine 2985P. **LEAD RULED both (2026-07-23): split CONFIRMED + four
serving fixes GO.** Landed as owner commits: `14c66526a` four silent-substitute cuts
(coverage '◌ unmeasured', conventions-failure stub, om-absent section stubs +
context flags operating_model_analyzed/graph_readable, 865a missing-ids →
message+state_reason; 2991P) · `73b26c3fe` nine seed YAMLs → native tolerance/guidance
(machine-verified equivalent) — normalizer deletion ATTEMPTED+REVERTED: DAT-447
expected_formula teach overlay is a LIVE producer (parameters:{table,column,formula}
reaches the binder only via the fold); docstrings state the narrowed truth. **WITHHOLD LANE INTEGRATED (`074f1dadc`)** — heuristic DELETED (~500 lines net removed);
time gate = engine verdict OR honest visible withhold (gray "not yet known" badge vs
yellow determined-no, at the drill control); unit gate untouched; cockpit 1948P all
gates. **ALL WAVES COMPLETE — engine 2991P, cockpit 1948P, zero drift.** **DAT-868
created under DAT-671** (verdict-total + abstention rows + 3-state cockpit + 2
ride-alongs, map attached). **DAT-869 CREATED (2026-07-23, lead-confirmed)** — new epic
"config→DB completion + graph consolidation": first wave = the four config→DB parks
(expected_formula retype / cycle_types table / metric DAG-body home / ontology envelope),
headline = read-only graph-consolidation survey; awaits its own spec pass; DAT-671 keeps
the additivity charter. **STOP-THE-LINE ROUND 2 (2026-07-23): eval re-ran the failed legs
on pin `074f1dad` — DAT-866 VERIFIED FIXED (temporal oracle 5/5), DAT-867 VERIFIED FIXED
(folded shapes hard-pass, counts off 0), DAT-865 REOPENED as DAT-870/871/872 (eval-side
tickets, all code-grounded same day by 3 Sonnet readers):**
- **DAT-870**: 5 semantically-wrong generated checks on the two CLEAN corpora
  (cumulative-vs-periodic grain against tables the SAME run cataloged periodic_snapshot;
  the original sign-flip signature regenerated; month-start boundary cut;
  structural-absence-as-violation ×2). Root cause grounded: the SQL BINDER is grain-blind —
  resolver.py serves NO table_role/TableEntity fact (induction's served context labels
  every table with it; the binder schema never queries it); validation_sql.yaml is still
  v2.0.0 with no stock/flow-period block while graph_sql_generation.yaml:169-187 HAS one
  (the twin drifted); induction v3.0.0's abstain list covers sign/netting/ordering, NOT
  grain/coverage/boundary. Tell: seed tb_gl_reconciliation.yaml hand-encodes "PERIODIC,
  not cumulative" in guidance prose — the author wrote by hand what the catalog knows.
- **DAT-871**: correlated corroboration — two generated checks wrong for the SAME root
  cause sum 0.500+0.441=0.941 → journal_lines trio blocked on clean (block boundary:
  mass>0.75 at aggregation weight 0.8, band>0.6). pool() is a plain Σ rel·certainty with
  NO tier structure; conflict is structurally 0 for one-hot claims; reliabilities.yaml's
  "two independent generated witnesses" assumption is falsified (same generator + same
  served context ≠ independent).
- **DAT-872**: seed orphan_transactions mis-bound gl.entry_id→invoice_data (0.733315
  exact; structural absence, not orphanhood) at seed 0.95 trust — reliability keys ONLY
  on spec.source; the per-corpus LLM bind carries no trust factor; leg grounding is
  advisory prose (full schema served, declared relationships listed but not enforced);
  abstention is LLM-discretionary.
FIX WAVE 2: **871 INTEGRATED** (`e28a3e87d` — generated-tier witnesses capped at MAX in
CTC via _capped_evidence_mass; pool() untouched; conflict/ignorance recomputed
consistently from the capped mass; all 5 acceptance criteria test-pinned; both Opus
reviewers APPROVE) · **872 INTEGRATED** (`cfa66310a` — semantic_per_table v3.1.0: a
decisive clean containment asymmetry can NOT be silently overridden — flip, or confirm
+ NAME the orphan problem; lane settled (b) as NO persist-time flag (would recompute
the removed containment swap; loud signal is judge-authored in reasoning); 2 probes
both reported, orientation probe flips correctly on a neutral shape; both reviewers
PASS) · **870 INTEGRATED** (`356648ced` — binder grain-serving: TableEntity role/grain/
time facts + detected_granularity run-pinned fail-closed into the binder schema;
validation_sql v3.0.0 (grain semantics / period boundaries / comparison discipline /
existence checks / facts-over-hints protocol); induction v3.1.0 (grain grounded in
closed facts, existence only against enumerating tables, abstain list + grain/boundary/
coverage); sign_natural_balance gains the EXPRESSED-vs-STORED clause (comparisons:
both sides one convention, never one-sided); owner-ACCEPTED beyond-brief items:
<grain_facts> deterministic proximity callout in agent.py + validation effort
low→medium (probe-grounded, DAT-720 phenomenon); spec PASS + senior APPROVE; 4/4
probes reported). **FIX WAVE 2 COMPLETE (3001P/8s, zero drift). GRADED SWEEP RAN (lead, FRESH stack, pin
`f7930f9bf`): 873 CLOSED · 870 WORKED (5→2 wrong checks; induction v3.1.0 healthy
17/18; grain+boundary classes closed) · 871 HOLDS (zero blocked on clean; cap-vs-sum
path live-unexercised, test-pinned only) · 872 DID NOT CLOSE (byte-identical
0.733315).** Residuals owner-localized on the fresh stack, FIX WAVE 3:
- **872 re-cut INTEGRATED** (semantic_per_table v3.2.0 — direction/claim separation:
  the m:1 fast path settles DIRECTION only, a cardinality-agnostic CLAIM CHECK runs
  after it (1:1 → flip on decisive clean asymmetry; m:1 → cannot flip: two fact-shaped
  tables sharing an id, neither a dimension nor a master of the other ⇒ DECLINE the
  foreign_key as a shared axis; either → confirm+NAME orphans for a genuine dirty FK);
  both reviewers APPROVE; 0/2 probes spent (schema unchanged ⇒ no compile risk;
  behavioral verdict = next sweep). STRUCTURAL FINDING surfaced: the judge's output
  schema is Literal[foreign_key,hierarchy] — no emittable shared-axis verdict;
  decline is the fence-compliant realization (orphan check filters to foreign_key,
  cascade ends); a graph-RECORDED shared-axis edge for m:1 = DAT-869/671
  conversation, parked. Nit parked: hierarchy claims not literally covered by the
  CLAIM CHECK prose (same scope as v3.1.0).
- **DAT-874 INTEGRATED** (union 3003P/8s, zero drift) — temporal-form coherence rule at
  both prompts (induction v3.2.0, binder v3.1.0 + smoke tripwires; no engine code).
  LANE CORRECTED THE BRIEF'S PREMISE with dump evidence: the flow/stock verdict IS
  served to induction as og_columns Materialization (property_graph.py:433-437 →
  context.py:1765) — the owner's "zero renders" grep looked for the symbol name, not
  the projection (grep-liveness lesson, again); the failing run's persisted prompt
  showed both facts rendered and the model authored the check anyway ⇒ rule-layer fix
  is the honest shape, no parallel serving added. Sweep-attention #13: confirm the
  ALLOW side too (legitimate flow-vs-flow / movement-vs-Δlevel reconciliations did not
  vanish from the induced set). 2 probes (both behave: binder declines incoherent spec;
  induction proposes zero incoherent checks on a baiting graph).
- **DAT-875 LEAD-RULED (2026-07-23): follow-up, NOT in-epic** — typed stored-sign
  column fact on the temporal_behavior blueprint ("that one works well"), design
  endorsed; ticket re-homed under DAT-671 with the full sketch + ruling comment;
  interim 0.5-investigate false alarm on this class ACCEPTED (871 cap keeps it out of
  blocked); eval conditions the class per their open-ticket skip idiom.
**FIX WAVE 3 COMPLETE (engine 3003P, zero drift). SAME-PIN PASS GRADED (lead-relayed
2026-07-23, pin `380998e34`): 874 VERIFIED WORKING (induction proposed the coherent
form + binder abstained; allow side confirmed 20 checks) · 873/866/867 verified · sign
gate GREEN → band-3 YAML deletion GO · 872 root-caused to a DETERMINISTIC ENGINE bug
(NOT a prompt defeat — see Decisions) · DAT-876 NEW existence-check false-alarm class.
Closed in Jira on their verdicts: DAT-866/867/873/874 (874 eval-recommended; trackers,
reopen if you prefer holding to final sweep).**
**OWNER NOW ON OPUS (2026-07-23) — Fable allowance-dry fallback per the ledger's own
prescription (Opus owner, tighter cadence, check in at each fork). Re-grounded via 3
fresh Step-2 reads (band-3 scope, 872 merge integrity, 876). Two lead rulings taken:
872 → fix in-epic at the orientation layer; band-3 → full delete engine+cockpit.**
**FIX WAVE 4 IN FLIGHT — 3 lanes:**
- **DAT-876 lane RUNNING** (`feat/dat-725-876-existence-universe`, Opus) — existence-
  check loophole closure + served no-enumerating-table fact; typed existence-target
  field pre-registered as the sole fallback.
- **DAT-872 lane SPAWNING** (Opus) — resolution-driven FK orientation at oriented_row +
  _first_wins conflict detection + unwind wave-3 v3.2.0 decline; corpus-verified 100%
  vs 26.7% asymmetry (verification only, not in code).
- **band-3 full-delete lane SPAWNING** (Sonnet·high) — 9 YAMLs gone engine+cockpit,
  synthetic-seed test rewrites, frame.ts onboarding loses finance few-shot (band-6
  zero-leakage aligned), ontology push-route + README swept.
NEXT: integrate the 3 lanes as they land (union gate each; 876∩band-3 share
tests/unit/analysis/validation/ — rebase second lane if it conflicts), then re-pin the
new tip to DAT-736 for the eval's grading pass. In-epic remaining after wave 4: band 5
close set, band 6 (needs lead's dataset+vertical+done-spec). Budget: Fable dry; wave-4
lanes on Opus/Sonnet; sweeps lead-run (owner grant 0/6).
Bonus recorded: 856 confidently-wrong observed live (4 correct/1 undetermined/1 wrong
over 6 draws); 854 v3 MISLABELED split live+correct on a contrast draw; bind-parity
operational (7/9 seed binds drifted vs old pin as expected, new baseline;
sign_conventions gate + YAML deletion decided at the next same-pin pass). Remaining
eval reds all ticket-owned (875 clean — lead-ruled follow-up under DAT-671, 854 flat,
856 detection-v1; 874/872 fixes land on the new pin). CROSSUNIT VERDICT (lead-relayed 2026-07-23): O2
CAP-measured-in-truth VERIFIED live (6/6 pairings incl. CoA-fold; mixed-currency drill
surface correct) · DAT-873 filed (measured_in edge on a dimensionless ratio) →
OWNER-FIXED same day: catalogue_semantics v1.2.0 reorders the unit rule
(amount-vs-ratio KIND decided FIRST; two-unit-column ratio tell) — the projection was
faithful, the annotation was the miss; graded at next sweep (sweep-attention #9) ·
DAT-854 mislabel evidence → owner RULING posted on the ticket (split key-tables-short
vs mislabeled; flat truth accepts the payment leg on either cycle; the crossunit draw
= labeling flap → DAT-856 flap ledger) · DAT-856 direction flap now MEASURED (AP
outgoing-correct on crossunit vs undirected on detection-v1, same pin) — band-5
flappy-set evidence · 79-vs-518 call gap = detector-epoch accounting, resolved · eval
PARKED before the final sweep — sequencing: fix wave 2 lands → that sweep grades
870/871/872/873 AND provides sweep-#2's second same-pin bind-parity pass. Drill-gate
note: cockpit is 2-state until DAT-868 lands 3-state. Sweep-attention #7: CTC loss conflict/ignorance +
reliabilities.yaml. #8: served-context absence stubs + withhold badge (graded +
UI surface text changed). #9: catalogue_semantics v1.2.0 unit-kind reordering
(the dimensionless-coverage leg grades it). #10: validation_sql v3.0.0 + induction
v3.1.0 + <grain_facts> callout + validation effort medium (all bind/induction behavior
— graded by the clean corpora re-run). #11: semantic_per_table v3.1.0 orientation
sharpening (FK direction on 1:1s). #12: sign_natural_balance expressed-vs-stored
clause — REQUIRES a fresh stack to re-seed (insert-if-absent).
Probe ledger: 9 scoped calls. Sweep-attention #5: induction prompt v3 + id-carrying
conventions + binder serving. #6: folded dim-member legs (og_dim_members/og_filtered_by
UNION).
DAT-787 sweep-attention #4 recorded: grounding prompt v10.0 + filter_members contract.
Graph now 12 vertex / 15 edge kinds.

**Eval-repo status (updated 2026-07-22 late):** the hold is PARTIALLY lifted by the lead —
docs-only banking authorized + done: the seven oracle specs are committed as
`docs/oracle_backlog_graph_close.md` (each with its exact `cube.needs(...)` declaration
per the DAT-860 execution cube) + two generator capabilities in
`docs/generator_backlog.yaml` (CAP-measured-in-truth, CAP-roleplay-fk-fixture), eval
commit `ca21003`. Five open questions for the eval agent at the end of the specs doc
(dataset naming, version-bump coordination, parity×epochs, module splits, conditioning
idiom). Eval agent answered all 5 questions (2154e35): detection-roleplay-v1 dataset; O3/O4 move
to a new test_graph_shapes_e2e.py (version-churn-driver split); parity splits by verdict
leg (deterministic = per-draw hard, LLM legs = reduced-verdict under epochs); O1 one
module; conditional-cell = skip w/ machine-readable reason prefix. Owner answered the
bounced og_* head flag on DAT-736: begin_session declarations CORRECT (each element view
resolves its own substrate's grain; no unified graph promotion). **PIN PROTOCOL (owner-
proposed on DAT-736): on DAT-787 integration, post the epic-tip SHA → eval may pin vendor
for oracle DEV + Tier-1/2 immediately; graded sweeps re-pin the merged-main SHA for
verdict-store identity (expected byte-identical — main unmoved all slice).**

## State
Lanes: **W0-A (DAT-858) INTEGRATED** (`b69961b79`) — band 1's DAT-858 half GREEN: quote-
every-identifier rule + hint de-steering + EXPLAIN pin test; live-probed both rounds
(4 sonnet calls total), binds clean, both tables assessed; merged tree byte-identical to
gated lane tree; lane worktree/branch removed. NOTE for sweep #2: probe verdict on the
two-table corpus flipped failed(2 violations)→passed(0) across prompt revisions — LLM
account-classification judgment, not bind mechanics; verdict stability is graded at the
conventions-track sweep, not by this lane.
**W0-B (DAT-859) INTEGRATED** (`03eb991f4`) — **BAND 1 GREEN** (both wave-0 halves + the
driver-list 4th read site). Union tree (W0-A ∪ W0-B) gated fully: engine 2817P, cockpit
check/typecheck/test 1924P/build all PASS (epic worktree needed one-time `bun install` —
remember for future integrations). Landed: typed abstention end-to-end, explicit-absence
rendering, real-path DAT-695 regression, abstained badge + split count, CHECK at full
entropy precedent. Lane worktree/branch removed.
Parked from senior nits: resolve_target_type DB-wrapper keep-or-kill; cockpit formatDrivers
secondary-only-content edge (engine/cockpit read-surface disagreement, pre-existing);
DuckDB session-TimeZone pinning for the temporal module (pre-existing, module-wide —
one-time confirmation that connections pin UTC or columns stay naive TIMESTAMP). P5 (DAT-730) IMPLEMENTATION DONE — 2816 tests green, schema-drift clean, all four
rulings implemented; last_period_complete = rounded-median fill-fraction (owner-sanctioned;
known limit: half-period resolution at coarse grains — sweep #1 + the 2026-02 corpus
acceptance arbitrate); alternate-anchor reconciles_with PARKED per edge-gate rule (needs a
new per-anchor closed-period comparison pass — structural note in lane report); oracle §T
spec drafted (engine-side, eval HOLD respected). **P5 (DAT-730) INTEGRATED** (`46b7bbb3f`) — temporal substrate LIVE: og_temporal_coverage
(declared_anchor, absence falls loud), WorkspaceCalendar (stamped default), level-keyed
og_rolls_up_to + fiscal-aware ladder, Concept.ordering, last_period_complete, collapse
fact-own-gated (review critical closed; validator gap fixed by level-keying the edge).
Union tree gates: engine 2835P, cockpit 1924P + check/typecheck/build. Mirror regenerated
once (workspace_calendar, ordering, lastPeriodComplete). Lane cleaned. origin/main
unmoved — no rebase owed.
**P6 (DAT-731) INTEGRATED** — og_additivity + og_has_additivity + og_measured_in live;
drill gate consumes the persisted DAG-aware verdict (heuristic = stamped fail-open
fallback; duplicate retired); cross-unit flag per-fact keyed (lane-reviewer critical,
independently re-verified with own repro). Union gates: engine 2838P (ruff now src+TESTS —
P5 had left a tests/ format drift my src-only gate missed), cockpit 1937P + all gates.
**fx ruling (owner, band-2 acceptance met WEAKLY):** measured_in + cross-unit flag = v1;
the structural conversion-grounding edge is unbuildable today (fx_rates has ZERO
relationships — two agents verified) → parked with shape: a conversion CONCEPT grounded on
fx_rates via the standard grounding path once declared (vertical/frame territory).
DAT-731's "fx_rates resolves as conversion grounding" line → lead resolves against reality
at close-out. **P7 (DAT-732) INTEGRATED** — metric DAG typed + walkable (og_metrics/og_metric_parameters
+ derives_from/has_parameter; declaration-versioned per DAT-728 axis; fault-isolated
seeding); _resolve_parameters reads the typed home; derivation markers on the 4
working-capital YAMLs. **TEMPORAL TRACK COMPLETE (P5+P6+P7 in).** Conflict resolution
lesson recorded: git factors shared tails OUT of conflict hunks — blind both-sides
stripping breaks parens/commas; resolve with the shared-tail closers restored, and ALWAYS
regenerate generated files from source after (the merge briefly carried a P7-side
schema_graph.sql missing P6 elements — caught by the mirror pull's dump_ddl, fixed,
zero-drift verified). Final union gates: engine 2853P + ruff(src+tests) + mypy; cockpit
all four gates, 1937P. Graph now: 14 edges, 10 vertices.
**Sweep #1 (clean + clean-flat) is NEXT and BLOCKED only on the lead's eval all-clear.**
**DAT-793 INTEGRATED** — grain block in the cached stable context (empty ⇒ no block);
post-hoc note stays as backstop; cockpit 1943P + all gates.
**DAT-789 INTEGRATED** (byte-identical to gated lane tree: engine 2861P, cockpit 1948P) —
conventions typed home + all three consumer swaps (validation asymmetry CLOSED: frame
conventions reach validation prompts — proven by test); frame authoring live
(ProposedConvention/writeConvention); ordering passthrough. Decisions: source CHECK =
seed|frame (teach excluded until a writer exists — DAT-802 discipline); frame-UI
conventions COLLECTION (stage-frame accept/edit round-trip) = parked follow-up, not
close-gating. Oracle drafts banked (4 lanes' worth now, eval HOLD).
**P8 (DAT-733) INTEGRATED** — og_validity_filter + og_scoped_by; default posted-only
injection riding the parts substrate (discriminating test: 300.0 posted vs 600.0
unfiltered); parse-confident bypass w/ typed assumption; imperative binding prose removed
(sweep-attention list #2); engine union 2883P. Oracle-conditioning DECISION (owner): the
two new MATCH shapes are CONDITIONED-HARD — hard-assert projection WHEN a measured status
cycle exists in detected_business_cycles, skip otherwise (cycle detection is the recall
oracle's job, plumbing is this one's). Banked with the oracle drafts (now 6 lanes' worth).
**P10 (DAT-735) INTEGRATED** — typed `validations` home (seed|generated + check_type CHECK
+ drift-guard vs the cockpit vocabulary contract; expected_formula stays overlay-only),
nine YAMLs seed-migrated verdict-faithfully, loader = DB ⊕ teach overlay (lossless legacy
normalizer), `validation_induction` phase in the OM spine w/ full 6+1 registration
(whole-graph serving incl. dedicated additivity + metric-DAG sections; dedicated config
key; 1 live probe: compiles/persists/membership-validates), nothing_declared structurally
unaffected (pinned twice). Mirror regenerated (validations). Union gates: engine 2913P +
ruff+mypy, cockpit 1948P + all gates. KNOWN LIMITATION (accepted): first-run induction
sees the PRIOR run's cycles/additivity — sweep interpretation note. YAML deletion rides
sweep #2 parity. Owner Fable pass caught + fixed the check_type=referential cross-package
break pre-merge (the CHECK now pins the vocabulary).
**DAT-788 INTEGRATED** — role-separated referenced conformed identity live end-to-end
(judge→bus_matrix→graph proven; spurious cross-role edge negatively pinned; bounded new
judge leg = cost note for wide corpora); engine union 2929P; mirror unaffected.
**DAT-856 INTEGRATED** — direction axis live end-to-end: CycleFamily seed→DB; contract
carries family+direction; Cut B identity (undetermined ⇒ canonical_type=family, grounding
a family artifact — the distinguishable detected-but-undirected state); loud off-vocab
degradation; family dedup; honest rendering both packages; tripwire green+extended.
2 probes: compiles / decidable→directed / starved→undetermined. Owner rulings at
integration: keep the _VERTICAL_SCOPED mirror (band-6 leak-safety); keep the raw
cycle_type emission (verbatim forensic artifact; authority = family+direction).
Union gates: engine 2960P, cockpit 1953P + all. Probe spend total this slice: 7 scoped
calls (4 W0-A, 1 P10, 2 DAT-856); sweeps 0/6.
**DAT-787 UNHELD + SPAWNED (Opus)** — lead-confirmed the derived-DimMember design (DAT-699
resolved: it's DONE under DAT-652; its item-7 landed value-set SERVING, not identity rows —
the citation over-read it). DimMember = derived vertex over typed where[] predicates keyed
(DAT-788 axis identity × normalized value); dimension-resolvable predicates only; where[]
stays lossless on the node. Persisted enumeration = NEW ticket DAT-864 under DAT-671
(consumer-gated exploration option, lead-suggested).
**Protocol (root-caused + FIXED 2026-07-22): Claude Code v2.1.217 disabled nested subagent
spawning by default. `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH=2` now set in
.claude/settings.local.json — verified live, applies to subagents spawned after the edit.
Lanes spawned from now on run their OWN two reviewers per the original /own discipline.
In-flight lanes (W0-A, W0-B, P5) predate the fix: they self-review; owner runs real
reviewers at their integration (W0-A's already done).** Probe LLM spend: 2 calls (band-1 scoped probe, sweep budget untouched: 0/6.) Scorecard: baseline = post-853 main @
3eaff9c06 (clean FULL PASS 135P / clean-flat 173P / detection-v1 171P+1F test_cycle_recall
AP — the DAT-856 carried acceptance); bands freshly rebaselined (dataraum-eval 9465db0,
seeds 46/47/48, merged engine). Substrate: grounding COMPLETE (3 readers, maps below).
Fable spend so far: owner thread only (spec pass + grounding synthesis); readers ran Sonnet.

## Open forks
- 871 pooling (same-provenance generated corroboration) · pre-registered criterion: on
  clean + clean-flat, NO column bands blocked from generated-only witnesses (any N, any
  scores); a lone seeded critical still blocks (0.95→risk 0.76); seed+generated may still
  escalate additively · reversal count 1 — reverses 865b acceptance #3 ("corroborated
  generated failure may escalate per pool semantics"), an owner-authored line, not
  lead-ruled; evidence: DAT-871 decomposition + pool.py:160-166 grounding · not escalated
  (owner-decidable; lead acked the plan with "go" 2026-07-23; lane in flight; criterion
  settles at the next eval re-run).
- 872: RESOLVED by owner measurement 2026-07-23 (third branch, neither pre-registered
  one) → see Decisions; lane in flight (judge-orientation hardening); attribution-routing
  design parked for lead triage.

## Parked
- Retype the DAT-447 expected_formula teach contract (parameters:{table,column,formula} →
  typed fields both packages) — the LAST producer of the legacy normalizer; its retirement
  unlocks deleting _normalize_legacy_check_fields. → DAT-869 first wave.
- Typed cycle_types vocabulary table + typed metric description/DAG-body home — would let
  teach_cycle/teach_metric drop their fs YAML reads (teach-surface lane, reviewer-verified
  gaps). → DAT-869 first wave.
- columns_used role tag (join-key vs compared-value) on ValidationSQLOutput — the CTC
  bystander blast radius; new expressive surface (865b investigation).
- ontology.yaml envelope parse on hot path (name/version/description + re-lint every
  load_workspace_concepts call; framed verticals get degraded metadata) — audit find.
  → DAT-869 first wave.
- temporalGateSource full UI surfacing beyond the withhold note → DAT-715.
- Posting-chain account-name evidence serving — slots with P10/DAT-856 (expected to make
  undetermined-AP rare); see DAT-856 interplay note.
- P6 absorbed scope (grounding find, blocks band 2): wire the first real consumer of
  additivity + retire cockpit drill-axes duplicate — P6 brief must cover it (cross-package).
- P10 × nothing_declared blast radius: zero GENERATED validations for a thin edge-graph
  must not fire `outcome=nothing_declared` (wide hand-mirrored cockpit surface) — P10 brief
  must address; band 6's fresh vertical is exactly this case.
- DAT-789 detail: validation_phase.py loads conventions from RAW YAML (not the DB-read
  path extraction uses) — the swap must cover it; cockpit conventions.ts is an fs-read →
  needs Drizzle mirror read (bigger cockpit change than engine side). Preserve
  "lint at author time only" (model_construct bypass, concept_store.py:203-208).
- DAT-788 sequencing: in close set but touches conformed identity — sequence after P6/bus-matrix
  surface is quiet to avoid cross-lane conflict.
- DAT-747 BridgeTable — not close-gating; absorb only if a lane covers it naturally.
- Catalogue `meaning` PROSE factually wrong on trial_balance columns ("cumulative…"
  where the closed table_role says periodic_snapshot — 870 lane grounding find; handled
  by ranking closed facts over prose in the binder). Possible catalogue_semantics
  sharpening: meaning prose must not contradict the closed facts. Semantic-authoring
  surface; triage at a checkpoint.
- graph_sql_generation.yaml has the SAME quoting-generalization gap W0-A fixed in
  validation_sql.yaml (narrower risk: parts contract forbids CTEs/persisted aliases, system
  appends `AS value`) — W0-A flagged, left untouched per brief; triage at a checkpoint.
(driver-list.tsx gap moved OUT of parked → in-scope, see State: owner brief missed a 4th
read site; abstained-as-"no significant driver" is the epic's core failure mode, lead
concurred 2026-07-22.)

---

## Substrate map (seeded from the 2026-07-22 re-grounding, code-verified @ 3eaff9c06)
- P5: `temporal_column_profiles` (window/granularity/periods/gaps; fiscal+update-freq DELETED,
  db_models.py:57-59) · `measure_aggregation_lineage.period_grain` = config echo, observed
  grain = `detected_granularity` · time_columns anchor contract enforced at LLM seam
  (models.py:284-306) but slicing_phase fallback bypasses validator (:327-336,:378-393) ·
  period_resolver already derives days_in_period live, flagged fallback stamped (DAT-785).
  Full notes: DAT-730 comment 2026-07-22. Old branch deleted (was cd200370b).
- P6: additivity = 2-axis verdict (additivity_db_models.py, (target_kind,target_key,run_id));
  NO graph projection (read-view only); no measured_in/conversion code; unit_source_column
  LIVE (authored by catalogue_semantics processor.py:138; 5 consumers).
- P8: `detected_business_cycles.status_column`/`completion_value` exist (db_models.py:66-67).
- P10: NINE validations in verticals/finance/validations/; SQL is LLM-generated, EXPLAIN gate
  at validation/agent.py:196 (DAT-858 defect lives there); conventions consumed by extraction
  (=graph, graphs/context.py:824-839), validation (validation_phase.py:209-226), cockpit-qa
  (prompts/conventions.ts).
- DAT-789: conventions[] = 3 prose entries in ontology.yaml:262-345; renderer ontology.py:245-280.
- DAT-856: cycle_type open string → canonical_type + is_known_type ((canonical_type,run_id),
  cycles/db_models.py:43-54); no direction field; direction prose in cycles.yaml (:52,:113,
  :357-374) behind de-leak tripwire (test_prompt_contract.py:80-99); chain-conditioned sign
  evidence served (cycles/context.py:669-718).
- Abstention primitive: entropy/models.py:93 (status/abstain_reason, nullable score,
  coverage measured|partial|unmeasured) — REQUIRED shape for new abstentions.
- Edge kind: RelationshipType.CONFORMED_DIMENSION, compound CHECK relationships/db_models.py:84-100.
- DAT-859 defect: drivers/processor.py resolve_target_type :82-101.

### Temporal track (grounding pass, 2026-07-22 — Sonnet reader, full report in agent output)
- **P6 ABSORPTION (blocks band 2's meaning): `current_metric_additivity` is WRITE-ONLY in
  production** — zero engine readers, zero cockpit readers; cockpit `drill-axes.ts:236-296
  temporalGate` independently re-derives a looser column-level rule from temporal_behavior
  (DAT-673/786). P6 = projection + wire the real consumer + retire the duplicate
  (cross-package: cockpit drill gate).
- P6 units: TWO unreconciled unit signals — semantic `unit_source_column` vs typing-layer
  Pint `detected_unit` (analysis/typing/units.py:113-183) — never cross-referenced; the
  measured_in edge must pick/reconcile. fx/currency: ZERO prior art anywhere (exhaustive
  grep) — clean-slate build.
- P5 calendar: DAT-783 deleted fiscal/update-freq as validated-WRONG, ceding territory to
  P5 — greenfield. Calendar ladder ⇒ NEW deterministic phase (dimension_hierarchies is the
  pattern: HierarchyMember{level} contract, db_models.py:40-64), NOT a temporal_phase
  bolt-on. `rolls_up_to` seam pre-announced at property_graph.py:49. Registration = 6-point
  checklist + 7th point: `_column_cleanup.py:59-69 column_keyed` tuple (FK-violation on
  re-run otherwise). Element-add pattern: _ELEMENT_VIEWS + _element_view_sql (::text keys)
  + _property_graph_sql + schema_graph.sql re-dump (CI drift gate) + own GRANT ON PROPERTY
  GRAPH.
- P5 one-home rule: anchor = `og_columns.anchor_time_axis` (witness > declared,
  property_graph.py:179-191, guarded by integration test :600-617) — never re-derive from
  time_columns. THREE distinct grain concepts coexist by design (profile cadence / hierarchy
  level / period_resolver live filtered window) — do not collapse.
- P7: `ParameterDef` (graphs/models.py:67-74) has NO persistence — typed home is greenfield
  (follow additivity_db_models.py precedent). Keep declared-default vs data-derived-override
  split (period_resolver keeps overrides OUT of the parameter object). Runtime merge point:
  graphs/agent.py:1342-1351 `_resolve_parameters`. days_in_period params live in 4
  working-capital metric YAMLs (dso/dio/dpo/ccc).
- Registration enforcement tests: tests/unit/worker/test_worker_registration.py (AST diff) +
  test_phase_constants.py; integration stub: tests/integration/worker/test_progress_query.py
  :60-135 (stubs must be added there too or progress test silently no-ops).

### Wave-0 + 856 + gap tickets (grounding pass, 2026-07-22 — Sonnet reader)
- **DAT-858:** NO repair turn exists (single LLM call, agent.py:283-389; EXPLAIN bind :196 →
  ERROR → artifact stays `declared`, visibly ungroundable). **Repo doctrine: NO in-place SQL
  text repair** (DAT-671 killed sql_repair — llm/config.py:73, query/execution.py:11,113,170,
  203) → bind-side auto-quoting is the rejected anti-pattern. Fix layers: sign_conventions.yaml
  sql_hints wording (:31-52, steers toward `natural` token) + validation_sql.yaml quote
  instruction (:20-26 — covers columns, NOT aliases) + presentation-side quoting (resolver.py
  schema formatting). TWIN prompt block: graph_sql_generation.yaml:193-278 has an independent
  copy of the conventions piping — don't drift them. Quote-at-use-site precedent:
  views/builder.py:160-166, drivers/processor.py:420-421.
- **DAT-859:** fix = two-layer status pair on DriverRanking + DriverRankingArtifact
  (`status measured|abstained` + `abstain_reason`, enum+CHECK per DAT-781, drivers-local — do
  NOT import entropy machinery; precedent hierarchies/db_models.py:31-37,131-133). Construction
  sites: processor.py:409,793,890 + resolve_target_type. THREE read sites to make consistent:
  engine graphs/context.py:634-653 + _append_drivers:2065-2099 (emits empty headings today!),
  cockpit look-drivers.ts:154-187 (no filter), drill-axes.ts:478-480; cockpit query-context.ts
  formatDrivers:892-897 already filters — align on ONE convention. test_processor.py:126
  asserts the silent default — must flip. Cross-package (Drizzle re-dump).
- **DAT-856 corrections:** persist site = business_cycles_phase.py:309-356 (NOT
  validation_phase — earlier note wrong). map_to_canonical_type FALLS BACK to preserving raw
  LLM type as canonical (config.py:98-100) — off-vocab silently becomes its own bucket. NO
  CHECK on cycle_type/canonical_type today. family/direction resolution belongs in
  map_to_canonical_type; two-layer per hierarchies precedent.
- **DAT-787 DEPENDENCY HOLE:** DimMember does not exist; ticket-cited DAT-699 "explorable
  value-sets" exists NOWHERE in code (engine or cockpit) — confirm in Jira before the lane
  opens; may need re-tiering (not Sonnet·medium if identity design is real work).
- **DAT-788 corrected sites:** write slicing_phase.py:417-465 (parse :435-446), projection
  og_has_dimension property_graph.py:334-349, fold collapse lineage/processor.py:327-391
  (:370-390). Verdict home: BusMatrixEntry (hierarchies/db_models.py:219-324, bus_matrix).
- **DAT-793 seam:** buildGrainBlock alongside buildDriversBlock (query-context.ts pattern),
  joins Promise.all at query.ts:585-603 + stableContext :621-623; reuse loadNearUniqueColumns
  (grain-note.ts:89-108); update query-subagent.contract.test.ts. Align empty-artifact
  convention with DAT-859.
- Cross-cutting: line-number drift in older planning notes — lanes re-grep, never trust cited
  lines. Naming collision: analysis/temporal/ (stock/flow) vs Temporal (workflow engine).

## Plan (living)
Two tracks after wave-0; DAT-736 continuous alongside.
- Wave-0: DAT-858 (Sonnet·medium) + DAT-859 (Sonnet·high — touches abstention semantics).
- Temporal track: P5 (Fable·xhigh — evidence/prompt-adjacent + design freedom) → P7
  (Opus·high). P6 (Opus·high) parallel to P5 where surfaces don't overlap.
- Conventions track: DAT-789 (Opus·high, cross-package) → P8 (Opus·high) + P10 (Fable·xhigh —
  generated validations are semantically graded) → DAT-856 (Fable·xhigh — prompt/contract).
- Gap tickets: 787/793 (Sonnet·medium), 788 (Opus·high) — slot when adjacent surface quiet.
- LAST lane (band 6): second dataset + second vertical (Fable·xhigh — vertical design +
  leakage judgment; done-spec written in the brief; needs the rewired eval).
- Reviewers: lane tier or one above; Fable for semantic lanes; never below Sonnet·high.

## Lane briefs

### DAT-872 — resolution-driven FK orientation (Opus · high) [fix wave 4]
Goal: a confirmed reference edge is stored in the direction its MEASURED child→parent
resolution supports, not the direction uniqueness implies; opposite-orientation dual
emissions and any divergence from the judge's named direction are recorded on the row
so the eval can measure incidence (DAT-853 ~25% flips). ROOT CAUSE (owner-grounded,
corpus-verified — the eval's "field-swap" story is observationally right but causally
incomplete): there is ONE synthesis call per run, not per-table judges (the two
"judges" are two entries in one output array, invited by the prompt's "add
relationships not in the candidate list", models.py:338-341). The false alarm is a
DETERMINISTIC engine bug: oriented_row (relationships/db_models.py:226-349), m:1 flip
block 319-336, orients by CARDINALITY/UNIQUENESS (FK on the non-unique side); for the
recurrence pair the child resolves 100% one way and 26.7% the other, but the non-unique
side is the PARENT axis (many child rows per parent key), so uniqueness picks the wrong
direction → backwards edge → 73% orphans → 0.733315 alarm. Both opposite emissions
canonicalize onto the SAME uniqueness-chosen row (grounding), and _first_wins
(processor.py:441-464, folded at 876-878) then keeps one by LIST ORDER — zero test
coverage (grep-confirmed). So a prompt-only fix provably cannot hold: even a correct
emission is re-corrupted downstream. (Owner verified from the corpus CSVs, no eval run:
child→parent resolution is 100% one direction vs 26.7% the other — a clean FK whose
parent axis is legitimately non-unique. This number is verification only — it must NOT
appear in code/comments.) Fix (3 parts, all engine — the removed-mechanical-swap
doctrine at db_models.py:259-303 is EXTENDED, not reversed: it said the swap "can't
tell a clean-subset child from an orphan-carrying one" — measured resolution IS that
signal):
(a) oriented_row: when orienting a CONFIRMED reference claim (foreign_key/hierarchy),
choose the stored direction by the strictly-higher measured referential-integrity /
resolution rate (both directions already live in evidence: left_/right_referential_
integrity, left_/right_key_coverage). CONSERVATIVE: only override when resolution data
is present AND asymmetric beyond a structurally-justified epsilon; absent or
within-epsilon-symmetric ⇒ fall back to the judge's named direction (this preserves the
1:1 no-mechanical-swap tie case and every detector-candidate path that already orients
by measured cardinality). Record orientation provenance on evidence (measured vs
judge-named; swapped yes/no). New evidence keys MUST be added to _SYMMETRIC_EVIDENCE_KEYS
(db_models.py:413-437) or be left_/right_-prefixed or swap_directional_evidence raises.
(b) merge conflict: at the _first_wins fold, DETECT two emissions describing the same
unordered column pair in opposite orientation; resolve by the SAME measured criterion
(not list order); record the conflict + resolution on the surviving row's evidence.
(c) unwind wave-3 v3.2.0 in semantic_per_table.yaml: direction is no longer the
prompt's job — the engine now orients deterministically by resolution, so REMOVE the
m:1 "fast path settles direction by uniqueness" and the "decline as shared axis on m:1"
gate (it would suppress a clean FK on a non-unique parent axis). The prompt's job is the
CLAIM (is this a real reference — does the child resolve cleanly in AT LEAST ONE
direction?); keep the "confirm + NAME orphans for a genuinely dirty FK" escape hatch;
the real decline case is a pair whose child mostly fails to resolve in BOTH directions.
Do NOT reintroduce the removed mechanical-swap doctrine. PRE-REGISTERED CRITERION (fork
discipline): store the direction with strictly-higher measured child→parent resolution,
tie→judge's named direction; a genuinely-symmetric or genuinely-dirty pair still
declines/confirms-with-orphans-named. HARD FENCES: no corpus names/numbers in
code/comments; the resolution comparison is a COMPARISON — no tuned threshold beyond the
tie-epsilon, and justify the epsilon structurally never by corpus; do NOT touch
joins.py / candidate detection / finder.py; do NOT touch entropy/, analysis/validation/,
graphs/context.py (876's surface) or dataraum-config/verticals/*/validations (band-3's
surface). Tests: NEW _first_wins coverage (exact-dup + opposite-orientation dual
emission), oriented_row resolution-driven orientation + conservative fallback, the
recorded-conflict evidence key; the full engine suite (3003) is the chokepoint
regression net. Probes: ≤2 scoped live calls ONLY if a structured-output change needs
compile verification (expect 0 — engine logic, not schema); report each. Eval data
access: fresh stack, ws ids (clean=ws_607cb3ee…/d4420201…/077b4eea…, flat=ws_4ae638d7…),
port 5433 dataraum/dataraum; CSVs /Users/philipp/Code/dataraum/dataraum-eval/data/clean*/.
Reviewers: senior (Opus) + spec-compliance (Opus), run SYNCHRONOUSLY. Model: Opus·high.

### DAT-725 band-3 — retire seeded validation YAMLs (full delete, Sonnet · high) [band 3]
Goal: the 9 seeded finance validation YAMLs are retired ENTIRELY (engine seed AND
cockpit onboarding few-shot); LLM induction becomes the sole validation source; NO silent
regression anywhere. Lead-ruled full delete (2026-07-23): the finance few-shot IS finance
vocabulary — leaving it reachable by other verticals' onboarding induction is exactly the
leakage band 6 forbids, so it goes on BOTH sides. Removal set (owner-grounded, exact):
delete all 9 files in packages/dataraum-config/verticals/finance/validations/ + the now-
empty dir (double_entry / gl_invoice_match / orphan_transactions / payment_bank_match /
sign_conventions / stage_date_ordering / tb_gl_reconciliation / three_way_match /
trial_balance). Engine: NO loader code change needed — VerticalLoader.shipped_base /
ensure_validations_seeded / load_all_validation_specs all degrade to empty cleanly
(FileNotFoundError→empty_base; phase surfaces "no_declared_validations" loudly one level
up, validation_phase.py:120-128). Adapt tests (the real work — assertions hardcoded to
shipped content; rewrite to SYNTHETIC seed via direct Validation(source="seed") DB insert
or a tmp_path verticals_dir fixture, NEVER the real dir): test_validation_store.py (6
tests: seed-count==9, tolerance/params-fold, idempotent, load-count, source==seed,
generated-collision-skipped), test_config.py (TestLoadAllValidationSpecs +
TestOverlayAwareLoading + all 4 TestGetValidationSpecsForCycles + the UNIVERSAL_IDS
fixture 159-167), test_validation_phase.py:183 (replace ensure_validations_seeded with
persist_generated_validations of one synthetic spec — the pattern used two tests above
it). Reword STALE docstrings to stop asserting present-tense "shipped YAML" (KEEP the
normalizer — DAT-447 expected_formula overlay is a live producer — reword only its "nine
seed YAMLs" clause): models.py ValidationCheckType 39-53 + _normalize_legacy_check_fields
118-126 (keep orphan_transactions as a historical design-note precedent, not present
tense), db_models.py 50+106, test_models.py header 1-9. Cockpit (full-delete side, tests
MOCK the config path so they won't catch behavior drift — the reviewer is the net):
frame.ts induceValidations (379-403) → nearestSeedVertical → readShippedValidations now
returns [] for every vertical; implement the lead ruling = a newly-onboarded vertical gets
NO finance few-shot (remove the finance-few-shot seeding from onboarding; if
readShippedValidations becomes fully dead, delete it, else keep with a reworded purpose);
update stale comments (validation-induction.ts:42-49 "the 9 shipped specs",
validation-spec.ts ShippedValidationSpec, teach-validation.ts header). dataraum-config:
ontology.yaml:268 — remove the now-vestigial "validation:sign_conventions" from
sign_natural_balance targets (the 865a pull-side relevant_conventions covers it; the
push-route dies once no validation is guaranteed to be named sign_conventions);
README.md:26 — drop "validations" from the layout comment. HARD FENCES: KEEP the
normalizer + its tests (only reword docstrings); do NOT touch the induction/binder prompt
YAMLs (876's surface) or relationships/semantic (872's surface); no corpus vocabulary
ADDED anywhere; verify the nothing_declared promote gate (workflows.py:895-921) still
behaves — finance keeps cycles+metrics seeds so it can't fire on validations alone, but
confirm with the existing test. Gates: FULL engine set AND full cockpit set
(check/typecheck/test/build — this touches cockpit). Probes: 0 (no LLM serving change).
Reviewers: senior (Opus) + spec-compliance (Opus), run SYNCHRONOUSLY. Model: Sonnet·high.

### DAT-876 — existence-check coverage universe (Opus · high) [fix wave 4]
Goal: generated existence checks never grade structural absence as a violation — an
existence claim binds only against a table that authoritatively enumerates the entity;
when the served schema contains none, the honest outcome is decline (binder) /
don't-propose (induction), loudly. Evidence (DAT-876, clean-flat, pin `380998e34`, run
head-resolved): gl_parent_account_exists + trial_balance_parent_account_exists,
deviation 13 / tolerance 0 → CTC 0.5-band on their columns; the 13 "missing parents"
are roll-up accounts folded into the fact rows' own parent attributes — they never
post; absence from activity ≠ nonexistence. Class regenerated across three prompt
versions and BOTH existence blocks carry the same loophole (owner-grounded):
"…or the declared target of a served reference/relationship"
(validation_induction.yaml:108-113, validation_sql.yaml:220-231) — satisfied by the
deterministically-detected SELF-referential hierarchy edge (parent_account_id →
account_id on the SAME fact table, DAT-763; served with no same-table exclusion to
both layers via context.py:_read_references and resolver.py formatted_rels), whose
target is the activity table itself — the exact anti-pattern the adjacent sentence
forbids. Fix layers: (a) close the loophole in BOTH prompts (minor bumps): the
served-relationship branch qualifies ONLY when the target table is itself
dimension/master-role; a self-referencing edge on a fact/periodic_snapshot table never
establishes an enumerating universe. (b) Serve absence as a positive fact (DAT-870
grain_facts blueprint — a typed-projection of table_role, no heuristic): when the
served schema has NO dimension-role table, render that explicitly at BOTH layers —
binder: _render_grain_facts (agent.py:44-90); induction: the served-context table
rendering (graphs/context.py) — "no served table authoritatively enumerates entities;
existence checks are unbindable here" in house voice; absence falls loud, never
inferred from omission. (c) Tests: pin the serving both ways (fact renders when no
dimension-role table; absent when one exists) + prompt-content tripwires. HARD FENCES:
no per-check special-casing; no corpus names/numbers in code or prompts; do NOT parse
spec prose/SQL to classify checks (forbidden); do NOT add fields to the induction
output contract — the typed existence-target declaration (865a relevant_conventions
blueprint) is PARKED as the pre-registered next step if the class regenerates under
(a)+(b); no fourth prose-only attempt. Do NOT touch semantic_per_table.yaml, entropy/,
relationships/ detection; resolver.py only if (b) strictly needs it. Probes: ≤2 scoped
live calls (compile class) only if structured-output-adjacent serving changes; report
each. Eval data access: fresh stack, same ws ids (clean=ws_607cb3ee…/d4420201…/
077b4eea…, flat=ws_4ae638d7…), port 5433 dataraum/dataraum; CSVs
/Users/philipp/Code/dataraum/dataraum-eval/data/clean*/. Reviewers: spec-compliance
(Opus) + senior (Fable), run synchronously.

### DAT-874 — flow-vs-stock comparison discipline (Fable · xhigh) [fix wave 3]
Goal: induced checks never compare a movement (additive) directly against a level
(point_in_time) — coherent forms or abstention. Evidence (DAT-874, clean, to the cent):
trial_balance_reconciles_balance_sheet_ending_balance compares TB periodic net vs BS
cumulative ending_balance; 1110|2026-01 decomposes exactly; join grain CORRECT (870's
table facts did their job). The veto fact exists this run, DETERMINED:
column_concepts.temporal_behavior — additive (tb.debit/credit_balance) vs
point_in_time (bs.ending_balance) — but graphs/context.py NEVER renders
temporal_behavior into the served context (owner-verified: zero render sites), so
induction authors blind; the binder's <grain_facts> callout DOES render the behaviors,
but no prompt rule covers CROSS-comparing them. Fix layers: (a) serve column
temporal_behavior in the induction served context (graphs/context.py column rendering;
the DAT-657 data-reconciled verdict; determined values only — absence falls loud,
house abstention style); (b) validation_induction vNEXT: comparing two measures
requires temporal-behavior coherence — movement vs Δlevel, or cumulated movement vs
level, never movement vs level directly; no groundable coherent form ⇒ do not propose;
(c) validation_sql vNEXT: the same rule binder-side (facts already served). HARD
FENCES: no corpus names/values in code/prompts; no per-check special-casing; do NOT
touch entropy/, semantic_per_table.yaml, relationships/, resolver.py beyond what (c)
strictly needs. Eval data access: fresh stack, same ws ids (clean=ws_607cb3ee…/
d4420201…/077b4eea…, flat=ws_4ae638d7…), port 5433 dataraum/dataraum; CSVs
/Users/philipp/Code/dataraum/dataraum-eval/data/clean*/. Probes: ≤2 scoped live calls
(compile class), report each. Reviewers: both, Fable-tier.

### DAT-872 re-cut — claim-level containment veto (Opus · high) [fix wave 3]
Goal: a foreign_key claim whose child side mostly fails to resolve is not persisted as
a clean FK at ANY measured cardinality. Wave-2's v3.1.0 veto sits in the ONE-TO-ONE
section and never fired: the recurrence pair measures MANY-TO-ONE (owner-verified,
fresh stack: gl.entry_id NON-unique — 25172 rows/11754 distinct; invoice_data
near-unique parent — 3000/2860; judge re-declared gl.entry_id→invoice_data.entry_id
foreign_key m:1 conf 0.85; child-side containment 26.7%). The m:1 fast path ("the
measurement settles it: the FK is the non-unique side") settles DIRECTION among true
references — it must not settle the CLAIM. Fix (semantic_per_table vNEXT, judgment
layer only — the removed-mechanical-swap doctrine stands): extend the
decisive-asymmetry discipline to EVERY reference claim regardless of cardinality — a
child resolving ~27% into its claimed parent is not a clean FK; honest outcomes:
(1) decline the foreign_key claim; (2) claim the shared-axis kind where that is the
honest reading (two fact-shaped tables meeting on an id = conformed_dimension — note
the orphan seed check ALREADY excludes conformed_dimension legs by guidance, ending
the false-alarm cascade); (3) genuinely-dirty FK ⇒ confirm + NAME the orphan problem
(the v3.1.0 escape hatch, generalized beyond 1:1). Also note the claimed parent is
only NEAR-unique (2860 distinct/3000 rows) — surface what the served numbers say
about that if visible, but do NOT touch joins.py/candidate detection (fence). HARD
FENCES: wave-2 brief's fences + do not touch analysis/validation/, entropy/,
graphs/context.py (874's surface). Probes: ≤2 scoped live calls (compile class),
report each. Reviewers: spec-compliance (Opus) + senior (Fable).

### DAT-870 — binder grain-blindness + sign-class close (Fable · xhigh) [fix wave 2]
Goal: generated validation SQL stops contradicting the run's own catalog on
grain/coverage/period-boundary, and the sign-flip class is closed at its real layer.
Defect classes (DAT-870 decomposition, all on CLEAN corpora): (1) cumulative sums
against periodic_snapshot tables; (2) one-sided natural-balance normalization of an
already-consistent signed comparison — the regenerated check DECLARED
sign_natural_balance (owner-verified on the pinned run), so 865a's routing WORKED; the
convention TEXT misleads: it instructs normalization without stating how the data is
stored; (3) month-start boundary cut (`x.date <= period` where period = the month-START
label); (4) structural absence graded as violation (parent-existence checked against
transaction tables when no master list exists). Root causes grounded (owner 2026-07-23):
resolver.py serves NO table_role/TableEntity or grain fact to the binder (induction's
served context DOES label tables); validation_sql.yaml v2.0.0 lacks the
stock/flow-period block graph_sql_generation.yaml:169-187 carries; induction v3.0.0's
abstain list covers sign/netting/ordering only. Fix layers (ANALYSIS FIRST per the 865a
discipline — no patching to the data): (a) serve table_role + period-grain facts into
the binder schema (resolver.py; include what the period column MEANS —
detected_granularity/period semantics — where the catalog has it); (b) validation_sql
v3.0.0: periodic-vs-cumulative + full-period-window instructions + comparisons compare
like-with-like (normalize both sides identically or neither; normalization is for
checks ABOUT natural balance, not reconciliations) — keep the graph twin conceptually
aligned, never blind-copy; (c) validation_induction.yaml: extend abstain-rather-than-
guess to grain/coverage/period-boundary; a parent-existence check with no served master
table ⇒ don't propose (or bind abstains); (d) sign_natural_balance text
(ontology.yaml:267-279): analyze whether it needs a stored-convention clause — vertical
data change allowed if the analysis demands it, flag it in the report. HARD FENCES: no
corpus names/values in code/prompts/seeds; no per-check special-casing; no
deterministic SQL rewriting (DAT-671 doctrine). Eval data (verification ONLY — the
corpus is never the argument): postgres localhost:5433 (or `docker exec
dataraum-eval-postgres-1 psql -U dataraum -d dataraum`), clean-flat schema
ws_4ae638d7_0acd_5833_b977_23574850c07c, clean seeds ws_607cb3ee…/ws_d4420201…/
ws_077b4eea…; failing SQL in current_validation_results.sql_used; CSVs
/Users/philipp/Code/dataraum/dataraum-eval/data/clean*/. Surface:
analysis/validation/{resolver,agent,induction}.py, validation_sql.yaml,
validation_induction.yaml, their tests; ontology.yaml convention text per (d). Do NOT
touch: entropy/ (871's), semantic_per_table.yaml + relationships/ (872's),
property_graph.py. Probes: ≤4 scoped live calls (bind/induction compile class), report
each. Reviewers: both, Fable-tier.

### DAT-871 — correlated-witness cap (Sonnet · high) [fix wave 2]
Goal: generated-only witnesses can never band a target blocked, at any N. Owner ruling
(fork pre-registered): the generated-tier contribution to CTC's evidence mass = MAX
over generated witnesses, not Σ; seed tier stays additive (distinct human-reviewed
templates = independent logic; same-draw generated checks share generator + served
context = correlated). Implementation home: cross_table_consistency.py where
evidence_mass is consumed (table grain ~:386, column grain ~:468) — do NOT change
pool() itself (generic ADR-0009 machinery, other detectors calibrated on it). All
witnesses stay persisted in claims (no evidence hiding); the persisted evidence fields
must reflect the effective capped mass honestly — design the exact shape, test-pinned.
ACCEPTANCE (all test-pinned): (1) lone seeded critical → blocked (0.95 → risk 0.76,
unchanged); (2) N≥2 generated failures alone, full strength → NEVER blocked, risk ≤0.4;
(3) seed + generated → additive escalation still possible (0.95+0.5 → capped 1.0 →
blocked); (4) worst-wins rollup untouched; (5) all witnesses persisted. Also: rewrite
reliabilities.yaml's CTC comment block — its "two independent generated witnesses
corroborating escalate additively → blocked" rationale is falsified by DAT-871; state
the max rule honestly; update the cross_table_consistency.py module docstring the same
way; test_corroborated_generated_failures_escalate pins the OLD additive behavior —
adapt it (tests follow design). Surface: cross_table_consistency.py,
tests/unit/entropy/test_cross_table_consistency.py, dataraum-config/entropy/
reliabilities.yaml (comments), pooling/models.py docstring note if needed. Do NOT
touch: pool() math, loss.py/loss.yaml bands, analysis/validation/ (870's), semantic/
(872's). No LLM calls. Reviewers: both, Opus-tier or above.

### DAT-872 — FK orientation honesty at the judge (Opus · high) [fix wave 2]
Goal: a reference claim oriented AGAINST decisive containment asymmetry must not
persist as a clean foreign_key. Evidence (owner-measured, pinned clean-flat run): the
judge declared general_ledger.entry_id → invoice_data.entry_id (foreign_key, 0.75) —
BACKWARDS: invoice_data.entry_id ⊆ gl = 100.0%, reverse 24.3% (CSV-verified 2860/2860
vs 2860/11754); the seed orphan check then faithfully measured the backwards leg →
the 0.733 false alarm (DAT-872). The judge HAD the directional evidence (left_/right_
metrics served in both orientations — semantic/processor.py:100-155 flip helper) and
the orientation rule (semantic_per_table.yaml:126-165); dependence-first with
numbers-as-corroboration let a wrong dependence intuition override a 100-vs-24
asymmetry. REQUIRED READING before design: oriented_row's docstring
(relationships/db_models.py:229-300) — the mechanical 1:1 containment swap was REMOVED
this epic for solid reasons (clean-subset-child vs child-with-orphans are
containment-indistinguishable); do NOT reintroduce it; the fix is at the judgment
layer. ANALYSIS FIRST, then candidates (settle in /refine): (a) sharpen the
orientation section: decisive asymmetric containment (~100% one way, clearly lower
reverse) on a 1:1 is stronger than corroboration — orienting against it means either
flip or DON'T claim foreign_key (a reference whose child side is ~76% orphaned is not
a clean FK; claim what it honestly is, or decline); make the extension-table tell
explicit (the smaller table whose key ⊆ the larger's is the detail/extension = child —
the prompt's own dependence examples already decide this case; tie them together);
(b) consider a LOUD FLAG (not an auto-fix) when a persisted foreign_key's own carried
evidence contradicts its direction — judgment stays with the LLM (no-algorithmic-
scoring house rule). Version-bump semantic_per_table.yaml. HARD FENCES: no corpus
names/values in prompts/code; no mechanical re-orientation; no changes to candidate
detection/joins.py. Eval data access: as in 870's brief. Surface:
semantic_per_table.yaml (version bump), semantic/processor.py serving text if needed,
relationships/db_models.py docstring/flag per (b), tests. Do NOT touch:
analysis/validation/ (870's), entropy/ (871's), oriented_row's orientation semantics.
Probes: ≤2 scoped live calls (compile class), report each. Reviewers: spec-compliance
(Opus) + senior (Fable — graded prompt surface).
PARKED NOTE (lead-visible): the attribution layer — a failed referential check on a
declared FK is evidence AGAINST the declaration (the ADR-0009 reading), not
automatically bad data; routing that disagreement onto the relationship claim is a
design conversation (closeout / DAT-671 / DAT-869 triage), not this lane.

### DAT-865a — induction sign grounding (Fable · xhigh) [sweep-fix wave]
Goal: generated validations derive leg SIGNS from declared conventions, never LLM guesses.
Defect: `balance_sheet_ending_balance_matches_journal_lines` flips natural-balance sign on
one leg only (liability leg; deviation = |ending| + |journal cum| — sum-of-magnitudes
signature). Fails on CLEAN data; EXPLAIN/membership structurally cannot catch it.
ANALYSIS FIRST (lead directive: "analyse well, do not patch to the data"): (1) what
evidence about sign/natural balance does validation_induction currently serve? (2) what do
the seeded conventions actually declare — can the convention vocabulary EXPRESS
natural-balance-per-account-class at all? If not, that is the real bug (fix the data
model: vocabulary/seed first). (3) only then the prompt/serving change. HARD FENCES: no
account numbers/names or corpus values in code or seeds beyond the vertical's generic
vocabulary; no balance-check special-casing; abstention discipline — a sign the declared
conventions do not determine ⇒ do NOT emit the check (absence falls loud), never emit a
guessed sign. Surface: analysis/validation/induction.py, validation_induction.yaml
(version bump), the P10 whole-graph serving section, dataraum-config conventions
seeds/vocabulary if the analysis demands. Do NOT touch: storage/property_graph.py,
drivers, cycle stores. Probes: ≤2 scoped live calls authorized (compile/persist class),
report each. Reviewers: both, Fable-tier.

### DAT-865b — CTC onto the witness pool (Sonnet · high) [sweep-fix wave]
Lead ruling (2026-07-23): the categorical 1.0 for a critical failure is BY DESIGN and
stays; the fix is routing cross_table_consistency onto the ADR-0009 witness pool with
check PROVENANCE (validations.source: seed|generated — the typed home P10 built) as the
reliability axis. Mirror the sibling derived_value (the pooled computational detector);
CTC is the outlier on the raw {score} shape. Keep it simple — the seams are pre-mapped
(865b investigation, 2026-07-23): cross_table_consistency.py _score/detect,
EntropyObject.witnesses, loss.yaml CTC row, reliabilities (if priors live there),
provenance threading from the validations home into the detector's spec/record path
(typed, no string sniffing). ACCEPTANCE (test-pinned, all four): (1) seeded critical
failure ALONE → blocked (today's behavior, unchanged); (2) generated check failure
ALONE → investigate, NEVER blocked (the DAT-865 clean-corpus scenario); (3) corroborated
generated failure may escalate per pool semantics; (4) worst-wins rollup untouched
(loss.py/readiness_context.py aggregation unchanged). No new heuristics — existing
Witness/pool() machinery only. Surface: entropy/detectors/computational/
cross_table_consistency.py + entropy tests + dataraum-config entropy/loss.yaml (+
reliabilities). Do NOT touch: analysis/validation/induction.py + validation_induction.yaml
(865a's), property_graph.py (867's), _score's critical branch semantics for seeded.
Reviewers: both, Opus-tier or above. No live LLM calls.

### teach-surface hard retire (Sonnet · high) [audit wave — unblocks band 3's YAML deletion]
Goal: the cockpit's three teach tools stop reading dataraum-config YAML via fs and stop
writing the legacy overlay shape — the retire-hard cut the 2026-07-23 audit showed is the
REAL blocker for band 3's "validations YAML deleted" line. Cockpit-only lane:
(1) teach-validation.ts readShippedValidations, teach-metric.ts, teach-cycle.ts: replace
the readdir/readFile shadow-detection reads with Drizzle reads of the ALREADY-GENERATED
views in src/db/metadata/schema.ts (validations, metrics + metricParameters,
cycleFamilies — currently zero consumers). "Shipped" for override/shadowed_spec purposes
= rows with source='seed' (the views carry source; NEVER compare against generated rows).
Views are vertical-scoped by the read layer — do not re-filter by vertical client-side.
(2) validation-spec.ts ValidationSpecSchema: replace legacy parameters/sql_hints with the
typed tolerance/guidance shape (DAT-735) so teach_validation writes the typed fields into
config_overlay directly. No-backwards-compat rule applies: existing legacy overlay rows
are NOT migrated; the engine-side legacy normalizer deletion is the OWNER's commit after
this lane and 865a both integrate (models.py is 865a-adjacent — do not touch engine code
in this lane). Tests: adapt the teach-tool + validation-spec tests; mock the metadata
client per the house vitest pattern. Do NOT touch: packages/engine anywhere,
dataraum-config anywhere, drill-axes.ts. Reviewers: both, Opus-tier or above.

### drill-gate withhold cut (Sonnet · high) [honesty wave — lead-ruled 2026-07-23]
Goal: DELETE the drill-axes temporal heuristic; the time gate has exactly two sources —
the engine's persisted additivity verdict, or an honest visible withhold. Lead ruling:
"if we do not have data, we honestly say so"; the silent fallback is the epic's core
failure mode. Design (per the 2026-07-23 feasibility map): missing/null verdict ⇒ strip
time grain + a USER-VISIBLE reason ("additivity not determined for this target") — new
client rendering near the time-grain control (drillable-grid.tsx / analyse-overlay.tsx;
the reason fields are currently dead on the client — wire them). Delete the fallback
inventory: TemporalBehavior/OffendingCause/OffendingColumn types, offendingCause/
temporalGate/phraseOffender/describeTemporalGate, the resolveDrillAxes else-branch
(behaviorByColumn + console.warn), couldNotDetermine tracking, the temporalBehavior
column selection (currentColumnConcepts join STAYS for unitSourceColumn), the
"heuristic-fallback" union member. Adapt/delete the ~10 fallback tests; add
withhold-shape tests (verdict present flow / verdict stock / verdict missing ⇒ withheld
+ visible reason). Unit gate (DAT-731) untouched. Engine verdict-total (abstention rows,
universe extension) is NOT this lane — it's DAT-671; the cockpit read stays 2-state
until then. Cockpit-only; do NOT touch packages/engine. Reviewers: both, Opus+.

### DAT-867 — folded dim-member axes (Opus · high) [sweep-fix wave]
Goal: og_dim_members/og_filtered_by must not zero out on folded/OBT corpora (sweep #1:
37 rows referenced vs 0 flat). Owner ruling: referenced-only was deliberate v1; extend
the axis identity to FOLDED attribute axes. ANALYSIS FIRST: read bus_matrix.py's folded
attachment cells — reuse ITS identity signature for the folded conformed_group, do not
invent a parallel one; read current_slice_definitions for folded slices
(dimension_table_id NULL). Keep the DAT-787 invariants: vertex KEY and edge DESTINATION
KEY inline the SAME expression; DISTINCT ON total; typed filter_members only (never parse
where[]); content-derived key (no per-run ids). Tests: unit shape pins + integration
fixture folded case (grounding with filter_members on a folded fact → rows appear).
Branch from epic tip AFTER 1eabf5d00 (DAT-866 touched property_graph.py — no other lane
touches it concurrently). Do NOT touch: og_temporal_coverage, induction surface (865a's).
Reviewers: both, Opus-tier or above.

### W0-A · DAT-858 sign_conventions generation fix · Sonnet · reviewers: spec-compliance
(opus) + senior (FABLE — prompt delta is semantic)
Goal: sign_conventions LLM-generated SQL EXPLAIN-binds clean on the finance corpus, all
target tables assessed, no other validation regresses. Surface: sign_conventions.yaml
sql_hints wording · validation_sql.yaml quote instruction (extend to ALL identifiers incl.
aliases/CTE names) · optionally resolver.py schema-presentation quoting · validation tests +
prompt-render smoke. MUST NOT: post-generation SQL text rewriting (DAT-671 doctrine); no new
repair turn (fork → ask); don't drift graph_sql_generation.yaml's twin block (flag if
implicated); no substance/tolerance relaxation. Verification: deterministic tests + LIVE
scoped probe (~2-4 real-LLM calls, pre-authorized band 1, report count) — blocked > green
mocks. Branch feat/dat-725-858-sign-conventions, worktree lane-dat-858.

### P5 · DAT-730 temporal coverage & calendar · Opus·high · reviewers: senior (opus) +
spec-compliance (opus); owner Fable review at integration
Goal: temporal coverage + calendar as typed graph structure — per-(relation × time column)
coverage (window, observed grain = detected_granularity, last-period completeness), anchor
consumed from og_columns.anchor_time_axis ONLY; DECLARED workspace calendar (typed home
sibling to active_vertical, v1 year-start; unset ⇒ calendar year stamped as default; generic,
domain-free; NO detection); rolls_up_to unnested from dimension_hierarchies (level = sole
direction) + fiscal-aware period-grain ladder via bounded recursive CTE; ordered|nominal
typed on DimensionConcept (default nominal, withholds windows); DAT-812 profile-read
collapse for unfiltered flows (flagged fallback intact); new DETERMINISTIC phase if the
calendar ladder needs one (6+1 registration + enforcement tests; NOT in _LLM_PHASES); every
new edge/vertex gets a MATCH-returns-rows test; trailing-partial-period representable;
unmeasurable coverage abstains/NULL-loud, never fabricated. MUST NOT: finance names in
engine code; anchor re-derivation; touching conventions/validations surfaces; collapsing the
three grain concepts; eval-repo writes (HOLD — oracle section spec drafted in lane report
only). Alternate-anchor reconciles_with: attempt ONLY if substrate supports cleanly, else
park with structural note (ask-don't-guess). Branch feat/dat-725-730-temporal-calendar,
worktree lane-dat-730.

### DAT-789 · conventions→DB · Opus·high · lane-run reviewers (senior opus + spec opus)
Goal: conventions get the typed home (Convention sibling to Concept: vertical/name/
statement-prose/source seed|frame|teach/superseded_at; active partial-unique; seed via
insert_if_absent; _VERTICAL_SCOPED view) and ALL THREE consumers swap to it — extraction
(concept_store YAML-fallback :197-208), validation_phase (raw YAML load :199-214, the
asymmetric one — a frame-authored convention must reach validation), cockpit
prompts/conventions.ts (fs-read → Drizzle read). Authoring: ProposedConvention Zod +
writeConvention direct-DB (concept-write.ts pattern) + conventionsWrite (write-surface.ts)
— NOT a config_overlay family. Include ProposedConcept.ordering passthrough (P5 handoff,
one field). Statement STAYS prose; lint-at-author-time-only preserved (model_construct
discipline); YAML remains the seed (runtime read retires, file stays). Two-layer source
CHECK. Cross-package: both gate sets + mirror regen via db:pull:metadata.
MUST NOT: WorkspaceCalendar authoring (parked); conventions content linting; overlay
families; eval writes (HOLD).

### DAT-793 · grain to answer agent · Sonnet·medium · lane-run reviewers (opus × 2)
Goal: serve table grain + near-uniqueness at COMPOSE time. buildGrainBlock in
query-context.ts (buildDriversBlock pattern :961-974), reusing loadNearUniqueColumns
(grain-note.ts:89-108)/cardinalityRatio; joins Promise.all at query.ts:585-603 +
stableContext; post-hoc grain-note stays as backstop; update
query-subagent.contract.test.ts; align the empty-artifact convention (no content-free
block). Cockpit-only; cockpit gates. MUST NOT: engine changes; removing the backstop.

### W0-B · DAT-859 driver fail-closed · Sonnet · reviewers: senior (opus) + spec-compliance
(opus)
Goal: NULL/undetermined temporal_behavior never silently ranks as flow — typed loud
abstention honored at every read site. Design (settled): status Literal[measured|abstained]
+ abstain_reason (drivers-local enum + CHECK, DAT-781 two-layer; hierarchies precedent) on
DriverRanking + DriverRankingArtifact; resolve_target_type NULL/unmapped → abstained
(missing_inputs); empty-construction sites :409,:793,:890 become explicit abstentions;
read sites ONE convention — engine graphs/context.py:634-653 + _append_drivers skip
abstained (no empty headings), cockpit look-drivers.ts surfaces status honestly,
query-context.ts formatDrivers filter extends, drill-axes.ts aligned; Drizzle re-dump.
MUST NOT: touch entropy/models.py or temporal_behavior detection; no backfill; no measured-
ranking behavior change. Flip test_processor.py:126. Cross-package → both gate sets.
Branch feat/dat-725-859-driver-fail-closed, worktree lane-dat-859.

## Eval history
- 2026-07-22 · baseline (from DAT-853 close, PR #537) · clean FULL PASS / clean-flat 173P /
  detection-v1 171P+1F (AP recall — carried into DAT-856) · bands 9465db0.
- 2026-07-23 · sweeps 0+1 (lead-run, eval side, clean + clean-flat) · 3 findings filed:
  **DAT-865** validation_induction generated a balance check with a one-sided natural-balance
  sign flip (liability 2120: deviation = |ending|+|journal cum|) → fails on CLEAN data;
  fallout: cross_table_consistency=1.0 attributed to all 15 touched columns → readiness
  blocked. EXPLAIN/membership structurally can't catch this class (binds+runs, semantically
  wrong) — the W0-A "verdict flip = LLM account-classification judgment" note materialized in
  generated SQL. **DAT-866** og_temporal_coverage resolves declared time columns against
  current_columns ONLY (INNER join) — an enriched-layer anchor (journal_lines
  entry_id__date, FK-joined header date) silently loses its coverage edge while
  og_columns still carries the anchor axis: graph self-contradiction. **DAT-867**
  og_dim_members/og_filtered_by EMPTY on the folded corpus (37 rows referenced vs 0 flat)
  — the referenced-only gate (bm.attachment='referenced' + dimension_table_id IS NOT NULL)
  zeroes the whole dim-member surface on OBT; owner ruling owed.
- 2026-07-23 · detection-roleplay-v1 (lead-run) · **O6/DAT-788 VERIFIED first draw**: 12
  tables handled; bill/ship FKs kept distinct referenced axes (role separation hard pass);
  no cross-role conformance; judge conformed delivery_addr↔ship_to_addr outright; the 3
  genuine_clean registry legs correctly stood recall down on an unseen corpus.
- STOP-THE-LINE 2026-07-23: sweep queue paused; fix plan proposed to lead (866 structural
  fix, 865 evidence-serving fix + attribution-blast-radius decision, 867 folded-axis
  extension ruling). No further sweeps until fixed + re-pinned.
- 2026-07-23 (later) · eval re-run of the sweep-0/1 failed legs on pin `074f1dad`
  (lead-run, eval side) · **DAT-866 VERIFIED FIXED** (temporal-graph oracle 5/5;
  journal_lines' enriched-layer anchor has its coverage edge) · **DAT-867 VERIFIED FIXED**
  (folded dim-member shapes hard-pass on clean-flat, counts off 0) · **DAT-865 REOPENED**
  as DAT-870 (5 wrong generated checks, 3 wrongness classes, both clean corpora; blast:
  journal_lines trio blocked + 20 column-intents investigate) + DAT-871 (correlated
  generated witnesses 0.941 re-block on clean) + DAT-872 (seed orphan mis-bind 0.733315
  at 0.95 trust; 7 targets banded on clean-flat). Data proven right (ground-truth
  invariants + offline golden SQL). Red oracles: test_clean_readiness_no_regression +
  test_clean_scores_within_measured_bands. STOP-THE-LINE ROUND 2 — fix wave 2 planned,
  see NEXT ACTION.
- 2026-07-23 · detection-crossunit-v1 (lead-run, pin `074f1dad`) · **O2/CAP-measured-in
  VERIFIED first live draw**: 6/6 truth pairings projected (10 edges, incl.
  level-dependent CoA-fold), declared mixed-currency column correct on the drill-gate
  surface (typed distinct_count leg matched the data-derived cross_unit truth). One red:
  DAT-873 — measured_in edge on fx_rates.rate (dimensionless ratio); caught by the
  dimensionless-coverage leg after slipping the Currency-only parity leg (complementary
  legs worked as designed). Bonus findings: DAT-856 direction flap directly measured
  (AP outgoing-CORRECT here vs undirected on detection-v1, same pin); DAT-854 recurrence
  is a MISLABEL kind (TB-shaped cycle wearing bank_reconciliation, 0/2 key tables) —
  owner ruling posted; 79-vs-518 LLM-call gap = detector-epoch accounting on injected
  corpora (~65 real calls, zero cache reads, no anomaly). Eval parked before the final
  sweep pending fix wave 2.
- 2026-07-23 · GRADED SWEEP (lead-run, FRESH stack, pin `f7930f9bf`) · **873 CLOSED**
  (no fx ratio edge, O2 green) · **870 measurably worked**: 5→2 wrong checks, induction
  17/18 correct on clean, grain/boundary classes closed → residuals DAT-874
  (flow-vs-stock; column temporal_behavior unserved to induction) + DAT-875 (one-sided
  sign flip; clause declared+served on a fresh seed; deviation exactly 2×|stored|) ·
  **871 holds**: zero blocked on clean, journal_lines regression gone, lone failures
  0.500/investigate (cap-vs-sum path live-unexercised, test-pinned only) · **872
  recurred byte-identical** (0.733315) → localized: many-to-one path, 1:1-scoped veto
  never fired (see Decisions) · 856 WRONG_DIRECTION observed live (4 correct/1
  undetermined/1 wrong over 6 draws) · 854 MISLABELED split live + correct on a
  contrast draw · bind-parity operational: 7/9 seed binds drifted vs old pin
  (expected), new baseline recorded; sign_conventions gate + YAML deletion decided at
  the next same-pin pass.

## Decisions
- 2026-07-23 · DAT-872 fix layer · ruled by lead: FIX IN-EPIC at the orientation layer
  (not park to DAT-869/671). Fork: the eval's dump find (judge reasons "real FK", emits
  swapped fields) reframed 872 from prompt to engine — owner grounding + corpus CSVs
  proved the true cause: oriented_row picks FK direction by UNIQUENESS, but the clean FK
  (invoice→gl 100% resolve) sits on the non-unique parent axis (gl→invoice 26.7%), so
  the mechanical m:1 flip stores it backwards → 0.733 orphan alarm; both opposite
  emissions canonicalize onto the same wrong row and _first_wins keeps one by list order
  (zero test coverage). Criterion (pre-registered): orient by strictly-higher measured
  child→parent resolution, tie→judge's direction; unwind wave-3 v3.2.0's uniqueness-
  forced direction + shared-axis decline (it would suppress this clean FK). OWNER MISS
  recorded: wave-2/3 treated 872 as a prompt problem across two waves; the deterministic
  orientation step was always the cause — a prompt fix provably can't hold because
  oriented_row re-corrupts a correct emission. Reverses the prior "prompt-only, don't
  touch relationships/" fences — the 1:1 no-swap doctrine is EXTENDED (resolution is the
  signal it said was missing), not reversed. Evidence: db_models.py:226-349 flip block +
  processor.py:441-464 _first_wins; corpus containment 100% vs 26.7% (verification only).
- 2026-07-23 · band-3 YAML deletion scope · ruled by lead: FULL DELETE engine+cockpit
  (not engine-only-keep-onboarding-few-shot). Fork: finance is the only vertical shipping
  a validations/ dir; cockpit frame.ts uses it as onboarding few-shot. Criterion: band 6
  forbids finance-vocabulary leakage into a new vertical's onboarding — the few-shot IS
  that vocabulary, so it goes on both sides; clean-cut default agrees. Engine degrades to
  empty cleanly (no loader change); real work = synthetic-seed test rewrites + docstring
  sync + frame.ts onboarding cut + ontology push-route removal. Evidence: owner band-3
  grounding (removal set + consumer map both packages).
- 2026-07-23 · owner model switch · Fable weekly allowance dry → Opus owner per the
  ledger's pre-registered fallback; re-grounded with 3 Step-2 reads before resuming (did
  NOT inherit compacted context blind — the documented Opus-mid-slice failure mode).
- 2026-07-23 · CTC 1.0-on-critical aggressiveness (DAT-865 fallout) · ruled by lead: the
  categorical 1.0 is by design and STAYS; route CTC onto the witness pool with
  provenance (seed|generated) reliability — generated-alone lands investigate, seeded
  behavior unchanged. Evidence: 865b investigation (CTC is the only computational
  detector bypassing the pool; severity is LLM-self-assessed; weights can't fix it).
  Bystander columns_used role-tag = PARKED (new expressive surface, closeout triage).
- 2026-07-22 · P6 scope fork · ruled by lead (not measured): 2-axis + projection here,
  edge-valued → DAT-857/671 · rationale: no in-epic consumer needs per-dimension values.
- 2026-07-22 · DAT-856 declaration home · ruled by lead: seed→DB from the start (DAT-728/789
  pattern), after DAT-789 lands.
- 2026-07-22 · scorecard sign-off · lead added band 6 (second dataset + vertical, last lane;
  +1 run → 5+1) — the epic's "generated per vertical" claim gets its first real second instance.
- 2026-07-22 · eval-repo contention · lead offered wait-for-rewiring vs second checkout ·
  owner call: WAIT (oracles built on the new layout; band 6 depends on it); fallback second
  checkout only if the hold outlives the temporal track.
- 2026-07-22 · P5 fiscal calendar · CLOSED, lead-confirmed in chat: DECLARED workspace fact
  (typed home sibling to active_vertical), v1 = year/calendar start; unset ⇒ calendar year
  STAMPED as default; GENERIC substrate — "most datasets have year-based/start-date temporal
  nature", finance FY is one instance; authoring path rides the DAT-789 pattern later; NO
  detection revival. Also lead-confirmed: P10 retire-and-replace direction is right; the
  end-state validation home converges on frame's authoring surface (source provenance
  seed|generated|frame|teach) — full multi-source home vs generated-only is a P10 /refine
  decision.
- 2026-07-22 · P5 lane tier · Fable·xhigh → Opus·high · rationale: the owner's brief settles
  the design (calendar model, edge shapes, one-home rules); remaining risk is caught by
  deterministic gates (MATCH-returns-rows + CI + schema drift) and the owner's Fable review
  at integration; P5 has NO LLM surface (deterministic phase). Saves Fable allowance for
  P10/DAT-856/band-6 (genuinely semantic lanes).
- 2026-07-22 · DAT-787 value-source fork · LEAD-RULED **Option A** (typed
  `filter_members [{column,value}]` declared by the LLM on ConceptGroundingBasis,
  save-enforced vs served value sets) after full deliberation of the parser route
  (DuckDB json_serialize_sql — mechanically easy, repo uses it as validator twice).
  Lead's rationale: a declared member list is a model surface with a FUTURE (updatable/
  teachable/re-servable — parsing only reads a frozen artifact) and that surface is
  expected to grow; update-staleness accepted + deferred (refresh = re-run lifecycle).
  No-parse locked call stays intact, no carve-out. History: owner premise ("typed
  where[] predicates") was FALSE (where[] = raw SQL strings — lane /refine caught it);
  owner rec flipped derivation→parser→(lead)A. Bonus: engine-injected P8 scope never
  enters filter_members → scope-vs-selection distinction structural for free.
  Sweep-attention list item #4: the grounding prompt/contract grows the declared-values
  field (graded surface; live compile probe required).
- 2026-07-22 · DAT-788 refine forks · owner-ruled: (A) FULL OVERLAY — og_conformed_dimension
  meets on the judged conformed_group (the graph reflects the judged model; deterministic
  same-name floor + judge can only ADD conformance = absence-falls-loud safe); referenced
  bus-matrix cells SPLIT to (fact, dim_table, role-group) grain (tests follow design).
  Judge gets a NEW call-bounded cross-fact referenced leg (differently-named pairs only,
  no prompt changes). Identity signature content-derived (sorted role names). THIRD
  graded-surface change: _format_shared_axes pairing gains fk_role → sweep-attention list.
- 2026-07-22 · P8 refine fork (exclude_patterns as scope source) · owner-ruled: DROPPED —
  ticket premise error; exclude_patterns are column-name MATCH exclusions, no faithful row
  predicate exists, fabricating one violates typed-predicates-only. ValidityFilter sources
  from cycles completion alone. Also approved: run-scoped injection source; head-projected
  graph shape; imperative-binding-line removal at context.py:2232 (else the typed default
  never fires) — GRADED-SURFACE change → sweep-#1 attention list. DAT-733 source list →
  lead resolves at close-out.
- 2026-07-22 · P5 spec-review MEDIUM (coverage edge's is_anchor = second anchor signal) ·
  owner ruling: RENAME to `declared_anchor` (not drop, not doc-only) — mirrors the
  time_columns role structure per D1 while making anchor-one-home misuse impossible by
  name; + doc note + witness≠declared divergence test. LOW noted: fiscal-quarter boundary
  COMPUTATION is a downstream consumer (P7/window generation) — tracked here so it is not
  silently assumed done by P5's ladder substrate.
- 2026-07-22 · W0-B measured-empty rendering · **REVERSED once** (senior review beat the
  owner's first ruling): suppression deletes the "analyzed, nothing significant" grounding
  signal and is INDEPENDENT of the abstention feature. Final design: status-only gate +
  measured-empty renders an EXPLICIT "No significant driver found." line (loud honest
  absence — neither empty heading nor silence). Cockpit formatDrivers untouched; its
  secondary-only-content edge parked as a known seam. Reversal count 1 — a second reversal
  of this fork escalates to lead.
- 2026-07-22 · P5 refine forks (lane asked, owner ruled — no lead needed) ·
  (a) ordered|nominal home = `Concept.ordering` (nullable enum+CHECK; NULL⇒nominal; time
  axes ordered BY CONSTRUCTION, no stored fact — v4's "NOT config-seeded" forbids
  config-ONLY, not seed-into-typed-home; frame's ProposedConcept extension = handoff, not
  lane scope). (b) NO new phase — views-only (element views + constant fiscal-aware ladder;
  last-complete-quarter is query-time; PGQ-infeasibility would be an epic replan signal).
  (c) explicit `last_period_complete: bool|None` ADDED to temporal_column_profiles — the
  proxy fails on a partial trailing bucket with recent data (not stale, ratio-diluted);
  data-relative deterministic definition, NULL on irregular/unknown grain. (d) coverage
  shape = og_temporal_coverage EDGE (table→column, og_has_dimension pattern).
- 2026-07-22 · P10 substrate fork · CLOSED against the v4 design (DD 49152002), no lead
  escalation needed · v4's mechanism: the validation SET is generated from the WHOLE graph
  (concept edges + og_references + additivity + conventions + cycle completion — NOT just
  ConceptEdgePredicate), with `validation_induction.yaml` as the reference prompt (exists in
  dataraum-config/llm/prompts/, zero engine wiring — verified) and the check logic getting a
  typed home (tolerances move into the check definition; "today sql_hints ARE the check
  logic"). Generation is agentic induction over served graph context, not deterministic
  derivation (consistent with the agentic≠deterministic house rule). Band 3's
  verdict-identical criterion stays the measurable grade — any shortfall is a measured red,
  not an ungradeable fork. P10 brief inherits this + the nothing_declared blast-radius park.
- 2026-07-23 · DAT-875 sign-class fix home · lead-ruled FOLLOW-UP (not in-epic), design
  endorsed: typed stored-sign fact on ColumnConcept, LLM-authored + data-reconciled on
  the DAT-657 temporal_behavior blueprint, served via grain_facts; re-homed under
  DAT-671. Prose ruled out by evidence: three prompt versions failed with the
  convention declared+served. Interim investigate-band false alarm accepted.
- 2026-07-23 · DAT-874 brief premise · lane corrected the owner's grounding: the
  flow/stock verdict IS served to induction (og_columns Materialization projection of
  temporal_behavior) — proven by the failing run's persisted prompt dump; the model
  contradicted a served fact, so the fix is the coherence RULE, not more serving.
  Owner lesson recorded: a negative serving claim needs the projection chain checked
  (grep for the symbol found nothing; the fact rides under another name).
- 2026-07-23 · 872 re-cut after the graded sweep · v3.1.0 did not fire: the pair measures
  MANY-TO-ONE (gl.entry_id non-unique 25172/11754; invoice_data near-unique 3000/2860
  parent), and the wave-2 brief scoped the veto to 1:1 from the owner's unverified
  distinct-count assumption — owner miss, recorded. Fresh-stack verification: judge
  re-declared gl→invoice foreign_key (m:1, 0.85); bind faithful again; 875's check
  declared+received sign_natural_balance and still flipped (prose ceiling reached,
  third version). Fix doctrine: the m:1 fast path settles DIRECTION among true
  references, never the CLAIM — containment veto extends to every reference claim.
- 2026-07-23 · 872 fix shape · pre-registered fork (declared vs undeclared) RESOLVED to a
  THIRD branch by owner measurement: the leg WAS declared and BACKWARDS —
  invoice_data.entry_id ⊆ gl 100.0% vs reverse 24.3% (clean-flat CSVs); the judge emitted
  gl.entry_id→invoice_data foreign_key (0.75, confirmation_source=judge) despite served
  directional evidence + the orientation rule. The eval ticket's bind-trust framing is
  corrected: the bind was FAITHFUL to a wrong catalog row — leg-membership enforcement
  would NOT have helped. Fix = judge-orientation hardening (872 lane, Opus·high);
  interim blast contained by 871's cap (seed 0.696 → investigate, not blocked);
  attribution routing (referential-check failure as evidence against the declaration)
  parked for lead triage. Also recorded: lead GO for fix wave 2 ("go, eval will pause"),
  871 max-rule proceeding as owner-ruled.
- 2026-07-23 · Where do the DAT-725 leftovers live · fork: fold into DAT-671 vs a new epic ·
  lead CONFIRMED the new epic (his "DAT-761" was a typo for DAT-671; the counter-argument —
  DAT-671 keeps its additivity charter, graph-simplification deserves its own spec
  conversation — stood) → **DAT-869** created with first-wave inventory + survey candidates
  in the description; awaits its own spec pass before any lane opens.

## Checkpoint log
- 2026-07-22 · kickoff: spec pass done in-session; scorecard draft presented for sign-off.
