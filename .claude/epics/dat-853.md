# DAT-853 wild-corpus stabilisation — owner ledger

Objective: All twelve wild-corpus findings closed at their four failure-mode roots, verified
on the corpora that evidenced them, with the measuring rig fixed FIRST — calibration green on
the fixed gauges, never on the old ones.

Scorecard (source: DAT-853 epic description, locked by lead 2026-07-21):
1. Every open finding's repro closed on its evidencing corpus (wave 2 → rel-f1, wave 3 →
   clean-flat), per-ticket acceptance.
2. DAT-851 proven, not asserted: degeneracy lint red-teamed — a planted constant-output
   detector is caught.
3. DAT-849: oracles grade (conflict, ignorance) bands for every surface the product bands on;
   a 0.0-score band-driver can no longer pass ungraded.
4. Abstention end-to-end: "not measured" renders as abstention at every consumer
   (845/843/844/846 are applications); an undeclared vertical cannot silently promote a
   green head.
5. Full calibration green on `clean`, 3 seeds, on the fixed gauges — only AFTER waves 1–3;
   coordinate with DAT-736 (owns the post-DAT-725 rebaseline).

Non-goals: DAT-714 (DAT-671 package) · DAT-725 phases (start after waves 1–3) · DAT-581
(postponed) · cockpit/drill work · tuning anything to the finance corpus.

e2e budget: none pre-granted — deterministic tiers per integration; full real-LLM
calibration only at wave boundaries, EACH run on explicit lead go · spent: 0

## NEXT ACTION
3 engine-side grounding agents still out (detector rig / catalog / drivers+om). Eval-side
report landed (substrate map below). When the rest land: finish map, cut wave-1 lanes.

## State
Lanes: none open. Scorecard: all bands red (nothing beyond 834/835 fixed).
Substrate: epic branch from main@890e88a3d (includes DAT-835 PR #535 + DAT-834 PR #529).
Eval repo path is ABSOLUTE from this worktree: /Users/philipp/Code/dataraum/dataraum-eval
(the skill's `../dataraum-eval` resolves wrong from .claude/worktrees/).

## Open forks
none yet

## Parked
- spikes/dat-544/driver_spike.py tracked in repo — delete with next driver-touching PR
  (also noted on DAT-543).

---

## Substrate map
(pending grounding fan-out; carried over from the planning session:)
- Pipeline: add_source (10 phases, fan-out per table) / begin_session (14, sequential) /
  operating_model (6) — worker/workflows.py orders them; phases self-register via
  @analysis_phase (pipeline/registry.py). No pipeline.yaml.
- Eval: /Users/philipp/Code/dataraum/dataraum-eval — runner `python -m calibration.run`
  (make calibrate), pytest only reads persisted results (`--strategy`, sidecar
  calibration_run.json); strategies/*.yaml incl. clean, clean-flat, detection-* family;
  wild corpora gitignored under corpora/relbench (rel-f1 …), staged via
  scripts/stage_wild_corpus.py. Real-LLM runs.
- DAT-853 assets: calibration/phase_contract.py + scripts/stage_wild_corpus.py COMMITTED;
  scripts/probes/add-source-audit/ + scripts/probes/dat826-rebaseline/ UNTRACKED in
  dataraum-eval → wave-1 hygiene: commit or delete.
- Fixed already: DAT-834 (cycle-space description, PR #529), DAT-835 (core/duckdb_types
  shared predicate, PR #535), eval strategy-scoping (faba11e, 39db4b7 in dataraum-eval).

### Eval-side substrate (grounded 2026-07-21, sonnet reader; paths in dataraum-eval)
- **DAT-851 rig half — exact defect:** scripts/build_clean_bands.py:75
  `if max(values) <= _FLOOR: continue` (_FLOOR=0.1 line 34) runs BEFORE the constancy
  check (83-90), so a detector constant at exactly 0.100 never trips the degeneracy lint.
  Lint is a hard build-break (SystemExit 136-139) with _DISCRETE_BY_DESIGN exemptions
  (44-58). Tier-1 tests: calibration/unit/test_clean_bands_lint.py. Fix: constancy check
  over ALL emitted keys independent of floor. Repro tool:
  scripts/probes/dat826-rebaseline/emission_check.py join_path_determinism.
- **DAT-849 — mechanism + existing pattern:** engine persists BOTH surfaces:
  EntropyObjectRecord.score (conflict-only; entropy/loss.py:114-121 reads ignorance from
  evidence) and EntropyReadinessRecord.band/intents (entropy/db_models.py:118-174,
  bands ready|investigate|blocked, loss.py:74-83). Score-only oracles:
  test_detector_recall.py (DETECTION_THRESHOLD=0.3, :439-471), test_detector_precision.py
  (clean_bands score-only), test_adjudication_recall.py. Band-grading pattern ALREADY
  EXISTS: conftest.py:359-488 intent_readiness fixtures + test_intent_readiness.py vs
  hand-authored intent_readiness.yaml floors. Fix shape: extend band oracles across the
  detector surface + expose (conflict, ignorance) as first-class fixture output.
  clean_bands.yaml = score-only [min,max,seen] per (table.column, detector), seeds 46-48.
- **Corpora/repro:** clean-flat = Tier-A strategy (strategies/clean-flat.yaml,
  scenario month-end-close-flat, no injections, full truth). rel-f1 = Tier-B, NO strategy
  yaml — staged by scripts/stage_wild_corpus.py (declared-structure truth only:
  relationships + semantic_roles.timestamp, tier:wild); vertical stand-in via
  scripts/frame_wild_vertical.py (hand-authored MOTORSPORT concepts, ungraded).
  is_wild() gate (calibration/metadata_truth.py:674-689) makes absent-truth oracles stand
  down on Tier B. rel-f1 IS graded today: FK recall/precision 13/13
  (test_relationships_e2e) + phase_contract.py (standalone CLI
  `python -m calibration.phase_contract rel-f1`, 7 add_source phases, engine-independent
  expectations; NOT wired into pytest).
- **3-seed mechanics:** scripts/sweep_clean_seeds.py (--seeds 46 47 48; throwaway
  strategy+workspace per seed; --reuse to fold in a completed run) →
  output/seed_sweep/seed_*.yaml → build_clean_bands.py aggregates + lints + writes
  clean_bands.yaml. Interim rebaseline ALREADY LANDED post-835 (DAT-826, commit 5c10965,
  2026-07-21).
- **DAT-736:** In Progress in Jira; no eval-repo branch; its real progress = commits
  9114ee7/8b36032/39db4b7/5c10965. Stays open until after DAT-725; final gate = P4-P8
  metadata_truth consolidation + v0.3.4 bands. Coordination honored: full rebaseline only
  after waves 1-3.
- **Probe dirs (wave-1 disposition):** untracked add-source-audit/ (6-file census
  toolkit) + dat826-rebaseline/ (diff_bands, emission_check) — epic earmarks
  commit-or-delete; dat850-edge-kind/ (validation_sql.py one-off) → wave-3 input, then
  delete per probe policy.

## Plan
Wave 1 — instruments: DAT-851 (join_path_determinism constant + lint blind spot) ·
  DAT-849 (band-grading oracles) · probe-dir hygiene. Gate: red-teamed lint + bands graded.
Wave 2 — abstention contract: primitive once, then DAT-845 / 843 / 844+808 / 846.
  Verify on rel-f1.
Wave 3 — catalog invariants: DAT-850 edge kind (one owner, write-site enforcement) pulls
  852/847/839; candidate-lifecycle bugs 823/824; judge over-confirm 723. Verify on clean-flat.
Wave 4 — hygiene: DAT-848 (concepts run-scoping) · DAT-836 (_adhoc/ontology.yaml).
Close: 3-seed clean calibration on the fixed gauges (lead go required per run).

## Lane briefs
(pending substrate map)

## Eval history
(none)

## Decisions
(none)

## Checkpoint log
2026-07-21 — slice opened. Spec from the planning conversation with the lead (phases plan +
three confirmations); five epics re-grounded in Jira; DAT-855 created as Phase-4 home.
