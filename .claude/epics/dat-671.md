# DAT-671 Phase 3 Lane B — owner ledger

Objective: Close stabilisation Phase 3 (RFC 3 B1) — drill UX finished, slice catalog scored,
additivity next cut, cross-fact drill-across live, config→DB complete, correctness bugs fixed —
eval-green on `epic/dat-671-phase3`. Spec agreed with lead 2026-07-28 (in-session).

Scorecard:
- Browser-verified (real browser, screenshot-judged where new UI): drill from answer surface
  AND report detail · drill steps survive reload · drilled result mints child report with
  `parentId` · cross-fact drill-across numbers verified correct on the conformed-dimension
  fixture · bus matrix consumed by a real UI surface.
- RFC 3 B1 engine exit: engine's own SQL computes a predicate-carrying per-entity unit metric,
  drilled one level down, additivity-gated; `sql_snippets` substrate in place for DAT-687
  (graded from eval repo — not this slice's gate).
- Deterministic gates green at every integration (full CI mirror both packages).
Non-goals: Lane A (DAT-884/885/687, DAT-680 incl. DAT-724/861 — eval repo) · Phase 4 / DAT-855
strands (549/573/738/837) · DAT-864 (parked, consumer-gated) · DAT-674 (stretch, not close-gating)
· no run-model surgery · no second eval harness · NO finance-seed retirement this slice (lead,
2026-07-28: finance stays the shipped vertical/induction seed AND the eval substrate until
Phase 4 strand 2; no B3-alpha pull-forward).
e2e budget: none standing — ask lead per real-LLM run, at wave boundaries only (lead-locked
cadence, re-approved 2026-07-28). Deterministic tiers per integration.
spent: 1 (wave-boundary batch, lead-approved 2026-07-28: full pipeline smoke on epic branch
— add_source+begin_session+OM on the 8-table finance corpus; the answer-drill smoke's first
chat turn doubles as the run_steps live compile probe).
EVAL-REPO LOCK (lead, 2026-07-28): Lane A is running concurrently IN dataraum-eval — do not
touch that repo AT ALL (no calibrate, no Tier-1/2 there, no submodule checkout flip) without
checking in with the lead first; he updates on Lane A state at that point. Per-integration
deterministic gates are in-repo only (engine + cockpit CI mirror).
EVAL BLOCKERS (lead, 2026-07-28): DAT-886 (sign on metric-extract path → extends W1-d, both
consumers of stored_sign) · DAT-887 (period axis unbound → new W3-c) · DAT-874 (stock↔flow in
induction, Jira-Done but flagged live → new W1-e, verify-first, after W1-d). All found by
DAT-687's first read on CLEAN data @ engine bce2256a — they are the eval-green critical path.
Also from that read: DAT-709's historical repro did NOT manifest (current_ratio=13.8058) —
W1-b's guard stays as class guard; the 2/3 baseline fails are DAT-874's signature.

## NEXT ACTION
**ALL SIX LANES MERGED.** W1-g (6f1f0a751) · W1-h (193c0ead8) · W2-d2 (8b3d7a7bd) · W2-b
(7763ea018, union-resolved grid conflict) · W2-e (bae99b9e5 + CI wired + owner
reconciliation) · W3-c (d482fd345, after FAIL→rework→PASS + final micro-round: end-stamp
coarse-grain guard with both repros loud, served-stock-unserved-relation abstains,
grounding.cached unified away). Merged gates GREEN: engine **3247P/8s** ruff/format/mypy
clean · cockpit check/tsc/test 2122/build 0 · test:integration 20/21 (1 env-stale, heals at
rebuild). Mirrors regenerated BOTH rounds (91b8d54c1 induction tables; a4b088b9c
resolved_period columns). Owner Fable review of v10.3: one REAL defect found+fixed — the
prompt claimed a model pin "would be ANDed" but the composer DEFERS (never ANDs); reworded
to the true mechanism (a4b088b9c). Haiku guidance prompt approved as-is earlier.
**W2-f MERGED (58b63810d) — WAVE COMPLETE: all SEVEN lanes integrated.** Round closed every
review item (idle-timeout exemption on compose; cancellable probe via signal→interrupt;
5 smuggled-clause refusals incl. the FILTER(WHERE) non-catch pinned; axes-door reduction
with the wrongly-pinned reason flipped positive; source_note last hop + cost-guard both
mutation-caught now). Merged gates ALL GREEN: check/tsc/test **2134**/build 0 ·
**test:integration 22/22 files, 137/137 tests, 0 skipped-as-excuse** — the dev cockpit_db
was migrated to branch head (db:migrate:cockpit — the rebuild would have done it; done
early), which opened every stack-gated suite incl. the SeaweedFS round-trip. Engine 3247P.
Noted: compose's abort/timeout wiring has no unit coverage (consistent with run-sql) —
verify on a slow drill in the browser pass.
**BROWSER PASS GREEN (2026-07-28, container rebuilt + marker-verified: foldsNothing +
source_note in the running bundle). All checklist items judged, 5 screenshots
(close-01…05):**
- Parts-at-source LIVE on the CASE-guarded scalar: the exact question that rendered
  "Slice [disabled] — no axes" this morning now offers an ENABLED Slice + "time grain
  withheld"; proven-source drill 1→5 rows, every value ground-truth-exact.
- Guidance chips on BOTH surfaces (answer + DPO canvas node): Driver · 0.104/0.101 (3-sig
  — 2dp would have merged them), Unjudged on catalogued-unjudged, NO badge on bare
  substrate. Descent correctly ABSENT (corpus has no confirmed drilldown hierarchies);
  Suggest correctly absent on measured nodes.
- Haiku Suggest VISIBLE on the pure-substrate tier-A report — the strict gate is reachable
  on its designed surface (the W2-b fork's pre-registered criterion: strict gate STANDS).
- W2-d2 band 1 GREEN: slice commits → ?drill= in URL → fresh navigation restores chip +
  drilled columns + result. Band 2 GREEN: child mint from drilled report → own URL +
  "Evolved from → parent" lineage. Criticals verified: pinned+charted mint renders its
  Vega thumbnail in the gallery AND opens on detail with $1 bound (no error, no dead page).
- Lead's no-op finding CLOSED live: self-grain slice → amber "already at this grain —
  nothing to fold: … counts are all 1 and the totals only relabel the rows already on
  screen". FI-4 composed correctly: a saved link encoding the refused slice rehydrates to
  base + the layered honest notice.
- Fold-probe latency sub-second at this corpus scale (real-data judgment stands deferred).
- Wart parked: child-mint title stacks "(drilled) (drilled)" — title composition polish.
CHECKPOINT #3 DELIVERED (chat + DAT-671 mirror). Lead ruled on his three findings + amended
mid-flight (→ Decisions): grey-out not exclude; one-hop json_serialize_sql read sanctioned.
**W2-g lane-COMPLETE (2a22267fa)** — existingIdentifierColumns one-hop predicate (single
SELECT_NODE else null-fail-open; GROUP BY ALL → bare COLUMN_REFs; ordinals via select_list;
NO-GROUP-BY → empty set so raw-detail first drills stay enabled), greyed items with reason
+ tooltip on BOTH paths (parts path gained the previously-missing currentSql signal
end-to-end), deCompoundedColumnRenames POST-probe (probe byte-unchanged), idempotent title.
Gates all 0 incl. IT 22f/150t with workspace-isolation 7/7 REAL. BOTH VERDICTS
PASS-w/FI ("best-tested new read in this file family", 7 mutation kills incl. both wiring
tests). Consolidated round SENT: [1-CRITICAL] can-never-fail disabled-click assertion
(mutation-proven) → flush-then-assert; [2] rename-wrap blind spot → one-hop see-through of
OUR OWN projection-only star wrapper + chain test + docstring truth; [3] owner ruling
_count→`groups` (info kept, lead's "never render _count" satisfied); [4] re-DESCRIBE the
cleaned SQL (DuckDB RENAME collisions are SILENT — probed); [5] currentSql→baseSql trap
removal; [6] parts-wire near-dead reality (scalar-subquery proofs ⇒ state.sql single-row)
→ honest seam comment + fixture reconciliation; [7] disabled-axes filtered from Haiku
gate+payload + style nits. Senior's "CI doesn't run integration" claim VERIFIED FALSE
(ci.yml:149-150 in the lane's own tree) — dismissed.
**W3-a lane-COMPLETE (1d0f0d1c7, 32 files)** — spike verdict: per-bucket ratio recompute
ALREADY EXISTED in parts.ts; the whole defect was gateAxes stripping temporal per-verdict-
global. AxisVerdict = TOTAL PROJECTION of the existing classifier (no second judge);
engine serves per-(target×axis) facts incl. carriers, cockpit composes (bucketable iff
every carrier additive); metric_axis_additivity replaces metric_additivity (clean cut,
'*' class row, non-NULL sentinel for ON CONFLICT); universe = DECLARED metrics, abstentions
written where nothing existed; stock/flow abstains on witness↔prior contradiction (unlike
og_columns' silent COALESCE — recorded divergence). Two self-found bugs fixed (savepoint
rollback escaping to caller's txn — integration-test-caught; FILTER predicate columns
over-collected as aggregated). Judges deleted rg-clean. Engine 3267P, cockpit 2153P + IT
106P (37 env-skips, no .env in worktree). SPEC VERDICT: PASS-w/FI — AC traced clause-by-
clause (gross-margin recompute end-to-end on real DuckDB; demotion + preset floor pinned);
clean cuts rg-verified 0 hits; DAT-715 residue probed structurally live; dump round-trips
byte-identical; mirror generator-shaped. F1 (fix before merge, 1 line): `reconciles`
assigned INSIDE the temporal-axes-present branch → a ratio's CATEGORICAL breakdown on a
no-temporal-axis node prints a total its parts don't sum to (the AC's third clause failing
silently; numbers right, total lies). F2: "non-aggregate window contributes nothing" is
FALSE (duckdb_functions reports lag/lead/row_number as aggregates — probed); state the
decision, fix the rationale. F3: case-sensitivity untested (toLowerCase load-bearing).
F4: temporalWithheldReason has zero readers. F5: masked total renders a bare — with no
why (house standard: dead-end-with-no-reason reads as a bug). F6: the PINNED case is a
metric but the verdict tests all use standardField — target-aware mock owed. SENIOR:
PASS-w/FI ("design does not need re-refining"; projection totality/carrier fail-safe/
savepoint/FILTER/universe/sentinel/run-versioning all independently verified). Gating:
(1) STALE BASE — W2-g merged under the lane; senior ran the trial merge itself: big files
auto-merge correctly, 2 textual conflicts (drill.ts field union + test), 1 latent tsc
break (required disabledReason missing from new test literals); (2) reconciles gap BOTH
directions — no-temporal-axis node prints a lying total AND answer-path target=null would
dash EVERY drilled total ({false,false} from reconciliation(null)) — OWNER RULING: absent
for null target, unknown makes no claim; (3) three of four pinned-AC call sites mutation-
proven DEAD in tests (demotion, footer mask, preset floor, node reconciles, signed-sum
flag — the lane's core premise undistinguished); (4) ride-along abstain-on-contradiction
zero coverage (no fixture inserts an aggregation-lineage witness) + the corpus verdict-
delta is UNMEASURED → WATCH ITEM for the eval (the one change that can WITHDRAW a working
verdict); (5) _time_axes/bucket_grain dead engine-side; (6) window rationale false
(lag/lead ARE aggregates per duckdb_functions); (7) parts.ts header teaches the retired
doctrine; (8) anonymous extract leaf → silent enable → abstain MISSING_EXTRACT; (9) nits.
Round applied (26aad0180: rebased onto W2-g head, conflicts as field unions, reconciles
hoisted with absent-means-unknown on the answer path, all four AC wiring tests incl. the
flag-both-ways premise test proving DIFFERENT SQL and values, window decision STATED with
a lead() test, witness/contradiction + cadence + anonymous-leaf coverage closed — the
lane's own new tests caught two of its own errors). **MERGED (fd053548e). Merged gates ALL
GREEN: engine 3273P/8s ruff/format/mypy 0 · cockpit check/tsc/test 2177/build 0 ·
IT 163/163 (22/22 files) · mirror regen ZERO drift.** W3 wave: a+c DONE, b next.
Owed at next /smoke batch: withhold sentences + dashed footer + greyed items browser check.
EVAL WATCH ITEM (standing): abstain-on-contradiction can WITHDRAW corpus verdicts —
Lane A's next cycle measures the delta. Owner Fable review of
the evidence surfaces DONE: _render_additivity vocabulary + rule line APPROVED (unknown-
is-not-additive doctrine explicit); the four withhold sentences APPROVED (mechanism-first,
the semi-additive one correctly places the limitation on the DRILL, matching the recorded
period-end-LAST follow-up). Discoveries parked below. Merge order: W2-g (round in flight)
→ W3-a (overlap: drill.ts/drillable-grid.tsx/drill-axes.ts — additive claims, resolve at
merge + W2-g's groups-rename lands first). Then: W3-b · W4 · W5 · W6 · W1-f.
(archived lane-completion detail: (1) W2-f lane-COMPLETE (all 5 items, zero grid changes — no-op
refusal is server-side via the existing refusal channel with a fails-open single-row guard
proven by two pre-existing tests; pin [4] FLIPPED, pin [5] deliberately KEPT expect-false —
it is still true, the flip-equivalent is a NEW sibling asserting the refusal names the alias
[spec-review record correction]; alias rejection parser-based after probing that a strip is
infeasible — query_location uint64 doesn't survive JS JSON; + fixture defect fixed with
engine evidence, slice_definitions belong under the typed FACT table_id; gates all 0, IT
13P/0F). SPEC VERDICT: **PASS**, 4 fold-ins none blocking. SENIOR VERDICT: PASS-w/FI, ONE
BLOCKING — compose became an EXECUTING route without disableBunIdleTimeout → Bun kills the
connection at ~10s on practitioner data = an unexplained refusal on an acceptable drill
(the lane's own failure class, one layer up); + source_note last-hop unpinned (mutation
survives 2123 green); dims-guard test asserts truth for the wrong reason; axes route hands
partsSources UNREDUCED and the new test PINS the false "stale snippet" reason as correct;
declaredValueExprRefusal misses GROUP BY/HAVING/QUALIFY/SAMPLE/modifiers siblings (probed:
each accepted then dies in the binder = the silent downgrade the gate exists to end);
probe uncancellable (wire request.signal → conn.interrupt per run-sql). Probe arithmetic
VERIFIED exact; refusal-before-commit traced; fixture fix mutation-proven load-bearing;
merge-tree reports NO conflicts vs epic. Consolidated round SENT (senior 1-6 + spec FI-1
+ n=2 docstring edge). Merge on green deltas.
Owner Fable review of its prompt diff: **APPROVED** — the carve-out names the guarded CASE
form as the NORMAL shape that never disqualifies, the new rejected-and-reported paragraph
states the TRUE mechanism and the honest cost (drill affordance lost, answer unaffected);
source_note is an OUTPUT-side field (no input-schema/constrained-decoding risk). → merge on
verdicts; (2) closing
batch: cockpit container rebuild (heals the env-stale IT failure) + screenshot-judged
browser pass — W2-d2 bands incl. pinned child report, W2-b chips/descent, Haiku gate on a
pure-substrate tier-A drill, W2-f live (parts-at-source on a guarded scalar; the no-op
slice refusing itself), AND spec-review FI-4: a drill persisted pre-W2-f that encoded a
now-refused no-op slice must rehydrate to base + the honest notice (W2-d2's persistence ×
W2-f's refusal meet here for the first time); judge the fold-probe's double-execution
latency on real data; (3) CHECKPOINT #3 to lead (then W3-a/W3-b, W4-W6, W1-f — the next
waves). PUSH pending this commit.
Remaining queue:
- **W3-c** REWORK COMPLETE (7 commits, 3218P green): strict `<` snap + grain-aware
  reachability, _latest_close made TOTAL (dead branch deleted), 8-case probe set as unit
  tests (probe-writing caught 2 more real bugs: day-31 month-advance overflow →
  monthrange-clamped; the dead branch), prompt v10.3 CONDITIONAL coupling (stock+calendar
  served ⇒ system binds; else standing MAX-pin; known-stock-unresolvable ⇒ typed abstain,
  mutation-tested), wire test mutation-verified, guarded calendar read, all fold-ins a-g
  (SnippetAssumption.dimension required-with-default; 0.2 unbound disclosure; fact-only
  shared context; UTC-normalized; dump regenerated). SENIOR RE-REVIEW: **PASS-w/FI** —
  corpus probe BINDS DECEMBER (-2,141,863.57, ≠Jan, ≠naked-MAX); acceptance bar holds on
  every enumerated branch (None-set = model pin; str-set = fall-loud abstain, SELECT NULL
  shape + sub-floor disclosure); _advance verified across the leap/month-end matrix (the
  day-31 clamp is CORRECTNESS — naive carry falsely reaches closes); all 3 mutations killed
  by named tests; schema_read.sql round-trips clean. FINAL MICRO-ROUND sent: (1) end-stamped
  labels at grain>month overshoot coverage_end (label+1day ≠ label+1grain; quarterly/yearly
  repro verified) → one-query end-stamp detection → fall loud; (2) served-stock-without-
  served-relation returns None (model pin) where the bar demands abstain; + 3 nits (dump
  relation record, og_uses comment, unknown-marker unification). On return: merge, engine
  gates, 15s mirror regen (epic-side — lane forbidden, would collide), owner Fable review of
  v10.3, push.
**W2-e MERGED (bae99b9e5) + CI wired (owner commit) + mirror REGENERATED (91b8d54c1 —
self-contained db:pull:metadata; the "needs stack rebuild" premise was wrong, cockpit
CLAUDE.md: no running stack needed; W1-h debt discharged, W3-c's regen = same 15s command
when its rework merges).** Merged-state gates: check/tsc/test(2122)/build 0;
test:integration 20/21 files — the 1 failure is ENVIRONMENTAL (legacy stack-gated
workspace-isolation suite hits the live dev cockpit_db, which predates W2-d2's migration:
"column sql_params does not exist"; CI unaffected — suite skips loudly there; heals at the
cockpit container rebuild). Owner reconciliation in the merge: harness mint test's
confidence.band → optional chain (nullable since W2-d2).
W2-f SPAWNED (harness net in place). W3-c carry-notes from its spec review
(PASS-w/FI): cockpit mirror lacks the 3 new columns (owner step); failed snippets also carry
resolved_period (disambiguated by `failed` — eval should know); period predicate not in
og_uses filter_columns (accepted DAT-733-class residue); Haiku-gate ruling: strict through
browser pass, widening = checkpoint question.
INTEGRATION WATCH: (a) drillable-grid.tsx(+test) conflict W2-d2×W2-b — confirmed via
merge-tree, resolution is a union of additive edits, full gates after. (b) W2-e's mint
coverage written pre-W2-d2-schema — its drizzle-kit-migrate fixture picks the widening up on
rebase; re-run test:integration on merged state. (c) RECORDS: column_annotation is ALREADY
structured-output (DAT-807) — runaway is grammar-legal digits in a NUMBER literal; "switch
to structured output" is MOOT (confirmed, git-show).

## State
**8 lane merges on `epic/dat-671-phase3`, tip 6f2e10c12, PUSHED.** W1 wave complete (DAT-840 ·
709 · 847 · 878/748 · 875/886-ext · 874) + W2-c (DAT-678, aa56527aa) + W2-a (DAT-879,
1f7bba5d0 + owner reconciliations 3902c3528/2ec5d13c7 — cross-lane semantic conflicts with
ZERO markers; only full gates on merged state catch them). ALL deterministic gates green:
engine 3169P/8s ruff/format/mypy clean · cockpit check/tsc/test 203f 2022t/build (gate
commands capture $? — a tail-pipe once swallowed tsc's exit). Mirror regen done on W2-a's
branch (W1-d debt discharged). Jira hygiene done (853/714/701/716-718→Done, 715→Duplicate).
**WAVE-BOUNDARY BATCH DONE (2026-07-28, lead-approved, e2e spent: 1):** pipeline smoke green
on branch containers (1 retry — column_annotation runaway, recorded bug); revenue exact to
cent scalar + 5-way; DPO 37.15 POSITIVE (DAT-886 gone live, AP +2,207,436.54); stored_sign
witness-driven (3 resolved, 50/51 'unsure'); run_steps LIVE compile probe PASS through the
production adapter; both AC drills browser-verified + screenshot-judged; blocked-mint live.
Lane A handover delivered (branch state, schema deltas, calibration-owed, watch items).
Stack still RUNNING (prompt-dumps overlay) — reuse for W2-d2 browser verification.
**Lead findings from his own pass (2026-07-28):** tier-A no-op self-grain slice → item 3 of
the answer-drill honesty lane; gross-margin time-grain incoherence → pinned W3-a AC.
**Lead approvals (2026-07-28):** cockpit integration-test harness starts NOW (lane W2-e,
results+reports surface first); W2-b + W2-d2 next, then W3; Lane A eval cycle STARTED.
**LANE A READ (relayed by lead, 2026-07-28, on epic tip 6f2e10c12):** pass-gate = DAT-889
(runaway kills ~1-in-2 passes, filed off our handover) + DAT-877 (operatingModelWorkflow
re-runs induction on the referenced corpus; graded-head window exposes incomplete first
generation → plausible-red on cycle_recall/no_fabricated_grounding; final head healthy;
NOT previously in slice — absorbed). DAT-887 decides run value (pulled forward). Noise
terms, not gates: DAT-723 (relationship judge over-confirm — NOT absorbed, lead triage)
· DAT-847 (branch's near-tie abstention IS the aimed fix — sweep after integration, not
between). Payoff reasons to run: DAT-874 (fix on branch, baselines banked) · DAT-884 ·
DAT-687 widening. DAT-886 Jira status was stale — hygiene done (evidence comment +
transitioned; Done at epic merge).
Scorecard: drill-from-answer-surface + report-detail bands GREEN (browser-verified); reload-
persistence + parentId-child-report red (W2-d2); cross-fact + bus-matrix red (W4); B1 engine
exit partial (predicate-carrying unit metric = W3-b).
Standing integration notes: owner runs both reviewers (nesting OFF) · Fable review on prompt/
evidence-surface diffs · cockpit migrations re-dump on rebase, never hand-merge · finance
corpus has no dim tables — period-FK logic graded by unit fixtures + calibration only.

## Open forks
(none open. Lane-fence note for the running trio: W2-b owns drill-axes*.ts + drillable-result-
grid.tsx chips; W2-d2 owns reports persistence/mint and does NOT edit drillable-result-grid.tsx
— if it needs a grid seam it ends its turn and asks; W2-e touches test infra + fixtures only,
product code changes limited to minimal config seams it must flag in its report.)

## Parked
- Stale merged-epic worktrees `.claude/worktrees/epic-dat-725`, `epic-dat-853` in the shared
  checkout — remove with lead ack at close (not mine to delete mid-flight).
- DAT-718 oracle half lives in dataraum-eval — Lane A's; note in closeout.
- `reporting_intent` persisted `entropy_readiness.intents` blobs keep the old string until a
  re-run — acceptable, note in rename lane brief.
- `analysis/lineage/__init__.py` stale `__all__` (re-exports nothing used, omits processor's
  imports; nothing imports the package) — W1-a discovery, trivial cleanup at close.
- Smart-integer calendar keys (dim_date.date_key=20240131) still resolve FACT — W1-a deliberate
  under-claim, fails safe; closing it = an LLM-contract change (RelationshipOutput can't say
  "target is a calendar"), NOT a name heuristic. Candidate future ticket at closeout.
- `semantic/agent.py::_load_profiles` uses profiled_at-latest, not run-scoped reads — W1-c
  discovery, pre-existing; relevant to any future run-versioning pass.
- Period-dimension identity residual (W1-a re-review): the cardinality witness is necessary
  but NOT sufficient — a small dimension with a genuinely unique event date (launch_date/
  opened_at class) still false-fires; the smart-integer calendar still under-claims. Closing
  BOTH = one LLM-contract change: the model names its period dimension, witness stays as the
  soundness gate. Future ticket at closeout; never a name heuristic.
- Generic retained-failure steer serves a NULL-aggregation tail irrelevant to
  provenance_invalid failures too — W1-b discovery, small prompt honesty cleanup.
- DAT-809 ticket cites stale file:line evidence (module reshaped since 2026-07-17) — refresh
  description at checkpoint.
- DAT-840 note: canvas node renders only the OUTPUT step's checks; an overlay-authored check on
  an extract step shows in the step list only — revisit when overlay authoring goes live.
- Numeric/range slice-AXIS build-out (DAT-280 tail): second slice_type + band edges +
  eligibility-gate change (measures through _EXCLUDED_SLICE_ROLES) + drill-axes rendering +
  teach surface — parked from W2-a fork 1 (score comparability landed; the axis TYPE is a
  feature). Lead triage at close.
- `slicing` runs before `driver_rankings` in _SESSION_VALUE_PHASE_ORDER — driver gains can
  never inform the slice score without a phase-order change (W2-a finding).
- **og_columns.materialization bypasses fail-closed abstention** (W1-e): COALESCE takes
  mal.pattern before cc.temporal_behavior — asserts flow/stock on ≥4 reachable paths where
  resolve deliberately abstained; affects answer agent + period_resolver:89 too. Same
  "absence renders as confident value" class as the DAT-853 harvest. Closeout ticket candidate.
- `ColumnConcept.temporal_behavior` has no CHECK constraint (unlike stored_sign/meaning_status)
  — out-of-vocabulary value silently becomes NULL materialization (W1-e). Hygiene at close.
- `test_property_graph.py:216-244` hand-seeds a state the writers cannot produce (behavior
  'point_in_time' beside pattern 'per_period') — COALESCE precedence only exercised on
  synthetic rows (W1-e).
- **DISCOVERED BUG (wave-boundary smoke, 2026-07-28, lead-confirmed): column_annotation
  runaway emission.** Sampled (Sonnet 5 retired temperature — `temperature: 0.0` in every
  prompt YAML is dead config), free-text-JSON phase annotating ALL tables in one call;
  1-of-2 runs emitted a runaway digit string to the SHARED thinking+output max_tokens cap
  (~70k chars vs 15.4k healthy; stop_reason=max_tokens → pydantic json_invalid →
  non-retryable PhaseFailed). Canned error hint ("raise max_tokens or reduce the batch")
  exists instead of a guard. Fix directions: automate the hint (retry with reduced batch on
  max_tokens) and/or structured output for the phase. HANDOVER item — Lane A calibration
  rolls the same dice every pass. Ticket at checkpoint.
- W1-e residuals (fail-safe, disclosed): unreadable operating-model graph renders every
  measure UNDETERMINED (conflates unmeasured with ungraphable — over-declines only); a
  de-facto measure with a missing role label gets neither verdict cell nor stated absence.
- Cockpit `bun run test:integration` has 3 PRE-EXISTING failures ("Invalid cockpit base
  configuration", src/duckdb/{connect,probe,probe-stream}) — byte-identical without W2-a's
  changes; lane gates use the unit project. Triage at close.
- Slice-relevance interval rendering (score is a lower bound under top-K truncation;
  exactness not persisted — fields deleted per built-but-unwired rule) — candidate if a
  consumer ever wants the bounds.
- `SemanticAgent.synthesize_tables` (semantic_per_table) sends the same all-tables-one-call
  shape through the same parse boundary as the DAT-889 runaway — same class, unguarded
  (W1-g discovery, fenced out of its lane). Generalize the guard or per-phase copy: lead
  triage at checkpoint.
- **`PhaseResult.warnings` dies at the activity boundary** (W1-g spec-review discovery):
  PhaseRun (worker/activity.py:132-146) and PhaseOutcome (worker/contracts.py) carry no
  warnings field — SIX phases now populate warnings into a void (correlations, temporal,
  validation, surrogate_mint, column_eligibility, semantic_per_column). Fix = contracts.py
  widening = cross-PACKAGE (cockpit hand-mirrors it), Fable-tier per model mix. Lead triage.
- DAT-889 ask #2 (delete dead `temperature: 0.0` lines across prompt YAMLs) — outside W1-g's
  fence (prompts YAML frozen there); trivial sweep, route at checkpoint before DAT-889 closes.
- W2-e residue: 9 legacy stack-gated integration suites still skip (assume seeded producer
  data; now loud, never silent) — separate uncertain expansion, lead triage. Report lineage
  (child→parent) harness coverage belongs with W2-d2's columns — W2-d2 fold-in round or
  post-merge owner test.
- **W3-c / Lane A interpretation weight: `workspace_calendar` has ZERO writers** — every
  workspace binds the stamped default-January calendar; the eval corpus is January-fiscal,
  so a green eval proves the DEFAULT path only, never the declared path (lane covers
  declared-April in tests). The cockpit calendar-authoring surface is the missing writer —
  candidate ticket at checkpoint.
- **W2-f residue (spec-review FI-3): `/api/drill/node` and `/api/drill/parts` have NO
  fold-nothing guard** — and items [1]/[2] just made parts-at-source the PRIMARY path for
  scalar answers, so the unguarded route is now the live one. The parts case needs its own
  wording (rows(base)≡1 there, the tier-A phrasing would be dishonest). Next honesty cut.
- W2-f informational: the wire door (acceptWireSources) carries the relation reduction but
  NOT the alias gate — safe by construction today (no aliased expr can be accepted at
  declaration), the same "safe only by accident" asymmetry class; close when touched.
- W2-g discoveries (spec-review durable-home fold-in): `allUnmeasured` counts DISABLED axes
  when deciding the Haiku Suggest gate (cosmetic inconsistency); reports always view via
  tier-A regardless of origin, so parts-path greying is unreachable from a report surface
  (matters only on the live answer canvas); metric-node path disabledReason permanently
  null (recorded in code). Also: ordinal/GROUP-BY-ALL determinations resolve to the model's
  ALIAS, so the catalog spelling isn't greyed on aliased projections — consistent with the
  name-match-collisions-OK ruling, noted for the eventual disambiguation pass.
- **W3-a discoveries (recorded, not fixed): semi-additive bucketing is the REMAINING
  honesty gap** — a stock measure is meaningfully bucketable at its own cadence but the
  composer can only SUM buckets → withheld with a typed reason; period-end (LAST) selection
  per bucket is the follow-up that unlocks the AC's other half. W3-c's period binding
  deliberately not consumed (semi-additive→withhold covers it — judgement call, flagged).
  og_columns.materialization still COALESCEs witness-over-prior silently while the verdict
  now abstains on contradiction — graph surface more permissive than the verdict. Cross-
  parser universe divergence (Python GraphLoader vs TS parseMetricDag) degrades visibly
  but is unpinned by a comparison test. unitGateReason still has zero client readers.
- W2-g finding-B residue (PARKED): the deCompound RENAME wrap makes a re-drilled/child-mint
  SQL unreadable to the one-hop determination (outer node reads ungrouped) — degrades to
  the post-click amber via the fold probe, never silently wrong; the minimal null-vs-empty
  honesty fix lands in the fold-in round; the deeper see-through (structural grouping
  metadata carried on the mint instead of re-derivation) is a future cut.
- W3-c residue: `last_period_complete` cannot detect window-alignment (DAT-730's "AR-NULL
  becomes knowledge" AC unmet by that flag for snapshot-stamped data — refresh DAT-730 note
  at checkpoint) · period binding freezes into the cached extract snippet (same staleness
  class as the cached sql — named, not new) · fiscal-YEAR close is the only expressible rung
  until a metric can declare a reporting window.
- W2-d2 discoveries, recorded not fixed: MintBody still `as`-cast with no runtime schema
  validation (pre-existing); chart authoring not carried into report-origin child mints
  (table-only, AC didn't require it).

---

## Substrate map (grounded 2026-07-28, two Explore passes @ main bce2256ab)
**Drill/parts:** DAT-714 FIXED on main (PR #462; COALESCE carrier spine `cockpit/src/duckdb/parts.ts:722-742`).
`parts.ts` greps as binary — use `grep -a`. Additive path = GROUP BY (`:720`). Local categorical
judge `flattenAdditive` `parts.ts:229` (consumed `:299,:583`) — DAT-868 must delete it.
**Additivity:** engine classifier `graphs/additivity.py` (457L) + resolver `graphs/additivity_resolver.py`
+ persistence `pipeline/phases/metrics_phase.py:456` (`metric_additivity`, unique (target_kind,target_key,run_id))
+ `current_metric_additivity` (`storage/read_views.py:96`) + PGQ og_additivity (`storage/property_graph.py:194,986-1025`).
Cockpit time gate reads boolean `time_additive` (`tools/drill-axes.ts:684-711`, helper `:307-329`);
categorical axis deliberately unread (`:244-250`); `categoricalAdditive` mirrored in Drizzle
(`db/metadata/schema.ts:425`), zero consumers. SQL-AST gate fails closed on window/FILTER
(`duckdb/sql-ast.ts:84-90`) — the DAT-715 residue, now DAT-868's.
**DAT-857/868: zero code on main.** Only typed-abstention precedent: driver-ranking status/abstain_reason
(`analysis/drivers/models.py:201`).
**DAT-847 (live, both):** discriminator bare `<` no margin `analysis/lineage/reconcile.py:106`
(guardrails `:100-103` abstain on bad fit, near-tie still picks); `derive_table_role`
(`analysis/semantic/db_models.py:137-153`) needs grain∩TimeColumn, call site passes event-role
date cols only (`semantic/agent.py:259-267`) → period-FK snapshot ⇒ FACT ⇒ over-permissive
`snapshot_count` verdict via `additivity_resolver.py:222`. Merged 847 commits fixed a different
surface (`entropy/resolve.py:72-140` None-through).
**DAT-709 (live):** grounding = 1 LLM call/concept (`dataraum-config/llm/prompts/graph_sql_generation.yaml`);
no cross-concept pass; snippet unique key INCLUDES standard_field (`engine/schema.sql:245`) so
identical statements dedup-invisible; `normalized_expression` matched only for formula type
(`query/snippet_library.py:303-313`); within-output uniqueness only (`graphs/models.py:451-470`);
finance ontology declares NO disjoint block — disjointness only via convention concept_groups
(`analysis/semantic/concept_edge_store.py:77-81`).
**DAT-748 (live):** only VARCHAR sampled (`analysis/typing/inference.py:79-84`); raw_type from one
DESCRIBE of DuckDB CTAS (`sources/backends.py:208-211`); strongly-typed heuristic
`pipeline/phases/typing_phase.py:84-88`, verbatim resolve `:189`.
**DAT-878 (live):** column load unfiltered `analysis/catalogue/context.py:758-764`; `is_surrogate_column`/
`SURROGATE_PREFIX` (`analysis/relationships/surrogate.py:32`) never used by prompt builders.
**DAT-739 (premise part-outdated):** producer EXISTS — `analysis/semantic/reconciles_with.py::derive_reconciles_with`
wired `metrics_phase.py:376-384` (SAVEPOINT); tolerance always NULL (`reconciles_with.py:33-36`);
consumers = renderers only (`graphs/context.py:1493,1514,1963-1968`, `validation/induction.py:394`,
`property_graph.py:518-523`). Missing: executor computing the two sides.
**DAT-673 remainder:** driver ordering shipped invisible (`tools/drill-axes.ts:193-224,638-642`);
`DrillAxis` (`duckdb/drill.ts:39-54`) has no gain field; no drill-UI hierarchy consumer
(`current_dimension_hierarchies` only in `tools/query-context.ts:319-439`, `teach-routing.ts:26-29`);
no Haiku on drill path (Haiku = NAV_MODEL/SUMMARY_MODEL `llm.ts:27,33`).
**Config→DB:** DAT-880 `_normalize_legacy_check_fields` = model_validator on every ValidationSpec
(`analysis/validation/models.py:116`, docstring names DAT-447 overlay `core/overlay.py:222-233` as
last producer; `check_type` bare str `models.py:84-90`; entropy reader branch `entropy/detectors/loaders.py:748`).
DAT-881 no typed cycle_types table (families yes `schema.sql:70`); readers `cockpit/src/tools/teach-cycle.ts:65-102`
(errors→[] swallowed) + `frame.ts:66,420` seed; engine `analysis/cycles/config.py:70-82`.
DAT-882 `teach-metric.ts:70-95` rglobs shipped YAML; DAG body via `metric-spec.ts:307-324`; 3 call
sites (teach-metric:109, frame.ts:67/467, routes/api/shipped-metric-dag.ts:19/50).
DAT-883 envelope re-parsed per call (`analysis/semantic/concept_store.py:216`, no-cache by design
`ontology.py:171-175`, linters re-run each call); fabricated "1.0.0" one layer down
(`core/vertical_loader.py:56` empty_base → pydantic default `ontology.py:85`); hot-path callers:
semantic/agent.py:142, column_agent.py:112, catalogue/agent.py:100, graphs/context.py:871,
semantic_per_column_phase.py:192. Constraint: do NOT run while DAT-724 runs (eval-side).
**reporting_intent:** config-only — `dataraum-config/entropy/loss.yaml` (14 hits); engine iterates
generically (`entropy/loss.py:67-68,139-152`); opaque string to cockpit (`why-table.ts:52,154`,
`look-table.ts:47`); literals only in 7 test-fixture lines + prose `why-table.ts:371`, `look-table.ts:6`.
**DAT-875:** NOT yet verified — W1 lane verifies first (claim: one-sided sign normalization survives
sign_natural_balance; deviation exactly 2×|ending_balance| on clean-flat).
**W2 cockpit surfaces (grounded 2026-07-28, Sonnet reader):** Only ONE drillable mount:
`AnalyseModal` → `DrillableGrid` w/ nodeRef (`analyse-overlay.tsx:296`). Answer
(`answer-result.tsx:267`), run_sql (canvas kind result-grid, `canvas-registry.ts:45`), report
detail (`reports/$reportId.tsx:225`) all mount plain `ResultGridWidget` — gap = no
`axesRequest`/`nodeRef` (required prop `drillable-grid.tsx:271`). DAT-678 seam: answer =
`tools/query.ts` (`querySubAgent`:581, capture `:187-210` composedSql+components); canvas boundary
DROPS components (`tool-result-to-canvas.ts:279-303` projects only {sql,summary,confidence});
`saveQuerySnippet` INSERT never populates `parts` (`db/metadata/snippet-writer.ts:108-123`);
`SnippetParts`/`sql_snippets.parts` is the join point `parts.ts:85,99` expects; tier B deleted
(`duckdb/drill.ts:16`); tier A `composeTierA` (`drill.ts:98`, `/api/drill/compose`) works on any
flat SQL = the honest fallback. DAT-676: drill state 100% useState (`drillable-grid.tsx:298-303`,
`analyse-overlay.tsx:179-187`); zero search-param usage in reports routes; reload loses all.
DAT-627: `reports.parentId` exists (`db/cockpit/schema.ts:405`) + threaded through
`CreateReportInput`/`ReportRow`; `MintBody` (`routes/api/reports/mint.ts:16-24`) has NO parentId;
sole mint caller `answer-result.tsx:189-214`; zero lineage UI. DAT-840: both DAG paths drop
`validation` — `parseMetricDag`/`MetricStep` (`tools/operating-model-graph.ts:162-198`) and
`narrowSteps`/`DagStep` (`lib/metric-dag.ts:10-67`); authorable field exists
(`metric-spec.ts` GraphStepSchema.validation:111-141, enforced by engine graphs/verifier.py);
render homes: `operating-model/nodes.tsx` (subtitle:70, badge:38), `widgets/metric-dag-steps.tsx:15-44`,
equation header (`analyse-overlay.tsx:269-277`). No component named ModelFrame exists (language only).
Design tokens: `ui/theme.ts` (canonical) + `styles.css` Tailwind mirror; UI quality bar in cockpit
CLAUDE.md. Cockpit baseline GREEN on main: 197 files / 1947 tests.
**W4 substrate (grounded 2026-07-28):** `current_bus_matrix` = `schema_read.sql:9-17`, computed
`analysis/hierarchies/bus_matrix.py::derive_bus_matrix` in dimension_hierarchies phase. Cell:
`attachment` referenced|folded · `confirmation_source` unconfirmed|judge|user|keeper ·
`conformed_group` (nullable = the identity signature) · `needs_confirmation`. DAT-809 filter =
folded ∧ `conformed_group IS NOT NULL` ∧ ¬needs_confirmation; precedent
`analysis/lineage/processor.py:435-479` `_shared_dimension_groups`. Referenced cells ALREADY flow:
`og_conformed_dimension` (`schema_graph.sql:156-170`) → `_read_conformed`
(`graphs/context.py:1323-1361`) → prompt. Folded cells: zero paths. Cockpit `currentBusMatrix`
mirrored (`schema.ts:83-99`), zero consumers. DAT-809 ticket cites stale line numbers — conclusion
holds, evidence needs refresh (parked). DAT-740: multi-fact FULL-JOIN precedent = `parts.ts:708-742`
(one metric's DAG only, joins by name not conformance, no per-side rollup); needs conformance-derived
join + additivity-aware grouped merge + loud abstain. DAT-730 (common grain/P5): reader claimed
"retired, redo needed" — CORRECTED: merged via `46b7bbb3f`, Jira Done. W4 blocker in-slice = DAT-857
only (W3-a, already sequenced first). DAT-737 delta: `concepts`/`conceptEdges` mirrored zero
consumers; `og_*` views have NO Drizzle mirror; model route builds its own parallel metric-DAG graph
(`tools/operating-model-load.ts:17-35`, DAT-591) never touching concepts/og_* — "faithful lens"
doesn't exist; config seam still fs-based.
**DAT-838 seam:** grounding-time substrate complete (`ExtractGroundingOutput.where` models.py:626-631,
`compose_extract_sql`/`compose_where_predicate` formula_composer.py:35-68, `compose_scoped_where`
validity_scope.py:127-177). Declaration chain (NO predicate anywhere): finance metric YAML →
`metric-induction.ts:104-128` extractShape → `metric-spec.ts:35-100` GraphStepSchema.source →
`teach-metric.ts:116-121` config_overlay JSONB → `graphs/config.py` → `loader.py:209-247` →
`StepSource`/`GraphStep` (models.py:85-136) → agent.py call sites (~1352,1578,1655,1797,1897).
Pure JSON-shape + prompt plumbing, NO migration. HARD CONSTRAINT: predicate = field on existing
extract branch, never a new step variant (`induction-schema.contract.test.ts` pins branch count vs
constrained-decoding ceiling).
**Per-entity grain (B1 exit) — DISCOVERED GAP, absorbed:** metrics are scalar-only end-to-end;
`OutputType.SERIES/TABLE` has zero consumers; composers hard-code one-row scalar. Needs: grain/entity
declaration on `OutputDef` (models.py:139-145) + grouped composition mode in
`compose_extract_sql`/`compose_formula_sql` + induction/spec mirrors. Absorbed into W3-b.
**DAT-875 (verified LIVE 2026-07-28):** no deterministic sign code — `sign_natural_balance` is
prompt text (`finance/ontology.yaml:272-290`, DAT-870 clause `4a6b39a36`); routed via
`relevant_conventions` (induction.py:100,206,364-406; validation_phase.py:225-256). Generator stores
`ending_balance` raw debit−credit ⇒ one-sided normalization = exactly 2×|ending_balance|, arithmetic
certainty. Lead-endorsed design on ticket: typed `stored_sign` fact on ColumnConcept, DAT-657
temporal_behavior template (LLM-authored + data-reconciled + served via grain_facts). No
stored_sign/SignConvention symbol exists yet.
**Eval mechanics:** deterministic gate = `uv run pytest calibration/unit -q` (Tier 1/2, no docker/LLM;
`-m 'not llm'` equivalent; 33 unit files + frozen sqlite fixture). Real-LLM = `make calibrate`
(docker+LLM) — wave boundaries, lead go only. BASELINE (pre-existing, do not attribute to slice):
clean pass `439ed742a193` 2026-07-23 engine `aeb2421b`: 47P/**3F**(clean_scores_within_measured_bands,
pooled_llm_detectors_below_measured_ceiling, clean_readiness_no_regression)/6xf/31s; clean-flat
`add243032d75`: 1F (cycle_recall). Verdict store `calibration/results/verdicts.jsonl`; engine_commit
computed live from `vendor/dataraum-context` checkout (editable install) → to eval the branch: push
it, `git -C vendor/dataraum-context fetch && checkout epic/dat-671-phase3`. Conformed fixture =
clean-flat (account axis folded into general_ledger/trial_balance/balance_sheet;
`schema_transforms.py:373-419`; numbers correct-by-construction; folds declared in metadata_truth).

## Plan (living)
W1 bugs → W2 drill UX + catalog → W3 additivity + grammar → W4 cross-fact → W5 config→DB → W6 739.
Lanes per wave, independently mergeable into epic branch:
- W1-a DAT-847 (Opus·high) · W1-b DAT-709 (Opus·high) · W1-c DAT-878+748 (Sonnet·high)
  · W1-d DAT-875 verify→fix (Sonnet·high)
- W2-a DAT-879 + graphs/context.py 3-way split (Opus·high; HOLD until W1-c integrates — W1-c
  touches graphs/context.py:509-522; W1-b touched zero lines there, hold re-based 2026-07-28) · W2-b DAT-673 remainder (Sonnet·high) · W2-c DAT-678 (Opus·high;
  before W2-b or serialize — both touch drillable-grid.tsx) · W2-d1 DAT-840 (Sonnet·medium,
  UNBUNDLED — zero deps, spawned 2026-07-28) · W2-d2 DAT-676+627 (Sonnet·medium; after DAT-678
  — the surfaces they persist/mint from aren't drillable until 678 lands)
  · W2-e cockpit integration-test harness (NEW, lead-approved 2026-07-28 in the roughness
  retro; Opus·high — fixture-workspace harness, revive test:integration, production-shape
  fixture rule; results + reports surface first) · W2-f answer-drill honesty (now 5 items:
  [1] prompt carve-out unblocks parts-at-source · [2] proven source solves the alias gap ·
  [3] no-op self-grain refusal via rows(drilled)==rows(base) → amber · [4] /api/drill/parts
  applies bareRelationName (W2-e defect — safe today only because the client pre-reduces) ·
  [5] model-authored `AS alias` in a declared value expr enforced beyond silent tier-A
  downgrade (W2-e defect); AFTER W2-b — shared drill surface; Opus·high — prompt diff ⇒
  Fable review; W2-e's harness tests are the regression net for all 5)
- W3-a DAT-857+868+715-residue (Opus·high; Fable review — semantic verdict surface)
  **Pinned AC from lead finding (2026-07-28, gross-margin screenshot):** a ratio over flow
  carriers (gross_margin) OFFERS month/quarter time bucketing with per-bucket RECOMPUTE
  (carriers summed per bucket, formula recomputed, total row dashes as non-reconciling) —
  the boolean time gate's "TIME GRAIN OFF + raw 365-day categorical date" state is the
  defect this cut exists to end. Companion: once a bucketable time axis exists for a target,
  the raw-date categorical slice ranks last/demoted (it is the least honest form). W2-b's
  time-bucketing chips (DAT-673 P2) consume the same verdicts — coordinate the two lanes.
  · W3-b DAT-838 + per-entity metric grain (discovered, absorbed — B1 exit needs it; Opus·high,
  raised from Sonnet: grouped-composition design spans models+composers+mirrors, same declaration
  chain as the predicate so one coherent cut)
  · W3-c DAT-887 (ABSORBED 2026-07-28, lead-flagged eval blocker): point-in-time extracts bind
  naked MAX(period), unbound to fiscal window — AP 37.6% off, AR right by luck. Fix = period/window
  binding on point-in-time extracts riding DAT-730's calendar/period-grain substrate + the RESOLVED
  period recorded on the snippet/artifact (the ticket's required observable). Opus·high.
  PULLED FORWARD 2026-07-28 (Lane A: 887 decides whether a pass is worth its cost) — spawned
  ahead of W3-b; risk accepted: shared declaration chain, W3-b builds grouped composition ON
  W3-c's seam instead of the reverse. Schema/snippet-field changes ⇒ cockpit mirror regen is
  an owner integration step.
- W1-f surrogate serving seam (DISCOVERED 2026-07-28, W1-c spec sweep): ~11 LLM prompt sites /
  ~10 column sources; two physical DESCRIBE paths unfilterable by ORM (graphs/agent.py:1261 —
  which ALSO feeds validate_grounding_basis, so contract-v2 ACCEPTS surrogates while
  served_membership rejects them; hierarchies/processor.py:245); unfiltered: validation/
  resolver.py:411, cycles/context.py:322, enriched_views_phase.py:226, slicing_phase.py:805,
  semantic/utils.py::load_persisted_annotations, og_columns projection (which _read_references
  legitimately needs surrogates from — wholesale view filter would break relationship evidence).
  /REFINE FIRST — candidate cuts: exclude _sk__* from the enriched-view PROJECTION (joins keep
  using them; DESCRIBE class clean by construction; cockpit-visible change) vs a single shared
  column-serving seam. Opus·high. After W1 closes; does not gate the B1 scorecard.
- W1-e DAT-874 (ABSORBED 2026-07-28, lead-flagged eval blocker; Jira says Done — VERIFY FIRST):
  validation induction compares stock to flow while column temporal_behavior is determined for both
  sides; repro signature = 2 of the 3 pre-existing baseline eval fails (clean_scores_within_measured_
  bands + clean_readiness_no_regression, 12×CTC 0.500). Fix direction: column-level temporal_behavior
  served/honored at induction so a stock↔flow comparison is impossible or abstains. SERIALIZED after
  W1-d (same serving seam). Opus·high.
- W1-g DAT-889 (ABSORBED 2026-07-28, Lane A pass-gate; Sonnet·high): column_annotation runaway
  → recoverable. Guard, not prompt surgery: on stop_reason=max_tokens, retry the phase with a
  reduced table batch (automating the existing canned hint), bounded retries, loud disclosure;
  structured output for the phase is a candidate follow-up but needs a live compile probe
  (lead-gated) — refine notes it, does not build it.
- W1-h DAT-877 (ABSORBED 2026-07-28, Lane A pass-gate; Opus·high, VERIFY-FIRST): why does
  operatingModelWorkflow re-run induction on the referenced corpus, is the retry deterministic,
  does it always converge, and can the graded-head window (incomplete first generation visible
  between generations) exist? Evidence: code path (workflows/activities/induction phase +
  Temporal retry policy) + READ-ONLY Temporal history of our own smoke run on the running
  stack. If it can terminate short → correctness blocker, fix in-lane; if convergent →
  close the window (run-scoped reads / idempotent-writer class, ADR-0010), never a sleep.
- W4-a DAT-737 (Sonnet·high) · W4-b DAT-809+740 (Opus·high)
- W5-a DAT-880 (Sonnet·medium) · W5-b DAT-881+882 (Sonnet·medium) · W5-c DAT-883 + reporting_intent
  rename (Sonnet·high; rename needs lead approval at checkpoint)
- W6-a DAT-739 executor (Opus·high)
Sequencing: 847 before W3 (role fix feeds verdicts). 879 lands before/with 678 (axes). W2-d after
W2-c (676/627 depend on 678 per dep graph). Reviewers: senior-code-reviewer + spec-compliance
at every integration, Opus·high floor; Fable on prompt/evidence-surface diffs (709, 878 prompt,
857/868, 739, 673 chips prompt if any).

## Lane briefs

### W1-a DAT-847 — stock/flow honesty + period-FK snapshot role · Opus·high
Goal: (1) `reconcile.py:106` tie-break gets an abstention margin — near-tie between r_stock/
r_flow abstains (typed, existing abstention shapes) instead of picking a side; margin justified
from the residual statistics themselves, NEVER tuned to clean-flat numbers. (2) `derive_table_role`
(`semantic/db_models.py:137-153`) recognizes a snapshot grained on a period FK: grain column that
FK-references a period/date dimension counts as time-in-grain → PERIODIC_SNAPSHOT. Call site
`semantic/agent.py:259-267` passes event-role date cols only (DAT-780) — understand why before
widening; the fix must not regress DAT-780. Downstream check: `additivity_resolver.py:222`
snapshot_count rule now sees the corrected role (assert in an integration test).
Surface: analysis/lineage/reconcile.py · analysis/semantic/db_models.py · semantic/agent.py ·
their tests. Do NOT touch: graphs/additivity*.py (W3), entropy/resolve.py (done), cockpit.

### W1-b DAT-709 — cross-concept grounding disambiguation · Opus·high
Goal: two concepts silently grounding to the IDENTICAL extract becomes impossible — detected
→ disambiguate (re-ground with sibling-extract context served) or typed abstention. NO
deterministic override of the LLM judgment (locked); the guard detects and re-prompts/abstains,
never hardcodes a pick. Compare on normalized statement across concepts sharing a disjoint
group/family (disjointness today only via convention concept_groups —
`concept_edge_store.py:77-81`; finance ontology has no disjoint block — check coverage).
Surface: engine graphs/ grounding path + persistence lookup (`query/snippet_library.py`),
graphs/context.py disjoint serving, `dataraum-config/llm/prompts/graph_sql_generation.yaml`
(prompt diff ⇒ Fable review at integration). Do NOT touch: additivity, cockpit.
Acceptance: test where two disjoint concepts produce identical normalized statements → guard
fires (re-ground or abstain); no silent identical pair persists.

### W1-c DAT-878 + DAT-748 · Sonnet·high
Goal 878: minted `_sk__*` surrogate columns stop reaching the meaning prompt — filter via
`is_surrogate_column`/`SURROGATE_PREFIX` (`relationships/surrogate.py:32`) at the catalogue
context load (`catalogue/context.py:758-764`); check the other prompt builders (semantic/,
graphs/context.py) for the same leak. Evidence-surface change ⇒ Fable review at integration.
Goal 748: typing stops blindly trusting catalog raw_type for DB sources — minimal honest
cross-check (refine decides: TRY_CAST sample or DESCRIBE re-verify) for non-VARCHAR columns;
do not over-build (`typing/inference.py:79-84`, `typing_phase.py:84-88,189`).
Do NOT touch: relationships/surrogate.py logic, prompts YAML wording, cockpit.

### W1-e DAT-874 · Opus·high — verify-first on the EPIC branch (grain_facts now renders
column temporal_behavior + stored_sign post-W1-d; the gap may have narrowed). If live: serve/
honor column-level temporal_behavior at the induction seam so stock↔flow direct comparison is
detectable/abstained — floor + facts, NEVER a deterministic rewrite of the check SQL. W1-b's
disjoint floor bullet is the prompt precedent. Do not touch: additivity, reconcile.py.

### W2-a DAT-879 · Opus·high — comparable relevance score replacing ordinal slice_priority +
UNRANKED=1000 floor (absorbs 280/622/827); threshold the catalog, describe drops; vehicle for
the graphs/context.py 3-way split (model / reads / formatter; CURATED_SLICE_BUDGET lives
there + cockpit query-context.ts counterpart — W2-a owns ONLY the slice-budget lines there).
Do not touch: drillable-grid/answer surfaces, drill-axes chips (W2-b/c).

### W2-c DAT-678 · Opus·high — answers become drillable: parts-at-source + tier-A fallback.
Seam map in substrate (query.ts capture; canvas boundary drops components; saveQuerySnippet
writes no parts; tier A composeTierA works on any flat SQL). Refine decides answer-time parts
vs save-time derivation; mount DrillableGrid on answer + run_sql + report detail with tier A
minimum. Do not touch: drill-axes.ts guidance surface (W2-b), metric-dag/step-check files.

### W1-d DAT-875 — typed stored_sign fact · Opus·high (raised from Sonnet: evidence-surface,
cross-layer typed-fact design; spawned 2026-07-28 after verification confirmed the claim live)
Goal: implement the lead-endorsed design from the ticket comment — typed `stored_sign` on
ColumnConcept per the DAT-657 temporal_behavior template (LLM-authored + data-reconciled + served
via grain_facts), so `sign_natural_balance` can be applied two-sidedly and the BS-vs-GL one-sided
normalization (deviation ≡ 2×|ending_balance|) becomes impossible or abstains. Serve the fact,
never post-process the SQL (agentic ≠ deterministic). Engine + schema.sql only; cockpit mirror
regen is an OWNER integration step, do not hand-edit generated files.

### W2-b DAT-673 remainder — drill guidance · Sonnet·high (spawned 2026-07-28)
Goal: the ranking that W2-a computed becomes VISIBLE and USABLE in the drill UI. Three items:
(1) driver-gain chips — slice_relevance/slice_interest are persisted and served (metadata
mirror regenerated) but no drill surface renders them; surface them on the slice menu as
ranked chips with honest provenance (primary/supporting/unjudged states as they exist — no
invented tiers); (2) hierarchy descent — hierarchy substrate exists engine-side with ZERO
drill-UI consumers; wire descent (drill down a level within a hierarchy axis) into the slice
menu; (3) Haiku fallback per the ticket for guidance text where no measured ranking exists —
disclosed as such, never dressed as measured. SCOPE OUT: time-bucketing chips (DAT-673 P2) —
they consume W3-a's per-(measure×axis) verdicts which don't exist yet; leave the seam, note it.
Ground against MERGED state, not the ticket's file:lines: drill-axes.ts had ~300 lines move to
drill-axes-adhoc.ts; ordering is compareSliceRows (best-wins fold, name tiebreak, NO priority
field — slice_priority is deleted); run_steps carries `source` not `nodeRef`. Read both files
fully before designing. Surface: cockpit tools/drill-axes*.ts, widgets/drillable-result-grid.tsx
(chips rendering), guidance prompt if item 3 needs one (prompt diff ⇒ flag for Fable review).
Do NOT touch: tools/query.ts prompt + duckdb/answer-source.ts (W2-f's surface), reports/mint
code (W2-d2), metric-dag/step-check files. /refine first; full cockpit gates; commit in
worktree `.claude/worktrees/w2-b`, branch `feat/dat-671-drill-guidance` off epic branch.

### W2-d2 DAT-676 + DAT-627 — drill persistence + child reports · Sonnet·medium (spawned
2026-07-28)
Goal — the two red scorecard bands: (1) DAT-676: drill steps survive reload — persist drill
state so a reloaded canvas/report restores the drilled result, not the base; (2) DAT-627:
a drilled result mints a CHILD report with `parentId` — widen cockpit_db `reports` (parentId
self-ref, widen reports.confidence, store drill params), mint flow sets them, report detail
shows lineage (child links to parent). Then RE-ENABLE the blocked drilled/pinned mints in
widgets/answer-result.tsx — the block was an honesty stopgap because minted state didn't
persist; with persistence + parentId the mint is honest. Keep the amber/blocked path for
whatever remains genuinely non-mintable. Drizzle migration discipline: generate, never
hand-edit; migrations re-dump on rebase — never hand-merge; sibling lanes share the compose
Postgres volume, so leave the shared stack ALONE (owner browser-verifies at integration).
Surface: cockpit db/schema + migrations (cockpit_db only), tools report mint path, report
detail route/widgets, answer-result.tsx (unblock only). Do NOT touch: drillable-result-grid.tsx
(W2-b's — if you need a grid seam, END YOUR TURN and ask), drill-axes*.ts, tools/query.ts
prompt, engine. /refine first; full cockpit gates; worktree `.claude/worktrees/w2-d2`,
branch `feat/dat-671-report-persistence` off epic branch.

### W2-e cockpit integration-test harness · Opus·high (NEW, lead-approved 2026-07-28; spawned
2026-07-28)
Why (retro finding, lead-accepted): every high-value cockpit defect this wave was invisible
to unit/tsc/build — the gates are structural, the cockpit's truth is UX-semantic. This lane
builds the missing oracle: a fixture-workspace integration harness where the REAL routes/
composers/narrows run against a REAL seeded schema and "the feature fires" is a failing test.
Goal: (1) revive `bun run test:integration` — 3 pre-existing failures "Invalid cockpit base
configuration" in src/duckdb/{connect,probe,probe-stream}; diagnose the config seam first,
it's the harness's front door. (2) Fixture workspace: seed a Postgres testcontainer with the
engine's generated `ws_` schema (packages/engine schema.sql + schema_read.sql read views) +
canned production-shape rows; point the cockpit's metadata client at it; NO live LLM ever
(house rule), no Temporal needed. Refine decides the DuckDB story (answer execution) — a
metadata+composer-surface harness that lands is worth more than a full-stack harness that
doesn't; scope DuckDB execution in only if the config fix makes it cheap. (3) First coverage,
results+reports surface (lead-named priority): answer-source proof/parts path fires on
PRODUCTION-SHAPED SQL (qualified relations, aliased projections, CASE-guard scalars — the
exact shapes that broke live); tier-A axis derivation on aliased projections; metric-dag
narrows parity (checks reach the render path); report mint round-trip (coordinate with
W2-d2's schema — if it hasn't merged, write against current schema and note the follow-up).
STANDING RULE this lane establishes (write it into the cockpit test README/CLAUDE.md):
boundary tests use production-shape fixtures, never idealized bare-name SQL.
Do NOT touch: product source beyond minimal config seams (flag each in your report), feature
bugs you find (record them — the honesty lane owns the known ones), engine, shared compose
stack (use testcontainers). No wrapper deps — primary libs only. /refine first; full cockpit
gates including the revived test:integration project; worktree `.claude/worktrees/w2-e`,
branch `feat/dat-671-cockpit-it-harness` off epic branch.

### W2-g slice-menu curation + drilled-projection hygiene · Sonnet·high (spawned 2026-07-28,
lead-directed from his three post-close findings)
Goal — one root, four faces. THE RULE (lead ruling, verbatim intent): "we should not slice
on already existing slices" — a candidate axis whose column NAME matches one of the result's
existing non-measure identifier columns is EXCLUDED from the Slice menu, on BOTH axis paths
(tier-A adhoc name-intersection AND parts-at-source catalog axes — the lead's two SQL
examples cover both: the drill-wrap re-grouping `account_id__name` and the plain report SQL
whose top identifier is `account_id__name`). Name collisions are ACCEPTED for now — a
different catalog column sharing the name is excluded too; no disambiguation machinery.
(1) AMENDED (lead, same day): GREY OUT, don't exclude — the identifier axis stays VISIBLE
in the menu but DISABLED, carrying its reason on the item (short label/tooltip). The
determination may use SIMPLE structural SQL inspection (one-hop parser-tree reads — outer
projection/GROUP BY names via the existing parseSqlToJson seam); hard lines: no string/
regex matching over SQL, no complex parsing (nested CTE walking, rewriting) — fall back to
result-schema names if the read isn't one-hop.
(2) With greying, the menu never empties from this rule — no new empty-menu machinery; the
Slice button stays enabled. The post-execution fold probe STAYS as the net for cases the
determination can't decide (pins, functionally-dependent columns).
(3) Drilled-projection hygiene: the internal `_count` probe column must never render; a
re-wrap of an already-drilled stored SQL must not compound aggregate labels
(sum(sum(value)), sum(count)) — normalize the drilled projection's column faces at mint
time so a child report presents clean names. With (1) the tautology path is gone, but pins
still reach a re-wrap — hygiene applies regardless.
(4) Child-mint title stacking: "(drilled) (drilled)" — the suffix appends without checking
it's already present; make title composition idempotent.
Surface: cockpit tools/drill-axes.ts + drill-axes-adhoc.ts (axis-list construction only),
duckdb/drill.ts/drill-sql.ts (projection naming), drillable-grid.tsx note text (small,
fenced to the disabled-state note), reports mint title composition, tests incl. harness
coverage for the exclusion on both paths. Do NOT touch: the fold probe's logic, additivity/
time-grain gating (W3-a's), tools/query.ts prompt, engine. Worktree
`.claude/worktrees/w2-g`, branch `feat/dat-671-slice-curation`.

### W3-a additivity verdicts + grammar (DAT-857 + DAT-868 + DAT-715 residue) · Opus·high
(spawned 2026-07-28)
Goal: the boolean time gate becomes per-(measure×axis) VERDICTS with typed abstention —
the P2 cut the P1 midpoint was always waiting for. Zero code exists on main for 857/868.
**PINNED AC (lead, 2026-07-28, gross-margin screenshots — re-affirmed today with "no
interim guard, this is the fix"):** a ratio over flow carriers (gross_margin) OFFERS
month/quarter time bucketing with per-bucket RECOMPUTE — carriers summed per bucket, the
formula recomputed per bucket, the total row rendering dashes as non-reconciling — and
once a bucketable time axis exists for a target, the raw-date categorical slice ranks
LAST/demoted (it is the least honest form; today it renders 365 rows of carrier-misaligned
dashes while the meaningful ask is withheld).
Scope: (a) engine — per-(measure×dimension/axis) additivity verdict model (additive /
semi-additive / non-additive-recompute / abstain, typed), derived from the aggregation-
lineage + materialization substrate; persisted + served through read views (mirror regen =
owner step); (b) cockpit — DELETE the 2-state additivity read AND the second local judge
`flattenAdditive` in parts.ts (both named in the spec as clean cuts); consume verdicts at
the drill surface: time-bucketing chips per W2-b's seam (the PersistedAdditivity doc
comment in drill-axes.ts names it), per-bucket recompute composition, demotion ordering;
(c) fold in DAT-715 residue — sql-ast.ts:84-90 fails closed on window/FILTER: extend the
grammar so those parse rather than fail-closed, per DAT-868's description.
Locked rules: verdicts are SERVED FACTS from the substrate — no name heuristics, no
SQL parsing as typed data, abstention falls loud (an unjudged (measure×axis) renders as
withheld-with-reason, never as a silent enable). Prompt/evidence-surface diffs ⇒ Fable
review at integration. /refine FIRST — this is the design-dense lane of the wave; the
riskiest assumption is how the per-bucket recompute composes for ratio formulas (spike
that first). Coordinate note: W2-g (running) owns axis-menu EXCLUSION mechanics; you own
gating/demotion/bucketing — the seam is drill-axes.ts, keep your edits to the verdict-
consumption region; owner resolves overlap at merge (W2-g merges first).
Do NOT touch: axis-menu name-exclusion logic (W2-g), reports/mint, dataraum-eval.
Worktree `.claude/worktrees/w3-a`, branch `feat/dat-671-additivity-verdicts`.

### W3-b DAT-838 + per-entity metric grain · Opus·high (spawned 2026-07-28, after W3-a+c
merged — builds on BOTH their seams)
Goal — the B1 engine exit's missing piece: the engine's own SQL computes a PREDICATE-
CARRYING PER-ENTITY UNIT METRIC, drilled one level down, additivity-gated. Two halves,
one coherent cut on the declaration chain:
(1) DAT-838 declaration side: extracts declare their WHERE predicates as typed declaration
data — the substrate EXISTS (ExtractGroundingOutput.where, compose_extract_sql); the cut
is the declaration contract + persistence + serving, not new composition machinery. Read
DAT-838 for the ticket's observables.
(2) Per-entity grain (discovered work, absorbed — RFC 3 B1 exit): grouped composition —
a metric computable PER ENTITY (per account, per customer) as a unit-grain result, not
only as a workspace scalar; spans the declaration models + composers + (mirror regen =
owner step). This is the declaration-chain cut W3-c's seam was kept minimal FOR: you are
the intended consumer of that seam (period_binding precedent for additive declaration
fields; SnippetAssumption.dimension precedent). W3-a's verdicts are the additivity gate —
a per-entity breakdown offers/withholds by the served per-(target×axis) verdict, never a
local judge (both local judges are deleted; do not reintroduce one).
Locked rules: model authors against served facts; no SQL parsing as typed data; absence
falls loud; no name heuristics; corpus is one verification instance. Prompt diffs ⇒ Fable
review. /refine FIRST — the riskiest assumption is how grouped composition interacts with
the period binding on stock extracts (a per-entity AP balance is the crossing case: per-
entity grain × fiscal-close binding); spike that crossing first and STOP to ask if it
wants to restructure either seam rather than extend them.
Surface: engine graphs/ (models additive fields, composers, agent), schema.sql/read views
(dump_ddl ABSOLUTE path), tests both tiers. Cockpit: only if the drill surface needs the
grain served — keep it minimal; the axis-menu/greying/verdict-gating surfaces are settled,
consume don't rework. Do NOT touch: additivity classifier/resolver (W3-a's, merged),
boundary_resolver internals (W3-c's, merged — EXTEND via its call surface only),
reports/mint, dataraum-eval. Worktree `.claude/worktrees/w3-b`, branch
`feat/dat-671-per-entity-grain`.

### W2-f answer-drill honesty · Opus·high (spawned 2026-07-28, after harness merge)
Goal: the answer-drill path stops being silently dishonest in the FIVE known ways. The W2-e
harness (merged) is the regression net — two of its tests currently PIN the bugs with
expect-false assertions; flip them as the fixes land, and add coverage for the rest.
[1] CASE-guard prompt carve-out: tools/query.ts's `source` fill-in prompt demands "ONE
aggregate", but the house empty-aggregation rule wraps every scalar in CASE WHEN COUNT(*)=0
(3 aggregate calls) → the model honestly abstains → parts-at-source inert on scalars. The
harness proved the COMPOSER handles CASE-guarded scalars fine — the prompt is the blocker.
Fix = carve-out (the inert guard doesn't count toward "one aggregate") ± proof-side guard
handling; the executed proof stays the arbiter. Prompt diff ⇒ Fable review at integration.
[2] Alias gap: model-authored aliases miss the catalog spelling → tier A "no axes" on most
grouped answers; parts-at-source is the DESIGNED answer, unblocked by [1].
[3] No-op self-grain slice (lead-found): tier A happily wraps a result in GROUP BY on its
own grain — count==1 per group, identity sum relabeled. Fix = post-execution
rows(drilled)==rows(base) → the EXISTING amber refusal ("already at this grain — nothing to
fold"); NEVER parse the base SQL to detect it.
[4] /api/drill/parts does not apply bareRelationName (parts.ts:104; its :122-123 comment is
false — Query.from() quotes the whole string as one identifier); safe today only because the
client pre-reduces. Harness test pins it expect-false — fix and flip.
[5] Model-authored `AS alias` in a declared value expr: narrowDeclaredSource only trims —
`SUM(x) AS revenue` composes to a double-AS parse error the proof swallows into a SILENT
tier-A downgrade. Enforce at declaration acceptance with disclosure (reject or strip via the
parser the proof already uses — never regex-extract typed data from SQL); harness test pins
it expect-false.
Surface: tools/query.ts prompt block, duckdb/answer-source.ts, tools/answer-source-proof.ts,
routes/api/drill/parts.ts, the drill compose/execution seam for [3], the harness tests.
Do NOT touch: drillable-grid.tsx (just merged from two lanes — if [3]'s refusal needs a grid
change, END YOUR TURN and ask; prefer the server-side refusal channel the grid already
renders), reports/mint, metric-dag files, engine. Branch from CURRENT epic tip (harness +
W2-d2 + W2-b all merged — read the merged state, not the ticket file:lines).
Worktree `.claude/worktrees/w2-f`, branch `feat/dat-671-answer-drill-honesty`.

### W1-g DAT-889 — column_annotation runaway guard · Sonnet·high (spawned 2026-07-28)
Goal: the ~1-in-2 killed pass becomes a recoverable retry. The phase annotates ALL tables in
one free-text-JSON call; a sampled runaway digit emission hits the SHARED thinking+output
max_tokens cap → pydantic json_invalid → non-retryable PhaseFailed with a canned hint ("raise
max_tokens or reduce the batch"). Automate the hint: on stop_reason=max_tokens (and the
json_invalid it produces), retry with a reduced table batch (halving or per-table), bounded
attempts, each retry disclosed in phase output — never silent. No prompt-wording surgery, no
max_tokens raise as the fix (the runaway would just run longer). Structured output for the
phase: note as follow-up in your report, do NOT build (needs a lead-gated live compile probe).
Evidence lives in the ledger Parked entry (healthy run 15,418 chars end_turn vs ~70k runaway
tail …9999). Surface: engine pipeline annotation phase + LLM call/error path + unit tests
(stub the LLM, simulate stop_reason=max_tokens). Do NOT touch: prompts YAML, other phases,
cockpit. Worktree `.claude/worktrees/w1-g`, branch `feat/dat-671-annotation-runaway-guard`.
COVERAGE INVARIANT (lead-reviewed 2026-07-28): the guard changes call granularity, NEVER
coverage — batches partition the same total work, results merge, every table/column annotated
on success. Partial success (batch A lands, batch B exhausts retries) → phase FAILS loudly
naming the gap; never a completed phase with silently thinner annotations. Unit test required
for exactly that case.

### W1-h DAT-877 — operating-model induction re-run + graded-head window · Opus·high,
VERIFY-FIRST (spawned 2026-07-28)
Eval-side observation (referenced corpus, pin aeb2421bd): operatingModelWorkflow re-runs
induction, reports "Workflow execution failed", and between generations the graded head is
the incomplete FIRST generation (20 generated / 0 executed validations) — the eval runner
asserts immediately post-pipeline and grades that window as a plausible red. Final head
recovers. clean-flat ran induction once, healthy. Answer IN ORDER: (1) WHY does the workflow
re-run induction on the referenced corpus (intended retry? activity failure + Temporal retry
policy? second generation by design?); (2) is the retry deterministic and does it ALWAYS
converge — if it can terminate short, this is a correctness blocker: fix termination first;
(3) close the graded-head window — an in-flight generation must never be readable as the
current head (run-scoped reads / deterministic current-pick / idempotent-writers ADR-0010
class; NEVER a sleep or an eval-side workaround — the eval asserting immediately is correct
behavior we must survive). Evidence: engine worker/workflows.py + activities + induction
phase code; READ-ONLY Temporal history of today's smoke on the RUNNING stack is allowed:
`docker compose -f packages/infra/docker-compose.yml run --rm --no-deps --entrypoint temporal
temporal-admin-tools workflow list|show … --address temporal:7233` — list/show ONLY; never
start/signal/terminate anything, never run the pipeline (real-LLM is lead-gated), never touch
dataraum-eval. Report findings BEFORE cutting the fix if the fix wants to change workflow
topology (cross-package contract risk). Worktree `.claude/worktrees/w1-h`, branch
`feat/dat-671-induction-rerun-window`.

### W3-c DAT-887 — period/window binding on point-in-time extracts · Opus·high (spawned
2026-07-28, pulled forward)
Goal: point-in-time extracts stop binding naked MAX(period) unbound to the fiscal window —
AP landed 37.6% off while AR was right by luck. Fix = period/window binding riding DAT-730's
calendar/period-grain substrate, and the RESOLVED period recorded on the snippet/artifact —
that recorded observable is the ticket's acceptance criterion, not a nice-to-have. Serve the
period facts to the authoring path and bind at composition; never post-process generated SQL
into typed data (forbidden) and never a deterministic rewrite of the model's SQL — the model
authors against served facts, composition enforces the window. You are AHEAD of W3-b (grouped
composition / per-entity grain) on the shared declaration chain: keep your declaration seam
minimal and additive — W3-b builds on it. Schema or snippet-field changes: engine + schema.sql
only; cockpit mirror regen is an OWNER integration step. Prompt diffs ⇒ flag for Fable review.
Do NOT touch: additivity verdicts (W3-a), cockpit, dataraum-eval. Read DAT-887 + DAT-730's
landed substrate first. Worktree `.claude/worktrees/w3-c`, branch
`feat/dat-671-period-binding`.

## Review verdicts (2026-07-28, six-lane batch — full detail; head keeps only the queue)
- **W1-g** (DAT-889) spec+sr PASS-w/FI. Fold-ins sent: instance budget max(6, 2·tables) (a
  full binary tree costs 7 calls on 8 tables — probed); missing-tables-after-clean-parse
  pushed back as retry batch + post-loop safety fail (under-return = same sampled class);
  test pinning the gap message names ALL unannotated (popped + pending); warnings get real
  readers (phase_done log lines + summary suffix — PhaseRun/PhaseOutcome stay untouched);
  retry_notes appended on render/contract-break fails; per-attempt dump label (same-table
  retry truncates the runaway payload the eval wants — dump_prompt truncates, dump_response
  appends, keyed (label, prompt_hash)); content_tail on the runaway log; wording corrections
  (DAT-807 structured-output truth; batch-of-one comment; bogus fixture field; empty-batch
  short-circuit). Senior verified: budget check pre-call (call 7 impossible), no partial
  leak on any failure path, deterministic profiles order (DAT-725), stop_reason typed hook
  correct, synthesize_tables confirmed same-class sibling.
- **W1-h** (DAT-877) spec PASS, zero fold-ins. Verify-first grounded (workflows.py:842-849
  single schedule; transcript audited — list/show only, zero mutating stack commands, zero
  eval-repo refs); severity fork judgment CORRECT: no retry loop exists, "fix termination"
  would force a failed run to publish, inverting the house invariant "failed runs never
  surface" (workflows.py:862-865) — the promote gate subsumes it; robust to the unattributed
  first-execution failure (re-stage in place same run_id; new run_id publishes only on
  promote). DDL round-trip clean (schema-drift CI will pass engine-side). All vocabulary
  readers audited: select(Validation) exactly once repo-wide; unscoped call sites correct
  (om_run_id = promoted head); new queries ORDER BY validation_id. Jira timing-argument
  cosmetic note: the decisive evidence is 40 rows = exactly two induction writes. Senior
  PENDING.
- **W2-d2** (676+627) sr+spec PASS-w/FI, criticals found independently by both: (1)
  computeReportFingerprint(sql) takes no params — verified LIVE ("Expected 1 parameters"):
  summaryFingerprint always null on pinned mints, detail-open throws-and-logs every time,
  outdated pinned false forever, regenerate latently throwing; (2) ReportChartThumbnail has
  no params prop, hardcodes useChartData(sql, undefined), drops error → pinned+charted mint
  = permanently blank card re-firing on scroll (reachable: pin → author chart → mint; child
  mints unaffected — why the lane missed it). Jira authorization verified verbatim (676:
  URL-params IS the ticket's mechanism; 627: parentId:null for answer mints specified).
  Fence letter kept, escape-hatch taken without asking (brief named the 54-line wrapper;
  the real surface is drillable-grid.tsx) — judgment reasonable, notification owed.
  merge-tree CONFIRMS content conflict with W2-b in drillable-grid.tsx + test (imports +
  Alert block); resolution = union of additive edits. Authz structural, migration snapshot-
  verified safe on populated, URL path fail-closed. Round sent (incl. W2-e's two report-
  surface defects: confidence-read validation/errorComponent; band-badge Object.hasOwn).
- **W2-b** (DAT-673) spec PASS + sr PASS-w/FI. Criticals: (1) client sends ALL axes, route
  caps .max(8), action only offered on pure-substrate nodes (routinely >8) → raw zod 400 in
  the designed-for case; cap client-side with the imported constant; (2) hierarchy read has
  no ORDER BY under a first-wins fold — "Descend to X" flips between valid targets on
  identical data; (3) hierarchy-read→descent seam mutation-proven untested (empty-read
  substitution leaves 41/41 green) → one seeded resolveDrillAxes test. Fold-ins: empty-
  suggestion truthy-Map dead end; no abort/timeout on Haiku call; 3-sig-digit gains (toFixed(2)
  renders "Driver · 0.00"); promoted entry reuses item body. Verified good: DrillAxis
  widening compiler-proven at all sites; engine hierarchy claims independently confirmed
  (fact-id join, projected-name namespace, {fk}__{attr} over-specific but load-bearing claim
  right); ordering untouched; honest-null discipline real; DAT-762 precedent verified.
  Round sent. Owner rulings: Haiku gate stays strict through browser pass; time-bucketing
  seam correctly left.
- **W1-h senior** (arrived after the head entry): PASS-w/FI. Blocking beyond the known regen:
  context_reads.py:412 + config.py:109 load validation SPECS head-free while RESULTS are
  run-scoped — in-run metrics context drops newly-induced ids at the :427 continue and
  evaluates both-generation ids at the PRIOR tolerance; cycle-health pass_rate same. Verified
  good: atomicity real (one session_scope, one commit); promote serialization structural
  (workflow-id uniqueness, same id both packages); schema artifacts byte-identical on
  regeneration; zero blast radius on other activities; tests pin the exact eval scenario.
  Improvements: retirement impossibility (empty staging conflates zero-proposal with
  degraded); Pydantic round-trip = only new raise-site inside promote txn (retryable ×5 →
  OM fails post-spine); retry non-quiescence mints phantom superseded generations visible on
  the __READ__.validations pass-through; missing atomicity test; stale workflows.py comment.
  Eval premise check: __READ__.validations includes superseded history BY CONTRACT — posted
  to DAT-877.
- **W2-e spec** PASS-w/FI: all 4 goals verified (11 REQUIRED_DEFAULTS copies → one home with
  root cause named; providedByEnvironment beats placeholder-masquerade; credential-not-
  reachability S3 gate; fixture = real schema.sql/schema_read.sql layout the mirror was
  introspected from + real drizzle-kit migrate; docker-off probed loud; gates re-run exact).
  Fold-ins = owner-at-merge: ci.yml wiring + stale comment; "fourteen"→eleven doc figure.
  Coverage note: metric-dag narrows parity pinned by pre-existing unit test + new fixture
  test on the parse path; lib/metric-dag.ts narrowDag has no fixture-backed coverage of its
  own (informational).
- **W3-c** (DAT-887) lane report: substrate truth diverges from ticket — workspace_calendar
  has ZERO writers (every workspace on default January; eval corpus IS January ⇒ a green
  eval exercises the default path only; declared-April covered by lane tests);
  last_period_complete structurally cannot detect the alignment class (fill-fraction 0 ⇒
  trailing period reads complete); the binding seam was pre-declared at
  metric_graph_db_models.py:72-79 — DAT-887 is its first consumer. Binding rule: latest
  fiscal close ≤ relation max period, then snap to latest carried period ≤ close (stamping-
  convention-free by construction). Predicate-append cannot fix a model pin (AND on a MAX-pin
  yields empty) ⇒ prompt reversal is load-bearing; non-compliant runs DEFER with typed
  period.binding assumption. Reviewers running.

- **W3-c senior: FAIL** (craft acknowledged; falsified by EXECUTING _bind_to_close on
  corpus-shaped data). Core: typing.yaml standardizes period labels to period-START DATEs
  ('2025-12' → 2025-12-01 carrying December's CLOSING level), so close-instant (Y,m,1) +
  `<=` snap binds ONE PERIOD LATE (corpus: 2026-01 → -1,655,008.12 vs truth Dec-2025 →
  -2,141,863.57; naked MAX gave -1,591,296.01 — moved, not landed). Complete-FY end-stamped
  falls LOUD on good data; start-stamped silently binds JANUARY; daily off-by-one. Forced
  shape: strict `<` snap + grain-aware reachability (grain already on the substrate, unread).
  Critical 3: unconditional prompt leave-out + partial resolver ⇒ every non-resolving branch
  composes a predicate-free stock extract (~14× off) — WORSE than the defect; coupling must
  be settled in refine. Critical 4: binding_record→parts wire cut-with-suite-green (one test
  pins its ABSENCE). Critical 5: unguarded calendar read kills context assembly on
  pre-DAT-730 schemas + poisons the txn. Also: except-handler leaves poisoned session;
  unbound disclosure at confidence 1.0 passes the 0.5 floor gate; dimension key dropped at
  SnippetAssumption persistence; imperative leaks into the induction prompt via shared
  context_format; TIMESTAMPTZ as_of varies with worker TZ. Confirmed good: no-override real
  (traced through where_filter_columns AST walk), no SQL-as-typed-data, cached sql+binding
  same staleness row, calendar_source faithful end-to-end. Rework round sent 2026-07-28.
- **W1-g round applied** (f6f5f019f): budget max(6,2N) as method; omission re-queued on the
  same retry path + post-loop check; gap-message regex-pinned (naive assertion proven green
  under mutation first); phase_done warnings + summary suffix; _fail_with_retries closure;
  content_tail; DAT-807 wording fixed; per-attempt dump label investigated & PARKED (blocked
  by shared cross-agent label assertion in test_agent_request_shape.py — 9 agents share it).
  3178P. MERGED.
- **W1-h round applied** (ba85bc094): third reader run-scoped both legs + drifted-tolerance
  test (in-run 0.5 vs head 0.02); induction SEAL via new run-stamped induction_runs table
  (sealed-empty supersedes → retirement restored; unsealed keeps prior; grain gate caught
  the dump classification); promote txn freed of Pydantic raise-site; content-keyed retry
  quiescence (same row_id survives); atomicity test with the fake session_scope corrected to
  mirror the real rollback contract; workflows.py comment-only fix. 3183P. MERGED. Mirror
  regen now covers TWO new tables.

## Eval history
- 2026-07-28 · WAVE-BOUNDARY SMOKE (lead-approved batch) on epic @ fresh stack, branch-verified
  containers · pipeline: add_source+begin_session+OM GREEN (32/44 executed, 12 honest
  impossibles, 15/15 validations grounded) after 1 retry (column_annotation runaway — bug
  recorded) · VALUES: revenue exact to cent (51,766,199.72 scalar AND 5-way breakdown);
  **DPO = 37.15 POSITIVE, AP normalized +2,207,436.54 — the DAT-886 class is gone live**
  (days_in_period=30 fallback disclosed); AR non-detection this run (sampled variance, honest
  declared state) · substrate live: stored_sign 3×ledger_signed/48 NULL with 50/51 claims
  'unsure' (witness-driven, as designed); slice_relevance [0,1.000] clamp held; 6 primary/
  6 supporting/12 disclosed-unjudged; 26 additivity rows · COMPILE PROBE PASS (run_steps
  schema compiled through the production adapter; 3 real chat turns) · JUDGED SCREENSHOTS:
  canvas check-indicators on exactly the 4 check-bearing metrics w/ full a11y labels; DPO
  analyse drill (per-group carriers, honest dashes, ranked menu w/ true value counts); answer
  drill tier A (sum(revenue) naming, Report DISABLED while drilled — blocked-mint live);
  report-detail drill green. Both AC drills browser-verified.
- SMOKE DISCOVERIES (one fix family): (1) `source` fill-in prompt says "ONE aggregate" but
  the house empty-aggregation rule wraps every scalar in CASE WHEN COUNT(*)=0 (3 aggregate
  calls) → model honestly abstains → parts-at-source inert on scalars; (2) model-authored
  aliases (account_name) miss the catalog spelling (account_id__name) → tier A "no axes" on
  most grouped answers — parts-at-source is the DESIGNED answer to aliasing and is blocked by
  (1). One prompt carve-out (the inert guard doesn't count) ± proof-side guard unwrap lights
  both. W2-c follow-up lane candidate.
- (3, lead-spotted on the minted report): tier A happily wraps a result in a GROUP BY on the
  result's OWN grain — count==1 per group, identity sum relabeled `sum(x)` — a no-op presented
  as analysis. Honest fix: post-execution `rows(drilled) == rows(base)` → the existing amber
  refusal ("already at this grain — nothing to fold"); never parse the base SQL to detect it.
  → the three items form ONE follow-up lane: answer-drill honesty (prompt carve-out unblocks
  parts-at-source; proven source solves the alias gap; no-op refusal).

## Decisions
- 2026-07-28 · W1-d config-entry REVERSAL (1st): owner deferred loss.yaml/reliabilities entries
  as "invented numbers" → reviewer showed the sanctioned in-file form (relationship_discovery
  PLACEHOLDER, calibrated: false, not tuned) · call: add both entries per precedent; abstention
  then genuinely reaches coverage · evidence: reliabilities.py:67 fallback makes NO-entry report
  as calibrated — worse than a marked placeholder · reversals: 1 (this one).
- 2026-07-28 · W1-d minor==0 fork: absent evidence ≠ positive ledger_signed — abstain unless
  the mirror family was observable (coverage floor from held quantities, no tuned number) ·
  evidence: executed inversion proof 6/6/0→6/0/0 flips the label; DAT-847 abstention makes
  silencing likelier · reversals: 0.
- 2026-07-28 · W1-a composite-calendar fork: reviewer's "keep composite via jointly-unique"
  proved impossible (per-column profiles carry no joint cardinality — probed) · call: narrow
  to grain∩unique, composite calendars under-claim to FACT (fails safe: COUNT keeps its axis;
  over-claim denied a real axis) · evidence: lane's 3-shape probe table · reversals: 0.
- 2026-07-28 · W1-a period-FK fork: grain-membership predicate → CARDINALITY-WITNESS widening
  (dimension's event date one-row-per-period, exact structural uniqueness, no tuned threshold;
  missing witness → FACT) · criterion: must fire on the surrogate-keyed calendar (the DAT-847
  shape) while dim_customer stays out on DATA, not on the LLM's grain choice · evidence: senior
  review trace — old predicate fired only where DAT-780 already covered · reversals: 0.
- 2026-07-28 · slice cut = one /own, six waves · lead-picked over two-owner splits.
- 2026-07-28 · closures 853/714/701/715-718 approved · grounded file:line · 715 residue → DAT-868.
- 2026-07-28 · reporting_intent: rename now; lane proposes, lead approves at checkpoint.
- 2026-07-28 · model mix as recorded in Plan · lead-approved.
- 2026-07-28 · roughness retro accepted by lead: cockpit's gates are structural, its truth is
  UX-semantic → integration-test harness (W2-e) pulled INTO this slice now ("becomes more and
  more stable, specifically the results and reports part"); journey-scripted hardening pass +
  interim-states-say-their-name sweep deferred to post-W3 session · lead-confirmed sequence:
  W2-b + W2-d2 against merged state, then W3 · Lane A eval cycle started on the side.
- 2026-07-28 · Lane A read absorbed: W3-c pulled ahead of W3-b · criterion: Lane A holds the
  pass on 889+877 and rates 887 as deciding run value; W2 trio is cockpit-side so engine lanes
  run free · risk accepted: shared declaration chain — W3-b now builds ON W3-c's seam · DAT-723
  NOT absorbed (precision-noise term, not a gate; same sampled-divergence class as the
  no-temperature reality — guard-class candidate, lead triage at checkpoint) · reversals: 0.
- 2026-07-28 · W1-h SEMANTIC INVERSION (forced, not elective — log per spec review): empty
  induction now leaves the prior generation STANDING (was: authoritative supersede).
  materialize_induced_validations reads only the staging table and cannot distinguish
  "staged zero" from "never ran" without a new marker; lane chose the safer semantics,
  documented ×3, test renamed accordingly. Revisit only if a marker signal ever lands.
  Companion: both OM phases now ctx.require_run_id() — un-runnable outside a stamped
  workflow run (loud, house style).
- 2026-07-28 · W2-b Haiku-gate fork: all-axes-unmeasured (strict, lane) vs per-axis (brief
  wording) · pre-registered criterion: the browser pass on a pure-substrate tier-A drill —
  if the affordance appears there, strict stands; if unreachable even there, widen ·
  reversals: 0 · RESOLVED at browser pass: Suggest visible on the pure-substrate report —
  strict gate STANDS.
- 2026-07-28 · LEAD RULINGS on the post-close findings (his pass, 3 screenshots): (1) the
  ratio/raw-date incoherence needs NO interim guard — W3-a's pinned AC is the fix, full
  stop. (2) Slice menus must NOT offer a column that is already a result identifier —
  name-based exclusion ("we should not slice on already existing slices"), NAME COLLISIONS
  ACCEPTED for now (a catalog column sharing the name is excluded too); applies to BOTH
  axis paths (tier-A adhoc AND parts-at-source — his two SQL examples cover both); the
  post-execution fold probe stays as the net for what the schema can't decide (pins,
  functional dependencies). (3) = same root as (2); plus projection hygiene (leaked
  `_count`, compounded sum(sum(value))/sum(count) labels, "(drilled) (drilled)" title
  stacking). AMENDED same day: **GREY OUT, don't exclude** — the identifier axis stays
  visible but disabled with its reason on the item; and **simple structural SQL inspection
  is PERMITTED** (one-hop parser-tree reads like outer projection/GROUP BY names off the
  existing parseSqlToJson seam) — the hard lines are NO string/regex matching over SQL and
  NO complex parsing (nested CTE walking, rewriting). Relayed to W2-g mid-flight.

## Checkpoint log
- 2026-07-28 · #1 W1 wave integrated (5 lanes, 2 rework rounds, 1 salvage split) · eval-blocker
  cluster engine-complete (886 sign both authors, 875 typed fact, 847 honesty, 709 guard) ·
  asked: stack ack + Lane A sync · parked list delivered · budget 0 e2e.
- 2026-07-28 · #2 wave-boundary batch closed (smoke green, values verified to cent, compile
  probe PASS, both AC drills browser-verified, blocked-mint live) · Lane A handover delivered
  · lead findings absorbed (no-op slice → W2-f item 3, gross-margin time grain → pinned W3-a
  AC) · roughness retro delivered, harness lane approved · budget 1 e2e spent · mirrored to
  DAT-671.
