# DAT-671 graph recovery — owner ledger (continuation slice)

Objective: the graph becomes the ONE serving spine — five questions (source / identity /
axes / permission / total) answered from it exactly once, consumed by the GraphAgent,
the answer agent, the drill routes, and the Model UI; every parallel assembly deleted;
journeys green on real data. Direction locked by lead 2026-07-29 ("graph-first"; the
kill-graph option was retracted as manufactured by the adoption failure itself).
Authority: ADR-0024 (Proposed, on the phase3 branch → lands with this slice).

Scorecard:
- Journeys J1-J8 green as integration tests against real routes (W2-e harness):
  J1 scalar revenue exact + axes WITH month grain · J2 slice-by-account sums + greyed
  reopen · J3 monthly buckets sum · J4 grouped result can slice further (today "no
  axes") · J5 metric-node monthly recompute + printed total = headline · J6 ANSWER-path
  parity with J5 · J7 aliased projection keeps the drill · J8 genuinely-unclassified
  withholds WITH reason (the only legitimate withhold).
- Adoption ratio: each of the five questions answered from the graph/one home; the
  private assemblies (query-context concept block, in-memory concept-graph rebuild,
  drill-axes-adhoc parallel fold, per-path verdict decisions) DELETED in the same PRs.
- Agents see verdicts: additivity reaches BOTH agents' served context.
- Wire-or-delete executed for the census's dead surface (15 og views / 16 dead mirrors /
  31 dead columns / disclosure channels): every item an explicit decision, no "keep for
  later". privacy.py DELETED (lead order, obsolete).
- Full deterministic gates green; ONE integration PR (lead: NO stacked PRs).
Non-goals: entropy target-key re-mint (PROPOSED triage-out — lead answer pending);
finance-decoupling of stored_sign/fiscal-close (recorded, next program phase); Lane A /
eval-repo work (still locked).
e2e budget: none standing — ask per run.

## NEXT ACTION
R5 REFINING (route/UI unification: adhoc fold dies, grey-out consumption, census
disclosures S3/S4/S6/S7/S8). R-HYG reviewer pair running → fold-in round (incl.
slicing 5-field trim) → merge → R-ADAPT spawns (brief in ledger). Then R6 (wire-or-
delete + decided items: enriched-views run-failures · og widenings · vertical guard ·
mirrored constants · YAML version bump). SCORECARD: J1-J8 GREEN · agents-see-verdicts
GREEN (R4 engine + R3 cockpit) · adoption ratio 2/4 private assemblies deleted ·
wire-or-delete open · one PR at end. Watch: eval-side pass results; entropy-key answer;
lead triage of LOW/SEMANTIC audit items + prompt-lint proposal.

## State
- Phase3 PR #539 MERGED to main 2026-07-29 (true merge; CI green after the
  workspace-isolation dev-cred env fix 72a2e8374). Recovery branch merged origin/main
  at 775fa9cfb (ledger modify/delete resolved keeping ours).
- R3 INTEGRATED 88a7ca3af (builder deleted, graph serves both consumers, verdicts+DAG
  in answer context; gates green 2298u/189i) · R4 INTEGRATED 0df935d45 (verdicts in
  GraphAgent document, S1 disclosed, adoption 13/26; gates green 3475).
- R2 INTEGRATED fed738c5d (lane f3d7ec13a, 30 files +1834/−402): ALL SIX JOURNEY
  PINS FLIPPED — journeys 25/25, zero .fails. SCORECARD BAND GREEN: J1-J8. Identity =
  snippetId→concept via GRAPH_TABLE MATCH; DrillTarget one-home verdicts both paths;
  totals+grain on answer path; J4 re-pointed per ADR-0024 d2. Lane ran both reviewers
  (nesting ON in this env): spec PASS; senior 2 real criticals (operand-count verdict
  collapse; adapted-SQL identity trust) fixed + independently re-verified. Gates green
  on tip (2305 unit / 170 int / build). R5 hand-off: client grey-out + per-path grain
  gate now redundant → R5 deletes.
- R-890 INTEGRATED f5e84efb2 (lane 91becbec: 2 commits; engine gates green on tip
  3457/8skip).
- R0 INTEGRATED 0d31daf0e · R1 INTEGRATED 1091df8d3 (journey suite: 22 green + 6 red
  pins J1b/J2b/J3/J4/J6/J7; census table in lane report). Both review pairs PASS, fold-
  ins folded. Gates green on tip after EACH merge (2281 unit / 159+6ef integration /
  build). NB: the integration worktree needs its own `bun install`.
- Jira: closeout comment on DAT-671; landed-fix comments on DAT-889 (ask#1 done, ask#2
  parked) + DAT-877 (window closed by construction). Ticket resolution = lead's call.
- The five audit reports live in this session's five Explore agents (cockpit overlap ·
  engine overlap · consumption census · string heuristics · finance coupling); their
  consolidated numbers are in ADR-0024 and the phase3 PR body. Key numbers: 42 analysis
  tables → 40% consumed / 26% dead mirror / 7% zero readers / 31 dead columns; 15/26 og
  views unread; 3 PGQ MATCHes repo-wide; snippet key ×7; stock/flow ×5 opposing
  policies; axis_key = served NAME cross-package; ~43 exact-name joins.

## Plan (lanes)
- R0 consumability substrate: GRANT ON PROPERTY GRAPH for the read role (find where the
  ws_<id>_read role is provisioned); a GRAPH_TABLE-via-Drizzle helper + ONE worked
  example; correct every false-constraint comment ("TS has no PGQ" in concept-graph.ts
  header + concept-graph-load.ts — the truth: PGQ executes in Postgres, client language
  irrelevant; PG19 gotchas: keys ::text, GRANT separate, fixed-depth only → recursive
  CTE for closures). No product behavior change.
- R1 journey suite: J1-J8 in packages/cockpit (W2-e harness, real routes, no mocked
  fetch; production-shape fixtures; numbers from dataraum-testdata ground_truth.yaml).
  Journeys that today FAIL (J4, J6, J7, parts of J1) land as fails-marked pins
  documenting current-vs-target, flipped by R2+.
- R2 resolution on the graph: resolveConceptTarget as MATCH over og_grounded_by /
  og_has_additivity / og_derives_from; snippetId+standardField carried on
  AnswerSource + DrillSource wire; parts path consults verdicts (kills the revenue
  withhold); tier-A alias resolution via sql-ast selectItemName; axis_key fragility
  closed at consumption. Flips R1's red pins (J1b, J2b, J3, J4, J6, J7 — suite at
  src/test/journeys.integration.test.ts; delete each `.fails` as it flips).
  R1-discovered inputs for R2: parts route z.strictObject REJECTS `grain` (the 400 is
  pinned — widening the input schema is part of the fix, not a side effect); parts
  route serves NO totals field; sql_snippets.parts.from is BARE by contract
  (validate_grounding_basis) and the NODE composer has no bareRelationName-style
  reduction — the answer path does; axes query key excludes `steps` so the server
  never learns what's already sliced.
- R3 answer agent onto the graph: concept block read via GRAPH_TABLE SQL (delete the
  in-memory rebuild in concept-graph.ts — builder stays only if the Model UI still
  needs a client shape, else delete); verdict/DAG edges added to the answer context.
- R4 GraphAgent context: additivity verdicts reach the served document; private
  assembly pieces replaced by MATCHes where the graph already carries the relation.
- R5 UI + drill routes on the same serving surface; drill-axes-adhoc's parallel fold
  and the per-path grain gates deleted (capability differences must trace to data).
  R1-discovered inputs for R5: drillable-grid's grey-out is client-side over local
  `steps` and never sets disabledReason (dead menu item, no explanation); verdict reads
  gate on the operating_model head not catalog (seeders and any new reader must honor
  it); slice_definitions.slice_type is CHECK'd 'categorical'-only — temporality comes
  solely from columns.resolved_type.
- R6 wire-or-delete sweep: privacy.py DELETE; dead columns/views/mirrors from the
  census each get wire-or-delete; disclosure channels decided (PhaseOutcome.warnings:
  widen the contract or delete the producers); dead metric_induction.yaml; the two
  entropy _id-suffix heuristics.
- R-890 semantic agents — FORENSIC REPORT DELIVERED (2026-07-29), fix design awaiting
  lead agreement. Findings: (1) CONFIRMED, perfect 12-phase separation: only the two
  sick agents ship raw sample values (88%/72% of prompt, 8,722/8,088 values; all ten
  healthy phases 0%) because DataSampler.prepare_samples' non-sensitive branch
  (llm/privacy.py:59-61) BYPASSES privacy.max_sample_values:10 and serves
  statistics.yaml top_k_values:200 — 20× the configured budget; under the cap the
  failing prompt is 131KB→~25KB. (2) CONFIRMED local trigger: column_annotation.yaml
  L100-128 orders fields the schema RETIRED (derived_formula_hypothesis,
  unit_source_column — DAT-637/823) which constrained decoding forbids; both
  pathologies fired at journal_lines.net_amount, exactly where those suppressed
  instructions peak (model smuggles the formula into description prose in all 3 runs;
  clean-flat same pass emitted a PHANTOM column at the same site, silently dropped by
  processor.py:269-270 continue). H1 prompt-side duplication KILLED (prompt clean, 91
  cols once each). H5 loose-pydantic KILLED with numbers (the two models are among the
  TIGHTEST: 0 optionals, 0 unions, confidence bounded; ""-legality universal — made
  the degenerate entry invisible, didn't cause it). H4 runaway artifact DESTROYED by
  the dump (label,prompt_hash) collision (parked). NOTE: both agents already run
  constrained decoding — the tickets' "free-text JSON" framing was imprecise.
  FIX APPROVED BY LEAD 2026-07-29 ("spot on"; frame: prompts/input/response formats
  are DATED — never adapted to other changes). Lane IMPLEMENTING: sample cap via the
  pulled-forward privacy.py DELETE (rehome capped serving on the healthy path); dead
  prompt sections deleted (+ audit rest of both YAMLs vs current models); responses
  limited to existing columns (phantom = disclosed drop; within-command duplicate =
  typed failure, NO fold); DAT-889 dump-collision fix rides along. EXTENSION (lead):
  staleness sweep of ALL prompt YAMLs + context builders (instructed-retired fields ·
  retired vocabulary · config-bound bypasses à la max_sample_values · dead config
  keys) — findings table only, owner routes. semantic_per_table = relationship
  judging: drift-repair only, NO redesign; semantics changes get reported not fixed.
  R-HYG config-hygiene lane QUEUED (lead-approved 2026-07-29, spawns AFTER R-890
  merges — same YAML files): (1) temperature knob DELETED end-to-end (PromptTemplate
  required field + extra=forbid → shared model change + all 12 YAMLs + request
  pass-through + provider drop logic); (2) caps consolidation per lead ("Claude loves
  writing MAX_XYZ caps… corner case bugs"): delete _ENTITY_FLOW_SAMPLE_BUDGET (read
  privacy.max_sample_values), delete the four char-truncation hardcodes; IF long-value
  bounding is genuinely needed (long-text columns re-bloat within 10 samples) it
  becomes ONE config-homed bound inside prompt_samples — else nothing. Principle
  recorded in agent memory (no-speculative-caps). ALSO in R-HYG scope (senior-review
  finds): cycles/context.py:774-815 slice value_counts rendered UNCAPPED into the
  business_cycles prompt (same class); catalogue/processor.py last-mention-wins fold
  (parked below) is a candidate if trivial.
  REVIEW-PROCESS NOTE: reviewers must read the ledger from the RECOVERY BRANCH TIP,
  not the lane worktree — a lane's checkout carries the ledger as of branch time and
  R-890's senior reviewer mis-flagged a lead-struck design as a deviation from it.
  Verification = eval side's next pass (no live calls from this repo).
  IMPLEMENTED eb27cb9c2 (24 files +410/−359, full engine gates green 3447): privacy.py
  DELETED, capped serving rehomed (semantic/utils.prompt_samples), all callers
  migrated; column_annotation.yaml v2.0.0 (dead sections + retired-prose header);
  persist returns Result — phantom = disclosed drop into PhaseResult.warnings,
  duplicate = typed fail naming both entries; dump keyed per attempt. Reviewer pair
  RUNNING. NOTE (visible, lead-covered): catalogue_semantics + business_cycles LOSE
  name-pattern sample redaction (the deleted egress no-op) — surfaced to lead.
  semantic_per_table.yaml audited CLEAN vs model; relationship logic untouched.
Fences: engine analysis internals only via call surfaces; worker/contracts.py changes
are cross-package (Fable review); dataraum-eval locked; no stacked PRs — everything
integrates on THIS branch, one PR at the end.

## Rulings (lead, 2026-07-29)
- PROMPT-CONTENT BOUNDS POLICY: CAP data-derived values (samples, value counts) +
  long corpus text — via config, one enforcement home. DO NOT cap metadata
  enumerations (table/column/slice/concept lists) — freak-level length FAILS HONESTLY,
  never silent truncation; if an enumeration risks growing long, consider a TOOL
  exposing it instead of prompt stuffing. Census of all prompt builders (engine +
  cockpit) running (Explore agent); result feeds R-HYG.
- All three recovery phases merged into this slice. Graph-first locked. No stacked PRs.
- privacy.py obsolete → delete. Footer dash-mask retired (recompute total prints).
- "We should prevent these situations": substrate-ships-with-consumer is a SPEC-time
  item (every new artifact names its reader in the spec conversation); false-constraint
  comments are a standing review target.
- Root cause accepted: graph was built supply-side (backend-up) instead of
  consumer-first; the original intent (better agent context) stalled at ring one —
  concept neighborhood serves the GraphAgent, everything beyond decorates.

## Parked (carried from phase3 + audits — closeout triage of the RECOVERY PR)
- Entropy name-composed target key re-mint (column:{id}) — proposed triage-out.
- stored_sign + fiscal-close-only boundary = the two hard finance couplings (audit #2);
  frame seed leaks 2/4 families; conventions have no authoring path for vertical #2.
- DAT-740 time-axis half (period-end LAST) still open.
- semantic_per_table has no in-phase retry on parse failure (1-of-N divergence class).
- test/global-setup.ts:85-102 (pre-R1): a throw during fixture setup skips the teardown
  closure entirely and leaks the container — inherited pattern, out of R1's lane scope.
- R-890 staleness sweep catalogue (owner routes): (1) temperature dead knob in ALL 12
  YAMLs — PromptTemplate.temperature is REQUIRED + extra=forbid, so the sweep = shared
  model change + 12 files, one small lane (this is DAT-889 ask #2, now with mechanism);
  (2) cycles/context.py:65 _ENTITY_FLOW_SAMPLE_BUDGET=10 hardcoded twin of
  max_sample_values — silently diverges on config change; (3) four hardcoded truncation
  caps (catalogue/cycles _SAMPLE_MAX_CHARS etc.), no config home; (4) enrichment_
  analysis.yaml "Pydantic tool" retired-prose ×2; (5) LLMPrivacy/privacy: YAML key now
  holds only a prompt budget — rename blocked by eval-vendored config until next
  re-vendor.
- DECIDED (lead 2026-07-29 "makes sense") → R6 work item: enriched_views_phase — BOTH
  silent per-fact paths become RUN FAILURES (whole-view grain-verification breach, a
  should-never-fire invariant; view CREATE exception → warning+continue at :495). The
  per-join grain filter (DAT-801) stays as the disclosed partial-degradation path for
  real-world dirty data.
- R-ADAPT (lead direction 2026-07-29: "lightly streamline" the operating-model agents
  + slicing): read-only input-vs-task audit RUNNING (all remaining LLM phases; per-phase
  deltas + trim confidence). Trim lane spawns AFTER R-HYG merges (shared catalogue/
  cycles builders) and AFTER lead sees the audit table. Trims/prose only — semantic
  changes report-not-fix. Slicing's 5-field trim already assigned to R-HYG fold-in.
- R6 og-widening candidates (R4 recommendations): og_grounding += failure_mode/
  failure_reason (then R3's + R4's private provenance reads both die); og_temporal_
  coverage += largest_gap_days + note (then the time-column assembly moves). One
  schema_graph.sql regen, two readers moved, at R6.
- R6: og_has_additivity joins concepts.name = target_key with NO vertical guard of its
  own (pre-existing, DAT-857/868) — narrow window post-vertical-change pre-promote; R4
  makes it reach LLM-facing text for the first time. Add the guard at R6.
- R6: PART_OF_MAX_DEPTH + AXIS_KEY_ALL are hand-mirrored cockpit↔engine constants —
  cockpit side now pinned against real engine-served rows (R3); ENGINE side has no
  guard either way. Close per the hand-mirrored-contract discipline (cross-package).
- OWNER at integration: bump graph_sql_generation.yaml version + changelog ONCE after
  both R4 and R-HYG are merged (both edit the file; lanes deferred deliberately).
- catalogue/processor.py:156-185 apply_column_concepts: last-mention-wins FOLD on
  within-response duplicate + debug-level silent phantom — same risk class R-890 just
  fixed in column_annotation, out of its fence (catalogue was a healthy phase).
  Candidate for R-HYG or R6. (R-890 spec-review observation.)
- DAT-889 hardening beyond the landed guard (lead, 2026-07-29, "another discussion"):
  strict tool-use for column_annotation + max-size bounds on JSON attributes
  (maxLength/maximum caps as an extra fence on runaway literals). Discuss before build.
- Answer-authored aliases lose drill affordance — FIXED by R2 (J7); remove from parked
  on landing.
- column_eligibility re-type-over-minted-table question (W1-f senior flag, unverified).
- derive_reconciles_with asserts on cardinality alone (predicate-disjoint populations).
- Reconciliation tolerance activation = ontology field + seed emitter (2 changes).
- MetricUnitGrain.target_kind 'measure' unreachable; bucket_grain unreconciled in
  dedupe; phase3 PR body carries the full earlier parked list.

## Census routing (prompt-content bounds, 2026-07-29 — full tables in census agent report)
- R-HYG (final scope sent): +slicing tables_json top_values (LARGEST uncapped site) ·
  +validation resolver distinct_values · [:500]/[:400] on AUTHORED PROSE deleted
  (metadata uncapped per policy) · corpus-value char bound consolidated to ONE config
  knob · entropy contracts.py:477,507 get "(+N more)" · GraphAgent value sets EXCLUDED
  (operational vocabulary + search_values tool = the tool-exposure pattern already).
- R4 gets: S1 — graphs/agent.py:1602-1615 prefer-enriched ALL-OR-NOTHING hides every
  typed fact + dim table from <data_schema> AND the contract validator's allow-list,
  zero disclosure. Engine original of the known cockpit gotcha.
- R3/R5 get: S2 cockpit preferEnriched <schema> twin (disclosure in wrong blocks) ·
  S3 digest blocker slice(0,5) undisclosed · S4 axis-guidance slice undisclosed ·
  S6 INTROSPECTION_ROW_LIMIT drops column tail w/o truncated signal · S7
  SAMPLE_TABLE_CAP → empty sampleValues ambiguous · S8 unresolved relationship
  silently continue'd out of <relationships> · cockpit why_* raw evidence JSON
  uncapped (B2, highest-volume cockpit free text) · query-context distinctValues
  dead read (delete).
- Do-it-right patterns (cite in briefs): slicing/curation.py CuratedSlices.note ·
  agent-sample.ts truncated flag · look-values.ts complete flag · graphs/agent.py
  search_values disclosed limit.
- Census FALSE POSITIVE dismissed: "6 modules don't parse / Py2 except" = PEP 758
  (valid, ruff-preferred on 3.14); suite imports them green.

## R-ADAPT brief (input-vs-task audit delivered 2026-07-29; lane spawns AFTER R-HYG merges, lead has table)
Verdicts: catalogue_semantics CLEAN (the reference) · dimension_conform near-CLEAN ·
graph_sql_generation output-schema CLEAN (input drifted) · all others DRIFTED ·
metric_induction.yaml DEAD (no builder; live induction = cockpit TS frame stage) → DELETE.
HIGH trims (mechanical, evidence in audit):
 1 dimension_alias: drop `distinct` from both candidate sides (processor.py:823-824,
   judge.py:255) — bijection premise restated as a number the prompt excludes. 3rd
   instance of the slicing pattern.
 2 metric_induction.yaml DELETE.
 3 validation_sql: delete `parameters` slot end-to-end (agent.py:437-439 + yaml:313-319)
   — ships only the tolerance the contract forbids applying.
 4 business_cycles: delete 5 never-rendered context fields (context.py:327,338,435,440,441).
 5 business_cycles: delete 3 orphan REQUIRED output fields (models.py:260-271) — prompt
   never asks; PhaseResult.outputs discarded at activity.py:250-253. (data_quality_
   observations = MEDIUM: also rides warnings→structlog.)
 6 enrichment: drop table_id/column_id/duckdb_path/row_count from tables_json
   (enriched_views_phase.py:1031,1039,1041,1042) — name-only output schema.
 7 enrichment: delete is_primary_fact / skip_reason / per-column reasoning
   (enrichment_models.py:78,82-87,40) — zero readers; skip_reason ordered then ignored.
 8 dimension_conform yaml:38 "record the shared family" → schema forbids, pipeline
   discards; reword to "Keep them apart."
 9 graph_sql_generation: drop constant expression/depends_on keys (agent.py:1713-1715);
   fix dead "constant step" description (yaml:356-359).
10 validation_induction yaml:29 "units" claim false — reword to metric-DAG units.
11 business_cycles yaml:21-22 orphan bullet + :37 retired "Business concepts" (DAT-769).
12 context_format.py:324-325 false sharing claim in the invariant comment (validation_sql
   does NOT read this document).
13 catalogue_semantics yaml:76-80: ADD missing derived_formula_confidence clause
   (required + consumed at derived_values.py:213).
MEDIUM (verify named check, then do or report): graph_yaml metric freight narrowing
 (check eval fixtures on rendered graph_yaml); resolved_params duplication; enrichment
 summary reader check (cockpit evidence); low-band collapse; prose under-advertisements.
DESIGN (lead nod given with table): format_served_context gains a for_grounding flag
 gating the 4 imperative blocks (search_values hint — a tool induction DOESN'T HAVE;
 grounding imperatives; conformed-dim compose instruction) — biggest single drift source.
LOW/SEMANTIC (report-only, lead triage): induction meanings-not-served vs yaml:81 clause;
 enrichment grain_preservation block superseded by measurement (keep as proposal-saver?);
 TableRole serving; repair-turn vocabulary asymmetry (induction.py:193-215).
Cross-cutting proposal for lead: a lint asserting every rendered input field is referenced
 in the prompt (3 confirmed instances of ship-the-excluded-statistic).

## Decisions (fork · criterion · call · evidence)
- R4 forks (2026-07-29): (a) graph_sql_generation.yaml 2-line fix IN-LANE (the false
  completeness claim IS the S1 defect; R-HYG overlap = temperature line only, owner
  resolves at integration). (b) og_grounding widening = RECOMMENDATION not lane work
  (R3 mid-implement against current shape; churn). (c) validator allow-list DO NOT
  WIDEN — accepted: additivity_resolver.served_relation + period_resolver would go
  silently wrong vs today's disclosed absence; DAT-811/812 invariant stands.
- R3 one-path-vs-two (2026-07-29): criterion = ADR-0024 d1 one home. CALL: loadConceptGraph
  becomes THE graph read serving agent AND Model UI (builder dies, pure formatters stay);
  evidence: og-view filters byte-identical to builder filters on all three surfaces; UI
  diff zero. Flags: og_concepts lacks description/indicators (PK-join projection — R4 may
  consider widening or decline); derives_from gains first cockpit reader (census WIRE);
  single-vertex MATCH spiked first, og_concepts plain-SQL fallback sanctioned.
- Slicing input drift CONFIRMED (lead probe 2026-07-29): v5 prompt forbids judging on
  cardinality/distribution yet tables_json ships 5 stats fields and the user prompt
  advertises them (DAT-879 shrank the task, input never re-derived). CALL: trim the 5
  disclaimed fields + fix the advertising sentence in R-HYG's FOLD-IN round (not
  mid-run); criterion-3 wording held pending relevance.py redundancy check. Systematic
  input-vs-task audit of the other 10 phases → closeout candidate.
- R2/J4 as-written vs re-point (2026-07-29): criterion = ADR-0024 d2 (capability diff
  must trace to DATA); tier-A orphan SQL has NO identity, offering month needs barred
  SQL-text inference + would hard-refuse on click (drill-sql.ts:171-176). CALL:
  re-point J4 at parts path w/ baseSql, target assertions verbatim; RETAIN green
  tier-A honest-absence pin with reason. Owner call, disclosed to lead.
- R2 wire identity (2026-07-29): criterion = ADR-0024 d1 (ids not names). CALL:
  snippetId per source on AnswerSource + DrillSource + both wires; model-chosen
  `name` stays display/formula token. J8 becomes the only identity-less source —
  its discrimination becomes real.
