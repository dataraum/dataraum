node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# Contracts cache — fetched openapi.yaml from dataraum-api by sync-contracts.
# The pinned SHA in contracts.lock + the generated src/api/types.ts are the
# committed artifacts; the cache itself is regenerated on every sync.
.contracts/

# Generated by scripts/build-workflow-bundle.ts (DAT-529): the pre-bundled
# JourneyWorkflow code string, emitted at build time and bundled into the
# server. Regenerated on every build — never committed.
src/worker/generated/
