# DAT-807 structured outputs across the board — owner ledger

Objective: every LLM agent in engine + cockpit obtains its structured result via Anthropic
structured outputs (`output_config.format`, `strict:true` for tools genuinely called) instead of
forced `tool_choice` + client-side parsing; the coercion/repair layer is deleted, not disabled.

Scorecard:
- Engine, one `detection-v1` run compared against the dumps already on disk
  (`output/detection-v1/prompts/`, keyed by `(label, prompt_hash)`):
  - malformation counters ZERO and structurally unreachable (code deleted):
    `stringified_tool_arg_coerced`, `stringified_tool_payload_coerced`,
    `tool_output_envelope_unwrapped`, `tool_output_validation_failed`, `*_repair` labels
  - completeness: per-call item counts vs the catalogue in the paired USER prompt, per label —
    not worse than the on-disk baseline
  - `calibration/test_cycles_e2e.py::test_cycle_recall` under `detection-v1` (sharpest single
    oracle: DAT-795's all-or-nothing collapse IS the envelope bug)
- Cockpit: not exercised by the eval. All 12 sites driven live — 6 changed, 6 already-native
  (shared adapter path).
- Verdict after ONE run: improved / worse / inconclusive. Lead decides next step.

Non-goals: dependency version changes; agent decomposition (DAT-823); new eval oracles beyond
reading the existing dumps; anything not the output mechanism.

e2e budget: 1 eval run granted 2026-07-20 · spent: 0

## NEXT ACTION
ESCALATED to lead: metrics/validations induction cannot be migrated as a mechanism change (see
Open forks). Awaiting ruling + a decision on one live call to validate the combined request shape.
Engine lane still running.

## State
- lanes: cockpit INTEGRATED-PENDING (4/6 sites, gates green, not yet merged to epic); engine OPEN
- scorecard: baseline on disk (233 calls, detection-v1); no post-change run yet
- substrate: worktree `epic/dat-807-structured-outputs` off `origin/main` @ 0d75da05b
- done: removed unused top-level `@anthropic-ai/sdk` from cockpit (0 imports; adapter loads its
  own nested 0.97.1)

## Open forks
- Does constrained decoding under-produce on the large batched extractors
  (`semantic_per_table`, `column_annotation`)? Pre-registered criterion: per-label item counts
  from the post-change dumps vs the on-disk baseline. Worse on both batched labels while the
  small-schema labels hold => mechanism is fine but batching is not, escalate (that is DAT-823
  territory, out of scope here). Reversals so far: 0.

## Parked
- `slicing_analysis` missing `model=` — IN SCOPE, same edit (lead ruling 2026-07-20)
- `@tanstack/ai*` pinned to `"latest"` — separate hardening ticket, NOT this lane
- ADR-0004 states "every call forces a tool for typed output" — superseded by this slice,
  needs an ADR entry at close

---   (history below: append-only)

## Substrate map

Engine — 8 agent classes / 9 labels, ALL Claude Sonnet 5, ALL `max_tokens` 24000, ALL through one
chokepoint `AnthropicProvider.converse` (`llm/providers/anthropic.py:301`).

| label | today | schema | repair |
|---|---|---|---|
| `semantic_per_table` | forced `analyze_tables` | `TableSynthesisOutput` (semantic/models.py:374) | yes |
| `column_annotation` | forced `annotate_columns` | `ColumnAnnotationOutput` (semantic/models.py:422) | no |
| `slicing_analysis` | forced `analyze_slicing` | `SlicingAnalysisOutput` (slicing/models.py:129) | no |
| `business_cycles` | forced `submit_analysis` | `BusinessCycleAnalysisOutput` (cycles/models.py:184) | no — degrades to raw dict |
| `enrichment_analysis` | forced `analyze_enrichment` | `EnrichmentAnalysisOutput` (views/enrichment_models.py:87) | no |
| `dimension_conform` | forced `judge_exposures` | `ConformBatchOutput` (hierarchies/judge.py:74) | yes |
| `dimension_alias` | forced `judge_aliases` | `AliasIdentityBatchOutput` (hierarchies/judge.py:110) | yes |
| `validation_sql` | forced tool, `strict:true` | `ValidationSQLOutput` (validation/models.py:68) | no |
| `graph_sql_generation` | `auto` + 2 tools, thinking ON | `ExtractGroundingOutput` (graphs/models.py:469) | yes (schema + contract) |

Key provider facts (verified, not assumed):
- thinking is disabled EXPLICITLY by model prefix (`anthropic.py:370-374`,
  `_THINKING_DEFAULT_ON_PREFIXES` :81), NOT by the forced tool_choice. Removing the forced tool
  does not enable thinking. Same for temperature omission (`_rejects_temperature` :88).
- `effort` already builds `kwargs["output_config"]` (:405-406) — `format` merges into that same
  object.
- `_strict_tool_schema` (:117-133) already does exactly the normalisation constrained decoding
  needs: recursive `additionalProperties:false` + strips `_STRICT_UNSUPPORTED_KEYS` (:102-114).
  Reusable as-is.
- Provider RAISES `PermanentProviderError` on thinking + forced tool_choice (:320-326).
- Coercion runs unconditionally on every tool call: `_coerce_stringified_args` (:155-206),
  envelope-unwrap (:509-539). Repair: `llm/tool_repair.py`.
- Every call dumped with `stop_reason` + tool JSON via `prompt_log.py` (:583-603), gated by
  `Settings.prompt_dump_dir`; eval sets `PROMPT_DUMP_DIR` per strategy
  (`calibration/runner.py:262-268`).

Cockpit — 12 sites, 6 ALREADY on native structured output.
- Already `chat({outputSchema})`, no change: `why_column`, `why_relationship`, `why_table`,
  `nav-agent`, `report-summary-agent`, and `grounding-teach` (combined `chat({tools, outputSchema})`
  — the reference shape).
- Forced tool, no schema blocker: `induceConcepts` (ProposedConcept), `induceCycles`
  (ProposedCycle), chart author (ChartConfigSchema — designed for direct validation),
  answer sub-agent (QueryDraftSchema, real tool loop => combined shape).
- Forced tool, BLOCKED by `z.record`: `induceValidations` (`parameters`, validation-spec.ts:95),
  `induceMetrics` (`dependencies` + `parameters`, metric-spec.ts:199-212). `z.unknown()` values
  also need a real type. Reshape to arrays; touches persistence + consumers.
- Adapter `@tanstack/ai-anthropic@0.16.1` already emits `output_config.format`
  (`dist/esm/adapters/text.js:263-272`) and supports combined tools+schema for Claude 4.5+
  (:296-299). No version change needed.
- `toolArgsGuardMiddleware` (lib/tool-args-guard.ts) is the cockpit's coercion mirror; attached
  only to forced-tool sites, explicitly NOT to `outputSchema` sites.

Evidence the mechanism is the cause (from the on-disk dumps + handoff.md):
- `stop_reason` across ALL captured runs: `tool_use` 687, `end_turn` 2, `max_tokens` **0**.
  Nothing is truncated. Failures are malformation at the tool-argument boundary.
- handoff.md documents the envelope-wrap: Sonnet 5 under forced `tool_choice` wraps the whole
  argument object under one paraphrased key, "silently zeroed every field for a
  sampling-dependent fraction of runs" — that is DAT-795's 4/4-or-0/4 collapse.
- run #5: `dimension_conform` returned `{"pair_ref_verdicts": []}` — empty tool call.

## Plan
1. Engine lane — chokepoint + 8 call sites + delete coercion/repair. (Opus 4.8 · high)
2. Cockpit lane A — the 4 unblocked sites. (Sonnet 5 · high)
3. Cockpit lane B — the 2 `z.record` reshapes. Only after lane A proves the mechanism.
4. One eval run + cockpit live pass over all 12 sites.

## Decisions
- fork: is truncation a contributor? · criterion: `stop_reason` distribution in the on-disk dumps
  · call: NO — zero `max_tokens` across 689 captured calls · evidence: dump scan 2026-07-20.
  Lead had said so before the data confirmed it.
- fork: does removing the forced tool silently enable thinking? · criterion: read the provider's
  thinking branch · call: NO — disabled explicitly by model prefix, independent of tool_choice
  · evidence: `anthropic.py:370-374` + `:81-85`.
- ruling (lead, 2026-07-20): **an optional field is usually a modelling mistake.** Per optional
  field, three dispositions, applied in the pre-flight before any migration code: nothing consumes
  it -> CUT; something consumes it -> REQUIRED with documented "" / [] sentinel; genuinely
  either/or -> both required with sentinel, NEVER a union (unions carry their own 16-per-request
  cap). Relayed to both lanes. Rationale: Pydantic/Zod render optionals as `anyOf` with null, so
  an optional plausibly spends from BOTH the 24-optional and 16-union budgets — cutting removes
  the pressure at the source instead of engineering around it. Out of scope, noted for later:
  optional free-text fields as a way to induce reasoning.
- fork: is `extract`/`formula`/`constant` a retired design? · criterion: engine dispatch sites +
  shipped YAML usage · call: NO, all three live — `agent.py:600` dispatches CONSTANT,
  `compose_formula_sql`/`compose_constant_sql` compose them, shipped YAML histogram is 44 extract /
  24 formula / 4 constant. Nuance the lead remembered correctly: DAT-643 made EXTRACT the SOLE LLM
  authoring surface (`agent.py:721`) — FORMULA/CONSTANT SQL is composed deterministically. The DAG
  still DECLARES them; only the SQL authoring moved. Strengthens the discriminated-union plan:
  each variant requires exactly what `compose_*_sql` reads.
- decision: `GraphStep.validations` (StepValidation, DAT-616) is MISSING from the cockpit mirror,
  so no induced/taught metric can declare a post-execution check while shipped ones can
  (`verifier.py:117` enforces them; dpo/dso/dio/current_ratio declare them). Consumed => disposition
  2: add as REQUIRED with documented `[]`. Zero cap cost (3 required subfields, no optionals, no
  unions). Induction prompt must ask for a check on the output step, else the field stays empty
  forever. IN SCOPE — the metrics lane rewrites this exact contract.
- naming: `tool_repair.py` holds a SQL-provenance contract check, not a JSON repair — it parses the
  emitted SQL with DuckDB and verifies the declared `column_mappings_basis` against the served
  relation schema (`graphs/grounding_validation.py:53-110`). Once `repair_tool_output` is deleted
  the module is only that, so it gets renamed in the same edit.

## Eval history
(none yet)

## Checkpoint log
(none yet)
