# DAT-855 slice B2 — dimension facet + coverage map — owner ledger

Objective: the typed `dimension` facet lands on the ontology surface end-to-end, and
/operating-model gains a coverage-map lens that is honest about dark — computed entirely
from workspace-native facts (eval is the gate on the map, never a runtime input).

Scorecard (source: spec conversation 2026-07-30 + dataraum-eval):
- S1 Every shipped finance concept + metric carries a positive facet (zero NULL),
  CHECK-enforced, projected into og, mirrored in cockpit schema.
- S2 Rendered map on clean corpus matches a hand-verified expected state; DAT-893 fix
  restores DSO/DPO/DIO/CCC/current_ratio on snapshot-view groundings in a
  calendar-undeclared workspace (= fresh-workspace default).
- S3 Dark/partial cells carry typed reasons — absent-concept (non-revisable, no churn),
  ungroundable-dimension (names what to link), unresolvable-reporting-instant (names
  "declare a fiscal calendar"); verifier.py:88 three-way conflation gone.
- S4 DAT-620 acceptance incl. no-false-abstention once the resolving table is linked.
- S5 DAT-724: entity_type recall/precision graded on reference corpus; journal_entries
  + fx_rates unambiguous.
- S6 Deterministic gates green at EVERY integration; judged screenshots of the lens in
  the PR.

Non-goals: facet derivation for framed verticals (B3/DAT-738); priors into frame; no
run-per-dimension (facet is a coordinate on catalog-grain artifacts; run economy =
DAT-861); no allocation; no new route; no forecast anything.

e2e budget: NONE granted — ask per run. Eval cadence per DAT-855: deterministic tiers per
integration; full real-LLM calibration only at slice boundary on explicit lead go (the
post-DAT-893-fix boundary re-run needs one — request at that checkpoint).

## NEXT ACTION
Grounding fan-out running (4 Explore agents: calendar/binder path, verifier/failure
typing, ontology+config surface, cockpit lens substrate). On return: write substrate map,
finalize lane briefs, open lane 2a (DAT-893) first — it is the scorecard's S2 gate and
smallest risk of conflict with lane 1.

## State
Lanes: none open. Scorecard: all red (baseline). Substrate: DAT-893 mechanism verified
in eval-stack Postgres (ws_29015abd sql_snippets = SELECT NULL, workspace_calendar 0
rows); eval containers left running on host.

## Open forks
(none)

## Parked
(none)

---

## Substrate map
(pending grounding fan-out — grounding shortcuts from the spec conversation, verified
2026-07-30:)
- og element inventory: storage/property_graph.py (og_grounding/og_grounded_by/
  og_additivity/og_temporal_coverage all exist).
- concepts table + `ordering` NULL-until-a-writer precedent: analysis/semantic/
  db_models.py:186. Metrics table: graphs/metric_graph_db_models.py:129 (`category` is
  finance-shaped — do not overload).
- OntologyDefinition: analysis/semantic/ontology.py:81 (post-DAT-883 typed envelope).
- Eval dimension_status(): ../dataraum-eval/calibration/results_store.py:508 — the
  lit/partial/dark gate off the verdict store, already live.
- Facet vocabulary: ../dataraum-eval/calibration/dimension_priors.yaml (demand/offer/
  capital lowercase; six-value enum + cross_cutting is the lead-locked shape).
- DAT-893 evidence: prompt artifact output/clean-sweep-46/prompts/graph_sql_generation/
  cash_conversion_cycle.4ceed2ba650f5a46.txt:825 ("not declared — the calendar-year
  default is assumed"); smoke ws_00000000 drew enriched_journal_lines groundings
  (exposure is draw-dependent).

## Plan
Lane sequence (each independently mergeable into epic branch):
- 2a DAT-893 calendar serving/binder agreement — FIRST (S2 gate; smallest surface).
- 1  Facet end-to-end (YAML → columns+CHECKs → og projection → finance hand-authoring →
     schema dump + cockpit db:pull). Settles the OntologyDefinition parse surface.
- 2b DAT-658 typed absent-concept abstention (DAT-893's lost-reason case = first
     fixture) → 2c DAT-620 ungroundable-dimension verdict.
- 3  Coverage-map read + coverage-map-view.tsx third lens on /operating-model.
- 4  DAT-724 entities: taxonomy — ONLY after lane 1 merges.
- 5  Eval follow (../dataraum-eval): write-surface pins, map-honesty oracle,
     absence-regression oracle. Coordinates with slice-boundary live run.

## Lane briefs
(to be written per lane at spawn; tier plan: 2a Opus·high (cross-module engine, gates
deterministic) · 1 Sonnet·high (mechanical + hand-authoring reviewed by Fable reviewer)
— facet CONTENT (which concept → which dimension) is owner-authored, not lane-guessed ·
2b/2c Fable·xhigh (abstention semantics the eval grades) · 3 Sonnet·high UI + Fable
design review · 4 Opus·high · 5 Sonnet·high.)

## Eval history
(none yet)

## Decisions
(spec-level locks recorded in header; none taken in-slice yet)

## Checkpoint log
(none yet)
