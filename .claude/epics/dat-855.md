# DAT-855 slice B2 — dimension facet + coverage map — owner ledger

Objective: the typed `dimension` facet lands on the ontology surface end-to-end, and
/operating-model gains a coverage-map lens that is honest about dark — computed entirely
from workspace-native facts (eval is the gate on the map, never a runtime input).

Scorecard (source: spec conversation 2026-07-30 + dataraum-eval):
- S1 Every shipped finance concept + metric carries a positive facet (zero NULL),
  CHECK-enforced, projected into og, mirrored in cockpit schema.
- S2 Rendered map on clean corpus matches a hand-verified expected state; DAT-893 fix
  restores DSO/DPO/DIO/CCC/current_ratio on snapshot-view groundings in a
  calendar-undeclared workspace (= fresh-workspace default).
- S3 Dark/partial cells carry typed reasons — absent-concept (non-revisable, no churn),
  ungroundable-dimension (names what to link), unresolvable-reporting-instant (names
  "declare a fiscal calendar"); verifier.py:88 three-way conflation gone.
- S4 DAT-620 acceptance incl. no-false-abstention once the resolving table is linked.
- S5 DAT-724: entity_type recall/precision graded on reference corpus; journal_entries
  + fx_rates unambiguous.
- S6 Deterministic gates green at EVERY integration; judged screenshots of the lens in
  the PR.

Non-goals: facet derivation for framed verticals (B3/DAT-738); priors into frame; no
run-per-dimension (facet is a coordinate on catalog-grain artifacts; run economy =
DAT-861); no allocation; no new route; no forecast anything.

e2e budget: ONE run granted (lead, 2026-07-30): clean + clean-flat AT SLICE CLOSE — not
mid-flight. Collect the validation list as lanes land (see "Boundary-run validation
list"). Everything else stays deterministic per integration.

## NEXT ACTION
Lane 3 INTEGRATED (fix round addressed all 5 findings; merged; cockpit gates green on
epic branch — biome 0 warnings, 2347 tests, build OK; worktree removed). VISUAL-PASS
SEQUENCING DECISION (owner): the dev stack's ws schema predates the facet columns and
recreating a real workspace = an ungranted LLM run — so the S6 judged screenshots
happen AT SLICE CLOSE, right after the granted calibration run refreshes a real
workspace on the new schema; an optional seeded-fixture design pass before that only
if time allows. S6 stays open until then.
Lane 2b INTEGRATED (3-fix round done, FIX-1 regression test falsified non-vacuous;
merged; 3513 engine tests + ruff + mypy green on epic branch; worktree removed).
S3 deterministic half GREEN (typed evidence-backed classes, verifier names the cause,
concept_absent churn stop test-pinned end-to-end); outcome check = close-run item 5.
Lane 2c (DAT-620, Fable) SPAWNED off post-2b epic head — fifth NoSupportClass
integration; STOP-and-ask points: numeric opacity threshold, validator conflicts.
Lane 4 DONE (feat/dat-724-entity-taxonomy, rebased onto epic, 3548 tests green).
OWNER CONTENT APPROVAL 2026-07-30: the 15-kind finance entities table APPROVED as
proposed (journal_entries=fact factless-event; fx_rates=dimension tiebreaker; honest
gaps customer/payment accepted). Deviations accepted: two-seam serving (DAT-823 split
— brief's single-seam assumption was stale), vertical_entities name, seed-only source
CHECK. Senior (Fable) PASS-with-notes — 3-item refinement round sent (phase-neutral
empty sentinel, intro into the formatter's non-empty branch, docstring register); spec
(opus) verdict pending. OWNER JOB AT 4's MERGE: run cockpit db:pull:metadata (new
vertical_entities table → schema-drift CI job trips otherwise) + write-surface check.
NOTE for lane 5 + close: local ../dataraum-testdata checkout is pre-DAT-884 (8-table)
— the eval repo's OWN vendored pin is post-884, so the roles oracle grades fine; don't
let anything resolve the sibling checkout. Close-run validation list += item 9:
entity_type/table_role follow the declared taxonomy on the live run (journal_entries
fact, fx_rates dimension) — the S5 outcome check + the two reworked prompts' behavior.
Lane 5 last.
Lane 4 (DAT-724, opus) spawning NOW off post-lane-3 epic head (surface disjoint from
2b). Lane 5 (eval follow) last. Calibration: SLICE CLOSE.
Boundary-run validation list += item 7: prefix-attribution false-lit risk (watch a
shared-extract failure attributing away from a consumer on the live corpus) + item 8:
the coverage lens on the refreshed workspace matches the S2 expected map state
(screenshots for the PR taken then).

## Boundary-run validation list (grows as lanes land; executed once at slice close)
1. DAT-893: no SELECT NULL cluster on balance-sheet extracts — DSO/DPO/DIO/CCC/
   current_ratio compute on snapshot-view groundings; AR back to ~exact vs truth.
2. DAT-893 flow side: days_in_period MEASURED (~364), not 30-by-fallback — DSO/DPO/DIO/
   CCC values shift accordingly; 3-seed clean bands re-baselined against the corrected
   pipeline (scripts/sweep_clean_seeds.py / build_clean_bands.py flow).
3. DAT-891 (merged in PR #540, closed): test_cycle_recall green on accounts_payable —
   both settlement directions persisted. Outcome confirmation was deferred to "next
   pass"; this is it.
4. S2 map state on clean: capital lit · offer lit · demand dark("no demand unit metric
   declared") · supply/capacity/throughput dark("no carriers") — matches the rendered
   lens AND eval dimension_status() off the store (map-honesty oracle, lane 5).
5. DAT-658 typed reasons (once 2b lands): no grounding-rate regression; retained
   failures carry typed classes; no churn (a non-revisable retained row does not
   trigger a re-author on the second strategy's run).
6. Eval absence-regression oracle (lane 5): a metric going graded-value → no-value vs
   the prior pass must RED, not skip (DAT-893 adjacent finding #2).

## State (contd)
Lane 2a INTEGRATED (merge on epic branch; both reviewers green — senior's one finding
[composition_abstain leaked onto non-VERIFIER_REJECTED modes] fixed as owner fold-in
199b33ed8 + cross-mode test; post-merge 3480 engine tests, ruff/mypy clean; cockpit
untouched by 2a — lane 1's cockpit gates stand; schema.sql unchanged, schema_graph.sql
merged clean). DAT-893 Jira updated with the widened flow-side consequence; ticket stays
open until the integration PR lands on main. S2's engine half DONE — the render half
rides lane 3; outcome-level proof rides the calibration run.
DISCOVERY (recorded, escalated at checkpoint 2): anchor mis-scoping also degraded
witness-classified FLOWS — days_in_period was 30-by-fallback; metric VALUES change.

## State
Lanes: 1 INTEGRATED (merge a8732f0b8; both reviewers PASS; post-merge gates green —
engine 3467 passed / ruff / format / mypy clean; cockpit biome (2 pre-existing warns in
operating-model.test.tsx, on base) / tsc / 2308 tests / build). Lane worktree+branch
removed. 2a open (background). 3 spawning.
SCORECARD: S1 GREEN (zero-NULL facets CHECK-enforced + og-projected + cockpit-mirrored,
test-pinned). S2-S6 red. Checkpoint 1 → lead + Jira 2026-07-30.
DAT-893 mechanism CORRECTED (see Decisions). Eval containers running on host.

## Open forks
(none — DAT-893 fork resolved by evidence, see Decisions)

## Parked
- frame.ts facet wiring: frame-authored concepts get NULL facet until B3/DAT-738 wires
  ProposedConcept + writeConcept — CONFIRMED deliberate (owner, 2026-07-30; senior
  review improvement #1). Belongs to strand 2's lane, not this slice.
- Cockpit round-trip test for writeConcept facet (senior review improvement #2) — no
  live caller until frame wires it; fold into the B3 lane.
- concept-graph-load.ts:32-34 stale comment (cites retired _read_grounding_provenance;
  cockpit still does the old provenance-JSON second read the engine retired, ADR-0024 d1)
  — align when lane 3 touches that file's neighborhood.
- why-detail.tsx:207 uses toLocaleString() (SSR hydration anti-pattern) — fix rides
  whichever lane next touches that widget; lens lane must use the run-row.ts UTC pattern.
- search_values high-cardinality gap: a model can search, get "no values matching", and
  still author SQL — the un-screened half of DAT-658's class (agent B §3). In 2b scope.
- operating-model.test.tsx 2 pre-existing biome warnings — FIXED by lane 3.
- Structural no_support_members on FailedSnippetProvenance (senior review of 2b,
  Improvement #2): would let the save path clear a concept_absent sticky carry
  deterministically when a member value returns to the served set, and close the
  near-constant-column edge (those columns are in _served_value_sets but excluded
  from the prompt's Value sets block, so the model cannot check the escape hatch).
  Parked: current prose-evidence + escape hatch is honest; extraction candidate.
- CONCEPT_ABSENT evidence embeds the full enumeration (growth vector on wide-but-
  complete value sets, ~50 values into state_reason/prompt) — noted, no cap (house
  rule: no speculative caps); revisit only on a named failure.

---

## Substrate map
(pending grounding fan-out — grounding shortcuts from the spec conversation, verified
2026-07-30:)
- og element inventory: storage/property_graph.py (og_grounding/og_grounded_by/
  og_additivity/og_temporal_coverage all exist).
- concepts table + `ordering` NULL-until-a-writer precedent: analysis/semantic/
  db_models.py:186. Metrics table: graphs/metric_graph_db_models.py:129 (`category` is
  finance-shaped — do not overload).
- OntologyDefinition: analysis/semantic/ontology.py:81 (post-DAT-883 typed envelope).
- Eval dimension_status(): ../dataraum-eval/calibration/results_store.py:508 — the
  lit/partial/dark gate off the verdict store, already live.
- Facet vocabulary: ../dataraum-eval/calibration/dimension_priors.yaml (demand/offer/
  capital lowercase; six-value enum + cross_cutting is the lead-locked shape).
- DAT-893 evidence: prompt artifact output/clean-sweep-46/prompts/graph_sql_generation/
  cash_conversion_cycle.4ceed2ba650f5a46.txt:825 ("not declared — the calendar-year
  default is assumed"); smoke ws_00000000 drew enriched_journal_lines groundings
  (exposure is draw-dependent).

## Substrate map — lane 1 (agent C, 2026-07-30)
Field-path recipe = copy the `ordering` precedent end-to-end:
- Enum + `_VALUES` tuple + CHECK + column: analysis/semantic/db_models.py:76-97/134/226-231/248.
- YAML parse: `OntologyConcept` ontology.py:25-42 (facet is concept-level, same as ordering).
- Seed lift + born-loud validation: concept_store.py:45/116-174(:145-149,:159); reverse lift :222.
- og_concepts view property_graph.py:490-500 + PGQ PROPERTIES :1281-1282.
- read_views: NO edit (concepts+metrics are `SELECT *` vertical-scoped, :165-183,:414-441).
- schema dump: `python -m dataraum.storage.dump_ddl schema.sql` (3 files); CI schema-drift job pins it.
- Cockpit: generated schema.ts (concepts :43-57, metrics :800-816) via db:pull:metadata
  (scripts/pull-metadata.sh — runs the engine dump itself); HAND-WRITTEN: write-surface.ts:65-81
  conceptsWrite (+ column, match ordering :77), tools/concept-write.ts (:38-39,:44-54,:94 —
  vocab array + input + pass-through). NO metric write surface exists (source='seed' only).
  write-surface.test.ts auto-pins columns vs schema.sql (order: engine dump BEFORE cockpit col).
- Metrics: loader = core/vertical_loader.py (Family.METRICS); SEED source = metric_store.py
  :82-100 shipped_base; row lift `_metric_row` :177-200 — facet reads off raw
  defn["metadata"]["dimension_facet"] (sibling to category, current_ratio.yaml:6 precedent;
  description/output/dependencies already read raw per DAT-882). Metric YAML parser silently
  ignores unknown fields (no extra=forbid on the definition path) — the born-loud validation
  for facet values therefore lives in metric_store (engine) exactly like concept_store's.
  og_metrics view :1132-1145 + PGQ :1292-1293. Metric model: metric_graph_db_models.py:129-172
  (CHECK pattern :93-98/:142-147).
- DAT-724 adjacency: entities: block = NEW OntologyEntity model + fifth list on
  OntologyDefinition (ontology.py:81-95) + likely entity_store; disjoint from facet's
  classes/tables/vertices — sequencing (facet first) avoids double schema.sql regen only.

## Facet authoring (OWNER content — lanes mechanize, never reassign)
Principle: a dimension facet only where the concept/metric is a genuine unit-economics
carrier of that dimension; ledger mechanics and whole-company aggregates are cross_cutting
(a positive classification, not a fallback). Cross-facet links ride edges, never facets.
Concepts (22): revenue→demand · cost_of_goods_sold→offer · gross-side none else ·
accounts_receivable→capital · accounts_payable→capital · inventory→capital ·
current_assets→capital · current_liabilities→capital · cash→capital · equity→capital ·
interest→capital · operating_expense→cross_cutting · depreciation→cross_cutting ·
tax→cross_cutting · fiscal_period→cross_cutting · entity→cross_cutting ·
account→cross_cutting · currency→cross_cutting · open_balance→cross_cutting ·
account_balance→cross_cutting · transaction_amount→cross_cutting · debit→cross_cutting ·
credit→cross_cutting.
Metrics (16): dso→capital · dpo→capital · dio→capital · cash_conversion_cycle→capital ·
current_ratio→capital · gross_profit→offer · gross_margin→offer · ebitda→cross_cutting ·
ebitda_margin→cross_cutting · net_income→cross_cutting · net_margin→cross_cutting ·
operating_income→cross_cutting · operating_margin→cross_cutting ·
active_accounts→cross_cutting · average_transaction_value→cross_cutting ·
transaction_count→cross_cutting.
Expected map on clean (S2 hand-verified target, post-DAT-893-fix): capital=lit (dso/dpo/
dio compute + hold; inventory has 4.45M since DAT-884) · offer=lit (gross_margin executes
+ holds) · demand=dark with reason "no demand unit metric declared" (revenue grounds as a
concept but no shipped metric carries facet=demand — honest per RFC; DB1/rev-per-customer
is B5) · supply/capacity/throughput=dark (no carriers) · cross_cutting excluded from the
map rows (classification, not a dimension).

## Plan
Lane sequence (each independently mergeable into epic branch):
- 2a DAT-893 calendar serving/binder agreement — FIRST (S2 gate; smallest surface).
- 1  Facet end-to-end (YAML → columns+CHECKs → og projection → finance hand-authoring →
     schema dump + cockpit db:pull). Settles the OntologyDefinition parse surface.
- 2b DAT-658 typed absent-concept abstention (DAT-893's lost-reason case = first
     fixture) → 2c DAT-620 ungroundable-dimension verdict.
- 3  Coverage-map read + coverage-map-view.tsx third lens on /operating-model.
- 4  DAT-724 entities: taxonomy — ONLY after lane 1 merges.
- 5  Eval follow (../dataraum-eval): write-surface pins, map-honesty oracle,
     absence-regression oracle. Coordinates with slice-boundary live run.

## Lane briefs
(tier plan: 2a Opus·high · 1 Sonnet·high · 2b/2c Fable·xhigh · 3 Sonnet·high UI +
Fable design review · 4 Opus·high · 5 Sonnet·high. Subagent nesting is OFF — lanes do
NOT spawn reviewers; the owner runs both reviewers at integration.)

### Lane 1 — dimension_facet end-to-end (SPAWNED 2026-07-30, sonnet·high)
Goal: the `dimension_facet` field lands per the substrate-map recipe, with the facet
assignments EXACTLY as in "Facet authoring" above (verbatim — the lane never reassigns
or invents a facet; a disagreement is a question back to the owner, not a change).
Surface: analysis/semantic/db_models.py + ontology.py + concept_store.py ·
graphs/metric_graph_db_models.py + metric_store.py · storage/property_graph.py
(og_concepts/og_metrics SELECT + PROPERTIES) · packages/dataraum-config/verticals/
finance/ontology.yaml + metrics/**/*.yaml · engine schema dump (3 files) · cockpit
db:pull:metadata + write-surface.ts conceptsWrite + tools/concept-write.ts · tests.
Constraints: ONE enum home (7 values: demand, offer, supply, capacity, throughput,
capital, cross_cutting; lowercase), enum-derived CHECK on BOTH tables (NULL-or-IN — NULL
means only "no writer classified yet"); born-loud validation in BOTH stores (metric YAML
parser ignores unknown fields, so metric_store validates); zero NULL on shipped config;
do NOT touch metrics.category, read_views.py, verifier/composition/calendar code, the
operating-model UI, or ../dataraum-eval. Regen order: engine dump → cockpit pull →
write-surface column → write-surface.test green.
Gates: engine ruff+format+mypy+full unit/integration; cockpit check+typecheck+test+build.
No reviewer spawning (nesting off) — owner reviews at integration.

### Lane 3 — coverage-map lens (SPAWNED 2026-07-30, sonnet·high,
Fable design review at integration)
Goal: /operating-model gains the third lens: per-dimension lit/partial/dark computed
from workspace-native facts, grouped by dimension_facet, dark/partial cells carrying
reasons (S2 render half + S6). Template = the DAT-740 bus-matrix lane VERBATIM (see
lane-3 substrate map): tools/coverage-map.ts (pure + tested) + coverage-map-load.ts
(server-only) + ui/.../coverage-map-view.tsx (Table grid) + operating-model.tsx wiring
(ViewMode + validateSearch + allSettled loader + SegmentedControl + always-mounted pane
data-testid="pane-coverage" + CoverageView wrapper w/ own PaneError copy) + server fn in
operating-model.functions.ts.
Semantics (lead-locked): lit = ≥1 metric of the facet grounds + executes + holds its own
declared expectations + no born-loud flag (+ reconciliation agrees where a second angle
exists). partial = grounds but not-executed/abstained/expectation-violated. dark =
nothing grounds, WITH reason (absent-concept / ungroundable / unresolvable-instant come
from lane 2b later — v1 renders the failure_reason text available on current_groundings
provenance + lifecycle state_reason; typed reasons upgrade when 2b lands). cross_cutting
is NOT a map row; facet=NULL concepts render as an "unclassified" badge count, never a
row. Six dimension rows always present (supply/capacity/throughput = dark "no carriers").
Reads (all mirrored already; facet arrives with lane 1): metrics+concepts (facet),
sqlSnippets/og_grounding provenance, currentLifecycleArtifacts state/state_reason,
current_concept_reconciliation, current_groundings (resolvedPeriod/calendarSource).
Badge: NEW three-state vocabulary as a SIBLING badge in band-badge.tsx module (one home;
do NOT overload ready/investigate/blocked or reuse coverage 'partial' meaning).
Dates: run-row.ts:45-54 UTC formatter pattern; NEVER toLocaleString inline.
Tests: pure-builder test (bus-matrix.test.ts shape) + loader integration test
(attachFixtureWorkspace + seed-catalog.ts; head-gating: assert REASONS not presence) +
operating-model.test.tsx it.each entry + failure-isolation case + view test from real
builder output. Screenshot-judged pass happens at owner integration (browser), not in
the lane.
DO NOT TOUCH: engine (any), config YAMLs, other lanes' files, ../dataraum-eval.
Gates: cockpit check+typecheck+test+build.

### Lane 2b — DAT-658 typed grounding-failure reasons (SPAWNED 2026-07-30, fable·xhigh)
Goal: S3's core — the verifier.py:88 three-way conflation is replaced by typed,
evidence-backed failure classes; absent-concept is non-revisable and the feedback loop
stops churning on it. Substrate: "Substrate map — lanes 2b/2c" + 2a's landed changes
(_build_prior_context now has an abstained branch; composition_abstain on provenance,
gated VERIFIER_REJECTED).
Design center (from agent B's map): the evidence to classify never converges today —
served_values die in _generate_sql, parts die after save, no row count is ever captured.
Classification therefore happens in agent.py between execution and failure-save, where
context + generated_code/cached_snippets are BOTH in scope. Evidence to gather there:
(a) a COUNT(*) disambiguation query composed from the extract's parts (relation + where)
— zero rows vs rows-with-NULL-operand; (b) the served value-set intersection for the
concept's filter columns — no family value in a COMPLETE served set = absent-concept
(non-revisable); note the search_values high-cardinality gap (no complete set → class
stays revisable, honestly). Vocabulary: extend the FailedSnippetProvenance shape with a
typed reason class following the additivity precedent (StrEnum + pydantic pairing
invariant — status/reason discipline; DB CHECK n/a, provenance is JSON — the pydantic
model IS the chokepoint). verifier.py's message may then name the class it received
rather than enumerating possibility space. Retention: a non-revisable class
short-circuits _build_prior_context's "Revise" into "do not re-author; <reason>" (its
own branch beside abstained/DISJOINT_COLLISION). metrics_phase.py:354's verbatim
state_reason follows the typed class.
Also in scope (small): the flow-side mismatch-message unit test the spec reviewer noted
missing on 2a's period_resolver split.
DO NOT TOUCH: DAT-620's verdict (lane 2c), facet files, boundary_resolver/period_resolver
beyond the named test, cockpit, ../dataraum-eval, prompt YAMLs (guidance strings in
agent.py are code and in scope).
Gates: engine ruff+format+mypy+FULL unit/integration. No reviewer spawning.

### Lane 2a — DAT-893 anchor-axis fix (SPAWNED 2026-07-30, opus·high)
Goal: DSO/DPO/DIO/CCC/current_ratio compute again on snapshot-view groundings in a
calendar-undeclared workspace (S2), and the composition abstain reason survives into
failed-snippet provenance (S3 seed). Scope per the corrected DAT-893 (Jira comment
2026-07-30) and the lane-2a substrate map.
Three parts: (1) find the writer of og_columns.anchor_time_axis and make a stock
measure's recorded anchor resolve on the measure's OWN table (balance_sheet.
ending_balance → period, not entry_id__date); (2) defensive typed abstain in
boundary_resolver._resolve when the anchor axis is not on the served relation — message
names the mismatch (axis, relation), never the generic text; (3) thread
compose_period_binding's abstain reason into the failed-snippet provenance so the
persisted row names the true cause (extend FailedSnippetProvenance.failure_reason
content or add a typed field — smallest honest cut; do NOT build lane 2b's full
vocabulary).
Constraints: engine only; do NOT touch verifier.py's message taxonomy (lane 2b), prompt
YAMLs, facet files (lane 1), or ../dataraum-eval. Deterministic tests only (unit +
integration incl. an end-to-end: undeclared calendar + snapshot relation + stock
measure whose witness saw event-lineage evidence → binds on the relation's own axis and
computes; and the mismatch case → typed reason persisted).
Gates: engine ruff+format+mypy+FULL unit/integration. No reviewer spawning.

## Substrate map — lane 2a (agent A + owner DB probe, 2026-07-30)
DAT-893 CORRECTED mechanism: og_columns.anchor_time_axis for balance_sheet.ending_balance
= 'entry_id__date' (a journal-lines column, off-relation). Binder (boundary_resolver.py)
_resolve:280-334 / _bind_to_close:337-385 abstains when the stock anchor can't resolve on
the served relation; abstain → agent.py:1093-1119 relation=None/select NULL →
formula_composer.py:54-74 renders SELECT NULL AS value; abstain reason rides ONLY as a
GraphAssumptionOutput dimension="period.binding" conf 0.2 (boundary_resolver.py:519-539)
and is DROPPED on the failure path (FailedSnippetProvenance = verifier text only).
Calendar is NOT the mechanism: og_period_grain (property_graph.py:853-873) COALESCEs
empty workspace_calendar → default; binder adopts it (no calendar_source branch);
integration test test_undeclared_calendar_binds_the_stamped_default_visibly proves it.
Serving: context_format.py:359-391; single binding call site agent.py:1077-1092 — both
read ONE shared calendar object. workspace_calendar: zero writers BY DESIGN (declared,
never detected; cockpit authoring path future). Anchor writer to find: the lineage/
witness path that populates og_columns.anchor_time_axis (DAT-847 W1-a suspect).
Test inventory: tests/unit/graphs/test_boundary_resolver.py (default-calendar fixture
_CALENDAR_YEAR line 35) + tests/integration/graphs/test_boundary_resolver.py.

## Substrate map — lanes 2b/2c (agent B, 2026-07-30)
verify_execution(graph, execution) is structurally BLIND — no context/conn/value-sets/
row-counts (verifier.py:56). Messages: :87-92 (the conflated no-support), :93-95, :99-102,
:112-134 (execute-and-flag). Failure write: agent.py:398-406 → _save_failed_snippet
:1758-1833 → FailedSnippetProvenance (models.py:583-594; SnippetFailureMode :503-528 —
the vocabulary to extend). metrics_phase.py:354 writes the raw verifier text VERBATIM as
lifecycle state_reason (stuck at grounded, no transition). Churn site: agent.py:2010-2124
_build_prior_context — DISJOINT_COLLISION gets targeted guidance (:2086-2101), everything
else the same generic "Revise…or abstain" (:2102-2111) forever. Evidence never converges:
served_values built in _generate_sql (:1014) and discarded; parts discarded after save
(assemble path: read cached_snippets, NOT generated_code); NO row count ever captured on
the scalar path (execution.py — total_count only in return_table mode). Classification
home: new step in agent.py between execution and failure-save/transition, where context +
parts are both in scope (widening verify_execution is the alternative). Pattern to copy:
additivity precedent (graphs/additivity.py:495-629 + additivity_db_models.py CHECK
status_verdict_reason) — StrEnum + __post_init__ pairing + mirrored DB CHECK, rows
written unconditionally. DAT-620 signals that exist: business_meaning score (1-conf),
_generate_column_flags (context_reads.py:1513-1552), high_entropy_dimensions
(:1560-1583), ColumnMeaning absence (field_mapping.py:33-48), og_references
(property_graph.py:553-581) for join-presence. Verdict home: metrics_phase alongside
_persist_additivity_verdicts (:362-371) + current_/og_ views + cockpit look/why-metric.

## Substrate map — lane 3 (agent D, 2026-07-30)
Template = DAT-740 bus-matrix lane VERBATIM (6 files ~1000 LOC): tools/coverage-map.ts
(pure, tested) + tools/coverage-map-load.ts (server-only, "tsc-bounded test-unexecuted")
+ ui/.../coverage-map-view.tsx (Table grid, not Accordion) + operating-model.tsx wiring:
ViewMode :70 + validateSearch :72-83 + loader Promise.allSettled :84-95 (NEVER
Promise.all) + SegmentedControl :250-254 + always-mounted pane w/ data-testid
"pane-coverage" (:279-288 mirror; owner ruling: panes never unmount) + CoverageView
wrapper w/ own PaneError copy. Server fn: operating-model.functions.ts mirror loadBus
:30-32. Data: all needed reads mirrored EXCEPT dimension_facet (arrives via lane 1 +
db:pull). current_groundings mirror carries resolvedPeriod/reportingWindowClose/
calendarSource (schema.ts:368). lit/partial/dark = NEW vocabulary — design call: sibling
badge in band-badge.tsx module (one home, not one component; don't blur with
ready/investigate/blocked). Tests: pure-builder test (mirror bus-matrix.test.ts 255
lines) + loader INTEGRATION test (attachFixtureWorkspace + seed-catalog.ts fixtures;
head-gating trap: row w/o snapshot-head entry is invisible — assert the REASON) + route
tab-wiring test operating-model.test.tsx (it.each + failure-isolation case) + view test
from REAL builder output. NO route-level journey harness (loader-level is right-sized).
Dates: copy run-row.ts:45-54 UTC formatter; never toLocaleString inline.

## Eval history
(none yet)

## Decisions
- 2026-07-30 · DAT-893 mechanism fork (calendar-split vs other-abstain-branch) · criterion:
  the code path + the workspace's own og_columns rows · CALL: anchor-axis mis-scoping
  (entry_id__date off-relation on balance_sheet.ending_balance), calendar exonerated ·
  evidence: agent A file:line map + ws_29015abd og_columns query + zero 'fiscal' hits in
  worker.log. Jira DAT-893 retitled + correcting comment. Zero reversals — the original
  ticket claim never drove code.
- 2026-07-30 · Lane 1 may run parallel to 2a (file-disjoint surfaces) · evidence: agent A
  (boundary_resolver/context_format/lineage) vs agent C (ontology/stores/property_graph
  og_concepts+og_metrics SELECT lists only) — no shared files.

## Checkpoint log
(none yet)
