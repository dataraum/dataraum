# DAT-725 graph-context slice — owner ledger

Objective: Graph context is live at the GraphAgent with the flat path deleted, the grounding
substrate is typed + enforced (provenance contract v2), the adjacent served-knowledge and
identity groundwork is absorbed, and the eval is green on oracles that actually measure the
graph — extended first, rebaselined post-cutover. v0.3.4 gate met.

Scorecard (source: dataraum-eval `calibration.run`):
- **Layer 1 — the correct ones (oracle-first, milestone 1, before any cutting).** Extend
  `metadata_truth.yaml` + oracles (DAT-736 deliverables pulled forward) to grade the
  objective surface: groundings enumerable per concept (PGQ MATCH-returns-rows), grounding
  parts parity vs `compose_extract_sql`, multi-grounding enumeration (`account_balance`
  across trial_balance/balance_sheet), `reconciles_with` population, and the P9 served-context
  effect (AP case surfaces both groundings + reconciliation; grounding-dependent sections
  improve vs baseline). Today NONE of these are graded — the existing `business_concepts`
  section grades column→concept bindings only.
- **Layer 2 — the specified ones (regression floor).** Full `calibration.run --all`: no
  section regresses vs the run-#1 baseline. Anti-masking rule (DAT-736 comment 2026-07-14):
  never relax an oracle to make a regression green.
- **Close-out.** 3-seed `clean_bands.yaml` rebaseline on the post-cutover engine (DAT-602
  pattern); no truth patches — a miss is a bug/teach scenario (DAT-685 rule); baseline
  history note v0.3.3 → v0.3.4.
- **Invariants (binary).** Flat context assembly deleted with zero remaining consumers; every
  bound edge ships a MATCH-returns-rows test on the finance corpus; absence falls loud
  (empty grounding batch = stop-condition, zero-support = NULL); both reviewers on every lane.

Non-goals: P5 (DAT-730, own track — absorbed temporal identity + grain derivation), P6
(DAT-731), P7 (DAT-732), P8 (DAT-733), P10 (DAT-735), P12 (DAT-737), P13 (DAT-738), P14
(DAT-740), DAT-739 (fast-follow AFTER this slice, by design), DAT-747 (no testdata),
DAT-771 (deferred with reactivation trigger), DAT-789 (gates P8/P10), corpus widening.

e2e budget: 6 runs granted 2026-07-19 · spent: 2 (#1 baseline+evidence · #2 P2 gate: clean,detection-v1,clean-flat on epic@0e04b282, COMPLETE — triage below)
- Run #1 doubles as pre-cut baseline AND the DAT-727 evidence run (measure: how often does a
  real grounding reference a column absent from its single enriched view — 0 `sql_snippets`
  rows exist today; metrics phase must execute). Philipp ruled this rides the budget.
- Planned: #1 baseline+evidence · #2 P2 gate · #3 P9 gate · #4–6 3-seed rebaseline. Any
  regression iteration beyond that → ask before run 7.
- Operational gate: eval stack needs one-time PG18→PG19 `calibration.run --reset` before any
  Tier-3 run (DAT-736 comment). Tier 1/2 (unit + recorded fixture) free, run after every
  integration. A `capture_fixture.py` refresh is needed when P2 changes output shape — ride
  it on a budgeted run's sidecar, don't spend a run on it.

## Spec provenance
Spec conversation with Philipp, 2026-07-19 (this session):
- Slice = P2→P9 spine + groundwork/adjacent open tickets ("not only P2–P9 … comments are
  important too"); borderlines ruled IN ("this is not planning but deferring").
- Scorecard = "the correct ones, not the specified ones" — his Socratic pointer; resolved as
  Layer 1 built first, Layer 2 as floor. DAT-736 explicitly in scope.
- Budget 6 runs; evidence run = budget run #1. Clean-up in scope; DAT-747 out (no testdata).

## Substrate map
(Step 2 fan-out COMPLETE 2026-07-19: engine-grounding, enriched/relationship, cockpit, eval.)

### Eval harness mechanics (mapped 2026-07-19)
- **New-oracle wiring = 5 points:** (1) truth section in the GENERATOR
  (`vendor/dataraum-testdata` `src/testdata/metadata_truth.py::canonical_metadata_truth` —
  fixture is GENERATED, never hand-edit; regen `scripts/regen_metadata_truth.py`);
  (2) `expected_*`/`read_*` in calibration/metadata_truth.py (read over `"<ws>_read".current_*`);
  (3) Tier-1 well-formedness in unit/test_metadata_truth.py (incl. fixture==generator pin);
  (4) `test_<section>_e2e.py` `@pytest.mark.llm`, skip-if-no-sidecar + activate_workspace,
  **determinism split** (structural=HARD; LLM-dependent=in-body `pytest.xfail`);
  (5) no run.py wiring — pytest auto-collects.
- **Truth today:** multi-grounding fan-in ALREADY encoded (business_concepts.required:
  3 cols→account_balance, 3→transaction_amount). `reconciles_with` edges: NET-NEW section.
  Served-context/AP oracle: NET-NEW. The old binding oracle is RETIRED (DAT-769 — grade
  consumers, not mappings); DAT-736 comment's `test_business_concept_grounding` no longer exists.
- **Rebaseline = LEAN gate (DAT-602, docs/history.md:9-25):** clean + detection-v1 + 3-seed
  clean resweep ≈ 15% of --all spend; `sweep_clean_seeds.py --reuse clean:<seed>` folds a
  completed clean run in as one seed (budget saver); `build_clean_bands.py` has a degeneracy
  lint that refuses constant continuous detectors; never hand-edit a band. Engine bump
  touching db_models ⇒ `make reset` first.
- **Tier-3 execution:** runner drives addSource → beginSession → operatingModelWorkflow
  (metrics ≈ 22 LLM calls) — **OM is try/except-wrapped (runner.py:525-535): OM failure =
  finding, sidecar still written with 0 snippets.** Evidence run must verify metrics
  SUCCEEDED (output/<strategy>/worker.log). Evidence surfaces: metrics stuck at `grounded`,
  state_reason, low-confidence assumptions, sql_snippets.parts.
- **Stack:** eval compose = `vendor/dataraum-context/packages/infra/docker-compose.yml`,
  project dataraum-eval, ports PG 5433/Temporal 7234/S3 8334; **engine worker runs on the
  HOST from the VENDORED working tree `dataraum-eval/vendor/dataraum-context`** — currently
  on plain `main` at b3eccf01 (BEHIND origin). → Run #1: ff-pull vendor to current main;
  gate runs #2+: point vendor checkout at the epic branch. `--reset` (the only sanctioned
  down -v) required once for PG18→PG19.
- **capture_fixture.py hardcodes legacy `ws_00000000…` schema (line 42)** but runner uses
  per-strategy workspaces (DAT-508) → would silently miss the run at the post-P2 refresh.
  Absorb: small fix, E1 lane carries it.
- **clean-flat strategy = denormalized-SHAPE variant (DAT-770 folded-dim truth), unrelated
  to flat-context** — nothing in the eval dies with P9's flat-delete.

### Enriched-view / relationship / bus-matrix seams (mapped 2026-07-19)
- **DAT-791 largely ALREADY BUILT** (DAT-516 sticky shape, 363c51e9 2026-06-22): prior-run
  lookup exists (enriched_views_phase.py:221-226), LLM sees only undecided pairs (:248-262,
  call skipped when none), cross-run identity = directional (from_column_id,to_column_id)
  (:233-238; views/db_models.py:76,86). **Residual defect = re-open trigger**:
  `_unchanged_considered_pairs` (:75-98) re-opens on dossier fingerprint which hashes
  LLM `confidence` + measured `coverage` (enrichment_agent.py:39-55) — jitter re-invokes the
  LLM → drift. Fix = re-open on TOPOLOGY delta only. Small, contained.
- **DAT-790 premise STALE**: single-column LLM-synth path populates introduces_duplicates
  since 2026-06-24 (semantic/processor.py:788-803, live conn from semantic_per_table_phase
  :120); surrogate path too (surrogate_mint_phase.py:621-623); oriented_row hard-sets False
  on one-to-many flip. **Genuine residual: manual `add` of never-detected relationship
  (relationships/materialize.py:272-278) → NULL; no-conn guard; stale comment
  context.py:1484.** Re-scoped to that residual + comment.
- **DAT-788 cleanly open**: fk_role written slicing_phase.py:385/396, surfaced (bus_matrix
  display :219; og_has_dimension property :309/417) but NO consumer branches on it. Collapse
  points = identity keys `(dimension_table_id, dimension_attribute)` at lineage/processor.py:375
  and property_graph.py:377 + og_references exclusion :289-291. Decision layer to wire:
  `bus_matrix.conformed_group` (union-find over judge CONFORM verdicts, bus_matrix.py:338-505);
  role-verdicts on dimension_hierarchies.role_verdict/:180-187; role pairs never fold (:264-275).
  All affected tables run-versioned → re-derives per run, no migration.
- **DAT-809 premise CONFIRMED**: only functional bus_matrix reader = lineage/processor.py:453-466
  (folded + conformed_group non-NULL). ContextDocument.bus_matrix is populated-but-dead
  (context.py:680-704 → :1182, never rendered to any prompt); current_bus_matrix has no
  engine reader and no cockpit query; bus_matrix is NOT in the property graph.

### Engine grounding/graph substrate (mapped 2026-07-19)
- **sql_snippets** (query/snippet_models.py:41): `parts` :102 (EXTRACT-only,
  {select,from,where[]} via `extract_parts_dict` formula_composer.py:71); `input_fields` :83
  (FORMULA-only — NOT a uses substrate, confirmed); `provenance` :93 dict with
  `column_mappings_basis` = {concept: {column, filter, resolution}} — **inner dict[str,str]
  STILL unenforced** (graphs/models.py:326; docstring :330-336 says contract v2 "rebases over
  this"). Semantic key uq over (type, standard_field, statement, aggregation,
  schema_mapping_id, parameter_value). No #487 remnants on main (confirmed).
- **Writers:** GraphAgent is the ONLY engine writer (SnippetLibrary.save_snippet
  ← agent.py _save_snippets:1363 / _save_failed_snippet:1439 / _save_composed_snippets:1510),
  fired from the METRICS phase (operating_model stage, worker/workflows.py:865-873).
  **begin_session produces 0 snippets — confirmed** (spine+value layer only). → Evidence run
  must execute the operating_model workflow. MeasureAggregationLineage rows DO come from
  begin_session's value layer (aggregation_lineage phase; upsert processor.py:721-726).
- **Basis/parts readers (contract-v2 consumer adaptation set):** _lookup_snippets:1607 +
  _build_prior_context:1676 (basis), additivity_resolver.py:131 + period_resolver.py:196/236/249
  (parts), DAT-631 gate reads sibling `assumptions` not basis (metrics_phase.py:409-427).
- **og_ inventory** (storage/property_graph.py; _ELEMENT_VIEWS :88-97, bodies :100-381,
  graph binding :384-433): vertices og_tables:109 / og_columns:148 / og_concepts:232; edges
  og_references:266 / og_has_dimension:295 / og_derived_from:312 / og_concept_edges:243
  (projects predicate+tolerance) / og_conformed_dimension:340. All keys ::text.
  **`grounded_by` placeholder documented at :236.** No current_grounding view, no Grounding
  storage model — P2's source view is NET-NEW (grounding lives only in snippet provenance).
- **reconciles_with:** predicate enum + tolerance col exist (semantic/db_models.py:73-96,:284),
  og_concept_edges projects them, but **no live writer emits RECONCILES_WITH** — P2 adds the
  producer (witness→Grounding→Grounding + ≥2-groundings-per-concept).
- **Flat assembly (P9 delete surface):** format_metadata_document context.py:1308-1550 +
  8 helper builders; GraphExecutionContext :293-378 filled by build_execution_context
  :386-1197 (~15 modules). Callers: with_rich_context agent.py:147; _generate_sql :755/762.
  introduces_duplicates fan-out note at **context.py:1480-1486** (not 1402).
  **Graph-replacement gap: value-sets, drivers, validations, cycles, conventions have NO
  graph elements** — P9 brief must settle how these five sections are served post-cutover.
- **Prompt:** graph_sql_generation.yaml v7.0 (dataraum-config/llm/prompts); slots
  {rich_context}=flat doc, {table_schema}=_build_schema_info agent.py:1155-1185
  (prefer-enriched all-or-nothing), {prior_context}, {vertical_conventions}, {graph_yaml}.
- **DAT-792 confirmed small:** _graph_to_yaml agent.py:1217-1272 omits GraphStep.validations
  (models.py:87-101,:128); verifier post-hoc at verifier.py:104-134 (flags, never gates).

### Cockpit consumers (mapped 2026-07-19)
- **The cockpit WRITES `sql_snippets`** (`query:<runId>` provenance, snippet-writer.ts via
  `sqlSnippetsWrite`) and reads only `graph:%` snippets. → **P2 contract: Grounding nodes
  must filter `source LIKE 'graph:%'`** or cockpit query-snippets leak into groundings.
- **P9 flat-delete is cockpit-safe**: NO cockpit reader of the engine-rendered flat context;
  query-context.ts is a parallel reimplementation over `current_*` views (mirror comments at
  query-context.ts:192,540,744 are prose, not consumption). **The real P9 risk surface is
  `graph:%` snippet production**: 6 readers (snippet-library/snippet-search,
  operating-model-load, drill-axes, drill-metric, why-metric, look-metric) consume
  parts/standard_field/statement/aggregation via the view, no typed API — the graph-traversal
  rewrite must preserve the snippet write contract.
- **current_bus_matrix: ZERO cockpit readers** (confirms DAT-809 premise); mirror includes it
  → engine column changes still need `db:pull:metadata` regen for schema-drift CI.
- **concepts/concept_edges read views: ZERO readers**; concepts TABLE written via
  `conceptsWrite` (concept-count.ts, concept-write.ts, list-verticals.ts). concept_edges: no
  cockpit consumer at all.
- **DAT-790 confirmation**: query-context.ts:687-693 is the ONLY reader of
  `relationships.evidence` / introduces_duplicates; caution rendered at :578-582; NULL → no
  caution. Engine-side fix suffices; cockpit needs nothing.
- **DAT-793 seam**: `nearUniqueColumns` (cardinality_ratio ≥ 0.9, grain-note.ts:89-108) is
  ALREADY loaded at compose time (query.ts:587 Promise.all) but only consumed post-run
  (query.ts:343 computeGrainNote → assumptions). Grain partially served via `<entities>`
  block (query-context.ts:739). Fix = surface near-uniqueness + grain into `stableContext`
  (extend entities block or new block); post-hoc note stays as backstop. **No engine change
  needed** — statistical profiles already mirrored.
- Answer-agent context = 5 builder blocks in query-context.ts (schema/dimensions/
  relationships/entities/drivers) + vocabulary, concatenated as a cached system block
  (query.ts:607-652). Prefer-enriched is all-or-nothing (query-context.ts:71-74) and hides
  typed facts + dim tables; enriched columns come from live DuckDB DESCRIBE (:87-118).
- Cross-package hand-mirrors to keep in lockstep: temporal/types.ts (snake_case Pydantic),
  worker/activities.ts ↔ engine worker/contracts.py (camelCase), task-queue.ts (DAT-818),
  write-surface.ts (4 control tables; grants in read_views.py `_CONTROL_WRITE_GRANTS`).

Known before fan-out (from Jira comments + eval repo, verified where noted):
- **DAT-727 state:** PR #487 parked (contract-v2 code salvageable: `ConceptGroundingBasis`
  submodel, `grounding_validation.py`, repair turn). Invariant 2 (decision=footprint) set
  aside by lead ruling; Invariant 1 (single-relation completeness) was the operative gap —
  mechanism landed as DAT-801 (Done): one neutral extension rule, one-hop bound,
  `relationships.confidence` gate, surrogate-mint pre-resolution. `reconciles_with`
  population moved INTO P2 (from P4): (a) aggregation-lineage witness reified as
  Grounding→Grounding reconciliation, (b) any concept with ≥2 Groundings. `ConceptEdge`
  model + `og_concept_edges` already carry predicate + tolerance (P4, shipped).
  `input_fields` is NOT the uses substrate (formula-template key; P7's `derives_from`
  substrate). `column_mappings_basis` is the uses substrate, contract unenforced today.
  `filtered_by → DimMember` = DAT-787, follow-on lane; until then `where[]` rides the
  Grounding node as a lossless property.
- **DAT-734 state:** passthrough/no-dim special case resolved by DAT-811 (Done) — every
  served relation is a uniform self-describing catalog citizen; no P9 branch needed.
  Flat-delete rationale locked in epic: no A/B, no switch; v0.3.3 tag is the rollback.
- **Eval surface (verified in dataraum-eval):** truth sections today: metric_additivity,
  stock_flow, reconciles_structurally, relationships, table_roles, semantic_roles,
  business_concepts (column→concept `required` map), cycles, folded_dimensions,
  degenerate_ids, bus_matrix. No grounding/multi-grounding/reconciles_with/served-context
  oracle exists. Last Tier-3 state (2026-07-14): 93 pass / 6 fail — 2 stale oracles to
  absorb (`test_fact_dimension_roles` role-taxonomy rename; `test_business_concept_grounding`
  invoices.amount→accounts_payable: decide relax-vs-real-misbinding), DAT-758/759 engine
  fixes since Done (expect green), driver oracle at xfail(strict=False) pending folded-dim.
- **PR #487 (verified 2026-07-19):** CLOSED; head branch `feat/dat-727-grounding-reification`
  ALIVE on origin at ac8d87e9. Footprint: grounding_validation.py (198+ / 172 test),
  graphs/models.py contract, property_graph.py og_ views (153+ / 253+ integration test),
  llm/tool_repair.py repair turn (77+ / 149 test), graph_sql_generation.yaml prompt,
  agent.py (78+), schema_graph.sql re-dump, cockpit prompts/query.ts (1-line). Salvage
  source for the P2 lane — cherry-pick/adapt, do not rebase wholesale (lead: "recut before
  re-entry, don't rebase").
- **Stale artifacts:** empty worktree `dat-727-p2` (branch `feat/dat-727-grounding-reification-v2`,
  ZERO commits beyond old main — an empty restart, delete both). Clean-up in scope.

## Plan
(living — re-cut freely within the objective)
1. **Lane E1 — oracle extensions (dataraum-eval)**: Layer-1 oracles vs the shapes P2 will
   emit. Milestone 1; blocks all cutting. Needs the P2 schema surface agreed first (names of
   og_ views/tables) — brief carries the contract.
2. **Run #1** — baseline + evidence (after `--reset`; metrics phase must execute so
   `sql_snippets` exist to measure). Sizes the P2 recut.
3. **Lane P2 — DAT-727 recut**: contract v2 (models + prompt + save-time validation +
   consumer adaptation: `prior_context`, DAT-631 confidence gate) → graph work (`og_grounding`
   vertex, `og_grounded_by`, `og_uses` un-nest, multi-grounding enumeration, schema_graph
   re-dump) → `reconciles_with` population. #487 salvage where sound. Two commits/phases.
4. **Lane G — bundled small groundwork (791+790+792, all shrunk by mapping):**
   DAT-791 = re-open on topology delta only (enriched_views_phase.py:66-98 +
   enrichment_agent.py:39-55); DAT-790 residual = measure at manual-add
   (materialize.py:272-278) + fix stale context.py:1484 comment; DAT-792 = thread
   GraphStep.validations into _graph_to_yaml (agent.py:1217-1272) + prompt slot.
   Disjoint files, one lane, one review pass ("bundle small work").
7. **Run #2** — P2 gate.
8. **Lane P9 — DAT-734**: graphs/context.py graph traversal assembly; prompt update; flat
   assembly + tests DELETED same cut.
9. **Lane F1 — DAT-787** filtered_by edge (follow-on to P2, per lead comment).
10. **Lane I1 — DAT-788** role-separated conformed identity (before or with P9 so served
    conformed structure is role-correct — sequencing decided after substrate fan-out).
11. **Lane K1 — DAT-793** grain to answer agent (cockpit, small); **Lane K2 — DAT-809**
    bus-matrix wire+filter (consumer: GraphAgent context render; e2e grading rides eval
    corpus — land the wire, record the grading limit honestly).
12. **Run #3** — P9 gate. Then DAT-736 close-out: truth consolidation, stale-oracle
    absorptions, 3-seed rebaseline (#4–6).
Sequencing of 4–6 and 9–11 is provisional until the substrate map lands.

## Lane briefs

### Lane E1 — oracle extensions (dataraum-eval + dataraum-testdata) · SPAWNED 2026-07-19
**Goal:** Layer-1 scorecard oracles exist and are Tier-1-green before any engine cutting:
(a) `reconciles_with` truth section (net-new, generator-derived) + oracle; (b) grounding
enumeration oracle — for each (concept → tables fan-in) in `business_concepts.required`, a
Grounding node per (concept, relation) is enumerable via the read view once P2 lands;
(c) parts-parity invariant test — for every persisted extract snippet,
`compose_extract_sql(parts) == stored sql` (structural, HARD, truth-free); (d) MATCH-shape
capability tests for og_grounding / og_grounded_by / og_uses: view absent → skip (pre-P2
engine), view present but EMPTY on finance corpus → FAIL (absence falls loud);
(e) served-context / AP oracle designed vs DD + DAT-734 AC (multi-grounding enumerated for
the AP-class concept + reconciles_with verdict present), in-body xfail until cutover;
(f) fix capture_fixture.py hardcoded legacy `ws_00000000…` schema → per-strategy workspace
discovery (DAT-508).
**Surface:** dataraum-eval `calibration/metadata_truth.py`, `calibration/unit/test_metadata_truth.py`,
new `calibration/test_grounding_e2e.py` (or clean extension of existing e2e files),
`scripts/capture_fixture.py`; dataraum-testdata `src/testdata/metadata_truth.py::canonical_metadata_truth`
+ fixture regen via `scripts/regen_metadata_truth.py`.
**Approach:** the 5 wiring points (generator → loader → Tier-1 → Tier-3 → auto-collect);
determinism split (structural=HARD, LLM-dependent=in-body xfail(strict=False)); truth is
GENERATED (never hand-edit the fixture). P2 shape contract to code against: Grounding
{relation, where[], select_expr} node; edges grounded_by/uses(1..n)/filtered_by(deferred to
DAT-787); reconciles_with producers = aggregation-lineage witness (Grounding→Grounding) +
any concept with ≥2 Groundings; contract v2 = ALL provenance keys typed (healthy:
column_mappings_basis/assumptions; failed: failure_mode enum + failure_reason).
**Work mechanics:** git worktrees inside dataraum-eval (`.claude/worktrees/dat-725-e1`,
branch `feat/dat-725-oracle-extensions`) and dataraum-testdata (same-name branch) — the
LIVE eval working tree is running budgeted run #1; do not touch it. Reviewers must cite
file:line; generic-praise review = failed review, redo.
**Do NOT touch:** `clean_bands.yaml`, existing oracle assertions (except surfaces retired by
DAT-769 being replaced), `strategies/*.yaml`, the engine repo, the live eval working tree,
no real-LLM/Tier-3 runs (Tier 1/2 only).

### Lane G — bundled groundwork 791+790+792 (engine) · SPAWNED 2026-07-19
**Goal:** three small, mapped fixes in one lane. (1) DAT-791: enriched-view re-open trigger
becomes TOPOLOGY-delta only — `_unchanged_considered_pairs` (enriched_views_phase.py:75-98)
re-opens on `dossier_fingerprint(cardinality, confidence, coverage)` (enrichment_agent.py:39-55)
which hashes jittery LLM confidence + measured coverage → change the predicate so a pair
re-opens only when added/removed from the confirmed set (or cardinality — the one
topology-shaped dossier fact — changes; lane judges, states rationale). Adapt tests.
(2) DAT-790 residual: manual `add` of a never-detected relationship
(relationships/materialize.py:272-278) persists evidence without introduces_duplicates →
measure empirical cardinality at that seam (reuse evaluator.compute_introduces_duplicates;
investigate conn availability at materialize time — if genuinely unavailable there, fall
loud/NULL stays and the lane reports why). Fix the STALE comment graphs/context.py:1480-1486
("LLM synthesis path doesn't yet populate it" — false since a1d3cfe54). (3) DAT-792:
thread GraphStep.validations (models.py:87-101,:128) into `_graph_to_yaml`
(agent.py:1217-1272) + the graph_sql_generation.yaml prompt so declared expectations are
served at authoring; post-hoc verifier (verifier.py:104-134) unchanged as backstop.
**Surface:** enriched_views_phase.py, enrichment_agent.py, relationships/materialize.py,
graphs/context.py (comment only), graphs/agent.py, dataraum-config graph_sql_generation.yaml,
matching tests.
**Do NOT touch:** snippet/provenance contract (P2's surface: snippet_models.py,
GraphProvenanceOutput, property_graph.py), flat context assembly beyond the one comment,
slicing/lineage identity keys (DAT-788's surface), cockpit.
**Note:** prompt YAML version-bump per its convention; DAT-790/791 premise corrections are
recorded in Jira later by the owner, not the lane.

### Lane S — slicing existence determinism (engine + cockpit) · SPAWNED 2026-07-19
**Goal (per the lead's doc, ruled in):** dimension existence becomes deterministic; the
slicing agent is demoted from elector to ranker. (1) Persist the WHOLE eligible set =
pre-filter survivors (grain-safe: DAT-805 gates) ∩ `semantic_role ∉ {measure, timestamp}` —
one SliceDefinition per eligible column, no phase reorder, schema unchanged (possibly
`detection_source` gains 'structural' CHECK value, slicing/db_models.py:70). (2) DAT-756
identity block iterates the eligible set (superset input, logic unchanged). (3) Agent output
(slice_priority/business_context/reasoning/confidence) merges as ENRICHMENT; un-ranked rows
get a priority floor; prompt reframed pick→rank. (4) Curation reads switch to
`ORDER BY slice_priority LIMIT budget`: cycles/context.py, graphs/context.py,
validation/resolver.py **+ the COCKPIT consumers of currentSliceDefinitions (query-context
buildCatalogBlock at minimum — grep ALL cockpit readers, enumerate don't sample; a larger
persisted set must not bloat agent context blocks).** (5) Bundled independent commit:
hierarchies MIN_SUPPORT_ROWS floors on `scan.d2` distinct pairs, not `scan.n` rows
(hierarchies/processor.py:999) — pre-registered, don't fish.
**THE TRAP (verbatim from the doc):** do NOT write the gate as "exclude a key with no
resolved FK" — a folded dimension key IS a key with no FK; keep every grain-safe survivor
that isn't measure/timestamp.
**Verification:** unit pins for same-inputs→same-slice-set (LLM affects only priority);
Tier-3 clean-flat (witness fires) rides owner-run gate sessions; never rebaseline reds.
**Do NOT touch:** bus_matrix conform logic (DAT-762), lineage pooling (DAT-800, later),
lineage/slicing identity keys (DAT-788 lane), P2 surfaces (snippets/provenance/property_graph),
enriched_views (Lane G landed).

### Lane P2 — DAT-727 recut (engine) · SPAWNED 2026-07-19
**Goal:** provenance contract v2 + Grounding graph reification + reconciles_with population.
Two phases/commits minimum: (a) contract v2 — strict Pydantic submodels for ALL
sql_snippets.provenance keys (healthy: column_mappings_basis all-columns-by-role per
concept + assumptions w/ AssumptionBasis; failed: failure_mode enum + failure_reason);
save-time membership validation vs the served relation schema (repair turn DAT-710 pattern,
fall-loud to failed-snippet); coarse lexical cross-check; consumers adapt in-cut
(_lookup_snippets, _build_prior_context; parts readers unchanged); NO backfill.
(b) graph work — read view `current_groundings` (cross-lane contract with E1) + og_grounding
vertex + og_grounded_by + og_uses element views (un-nest typed provenance ⋈ current_columns),
**source LIKE 'graph:%' filter** (cockpit writes `query:` snippets), multi-grounding
enumeration, schema_graph.sql re-dump, cockpit Drizzle mirror regen (db:pull:metadata,
self-contained), MATCH-returns-rows tests. (c) reconciles_with population from witness +
≥2-groundings; **shape fork (ConceptEdge self-loop vs Grounding→Grounding edge) must be
settled against P4 model constraints and CHECKED WITH OWNER before the graph-binding commit.**
**Salvage:** origin/feat/dat-727-grounding-reification @ ac8d87e9 — cherry-pick/adapt, no
wholesale rebase. Evidence: Invariant 1 HOLDS (0/13) → single-relation membership stands.
**Do NOT touch:** filtered_by/DimMember (DAT-787), flat context assembly (P9), slicing/
lineage identity keys, eval repo.

## Lane S — INTEGRATED 2026-07-19
6 commits merged (core rescope · curation reads + priority-sort bugfix + NULL-confidence
guard · cockpit budget + regen · handoff.md · spec follow-ups · senior criticals: head-scoped
role reads + attribute justify-and-pin). Both reviewers APPROVED (senior differential-verified).
Post-merge gates on epic: engine ruff clean + 2179 unit; cockpit biome/tsc clean + 1688 tests.
Slicing prompt v4.0.0 (pick→rank); CURATED_SLICE_BUDGET=12 engine+cockpit.

## Parked
(one line each: what · where · why deferred)
- Pre-existing `column_id` fallback seam (enriched col w/ unresolvable FK prefix stores
  view-table column id under fact table_id) · graphs/context.py `_build_context_data` ·
  load-bearing for DAT-621 look_values; reach widened by full inventory — follow-up candidate.
- Global-vs-per-table curation budget (12 global = old election semantics) · slicing curation
  reads · `PARTITION BY table_id` sub-quota is the known upgrade if Tier-3 shows crowd-out.
- Curation-read tiebreak `column_name` ASC is collation-sensitive SQLite↔PG for WHICH floor
  rows make the cut · curation reads · order semantics pinned; revisit only if flaky.
- **d2 distinct-pairs floor (slicing-doc deliverable 5)** · hierarchies/processor.py:999 ·
  Lane S traced the consumer chain: d2≤n always, floor 100 flips every ~27-pair account
  structure to needs_confirmation → excluded from fold components → NO conformed folded
  cells → the clean-flat witness this slice is graded on CANNOT fire, and drivers stop
  collapsing account_id≡account_name. Implementing as pre-registered ships a predicted
  regression; changing the constant = threshold-fishing; judge-confirmed-counts-as-
  confirmation semantics = re-spec → LEAD'S CALL. Severable by design; lanes 1-4 proceed.
- `.claude/handoff.md` entries for DAT-790/791 (shape stability, manual-teach caution) ·
  eval repo · deferred to close-out per Lane G brief.
- Legacy considered-entries (sha16 dossiers) re-open ONCE on next run then stick ·
  enriched_views · expected one-time re-judgment, not drift (Lane G note).

## Lane E1 — INTEGRATED 2026-07-19
Eval main: 3 commits merged (oracles: expected/read groundings+reconciles_with,
test_grounding_e2e.py 8 tests w/ pre-P2 skip guards verified live; capture_fixture
per-strategy discovery + loud empty-fail; review hardening). Testdata: 2 commits merged —
NOTE: vendored testdata sits on `feat/dat-757-2b-coincidental-bijection`, NOT main; E1 based
there and I merged there (the live-run lineage). Reconcile testdata branch topology at
close-out. Live Tier-1 gate post-merge: 93 passed. E1 seams relayed to P2 (labels
grounding_node/grounded_by/uses; current_groundings(concept,relation); prefix normalization;
reconciles_with read from concept_edges; **concept_edges CHECK source widening = P2 scope**).
Parts-parity verified 0/14 divergent on run #1 snippets. entropy_inputs.sqlite refresh rides
the post-P2 budgeted run.

## Eval history
(date · trigger · scorecard row · verdict)
- 2026-07-14 · pre-slice Tier-3 (DAT-736 comment) · 93P/6F · baseline-stale; re-establish as run #1
- 2026-07-19 · **run #1 baseline+evidence** (vendor@main e760197f, seed 42) · clean: 112P/2F/23S/1xf
  — FAIL test_relationship_recall (7/9: bank_transactions.account_id→chart_of_accounts NEVER
  A CANDIDATE = Layer-A gap; invoices.entry_id→journal_entries judge-DECLINED at 0.55 while
  its own reasoning says "genuine sparse FK", 100% RI invoice-side / 24% coverage JE-side) +
  FAIL test_bus_matrix_e2e::test_referenced_recall (bank_transactions.account_id cell —
  cascade of the Layer-A gap) · detection-v1: 124P/1F/24S/2xf/4xp — FAIL
  test_cycle_recall (accounts_payable cycle MISSING; JE/bank-rec/period-close detected) ·
  **metrics phase SUCCEEDED on clean: 13 grounded / 1 ungroundable, snippets saved, promoted**
  → evidence data exists. Failure set DIFFERS from 2026-07-14 (relationship recall + cycles
  passed then) → suspected regressions on main between 07-14 and 07-19, forensics running.
  NOTE: AP is the P9 grading case — the AP-cycle miss + the JE↔invoices FK decline sit ON
  the objective's critical path.
- 2026-07-19 · **run #1 forensics (read-only, no budget)** — mechanisms identified:
  · **EVIDENCE HEADLINE: 0/13 groundings reference a column outside their single served
    relation** (all 13 read enriched_* views; DAT-801 completeness holds; basis prose cites
    origin tables but SQL reads the materialized `fk__attr` columns). Contract-v2
    Invariant 1 HOLDS → P2 recut unblocked, #487 salvage sound on this point. 14th extract
    (`inventory`) = verifier_rejected fall-loud, CORRECT (corpus has no Inventory account;
    dio+ccc downstream metric_not_executed by design).
  · **R1 Layer-A gap** (bank_transactions.account_id→chart_of_accounts, 0 rows any method):
    bank_transactions.account_id has 2 distinct values, 100% contained in coa (60 distinct);
    Jaccard 0.033 < min_confidence 0.3; containment rescue GATED `min_distinct > 10`
    (joins.py:347 anti-trivial-containment guard) forces 0.0 → structurally unproposable.
    Deterministic, data-shaped. Cascades into test_referenced_recall (slice row EXISTS with
    dimension_table_id NULL — election was fine this run; the relationship is the hole).
  · **R2 judge decline** (invoices.entry_id→journal_entries): Layer-A candidate row exists
    conf 1.0 (1:1, 100% RI invoice-side); synthesized FK row conf 0.55 <
    REL_CONFIRM_MIN 0.7 (semantic/processor.py:645) stays unconfirmed while the judge's OWN
    reasoning affirms "genuine sparse FK". Confidence-vs-conclusion mismatch on asymmetric-
    coverage sparse FKs.
  · **R3 AP cycle**: the invoice→payment flow WAS detected (conf 0.92) but labeled
    accounts_receivable (truth doesn't list AR); detection-v1's own obscure_column_names
    injection renamed vendor_id/payment_terms; semantic recovered "vendor_identifier" at
    conf 0.25 hedged "vendor or customer"; cycles LLM (single call, no candidate log)
    emitted the AR label. Injection-stressor working as designed; served evidence too weak.
  · **Slicing-existence doc** (Philipp, Downloads/slicing-existence-determinism.md): its
    mechanism (LLM election gates dimension existence; ≤6 slots; account_id fault line) did
    NOT cause this run's reds — but current_slice_definitions IS og_has_dimension's
    substrate, so existence-nondeterminism directly threatens P9's served graph. Same class
    as DAT-791 (identity of inputs → identity of shape). Doc's fix: eligible set persisted
    deterministically (grain-safe ∩ role ∉ {measure,timestamp}), agent demoted to ranker,
    ~3 curation reads ORDER BY priority LIMIT budget; DAT-800 stays later.

- 2026-07-19 · **run #2 P2 gate** (epic@0e04b282, seed 42) · **clean: 123P/2F/25S/2xf —
  SAME two baseline reds only (R1 cascade), grounding oracles included and no new
  regressions; Layer-2 floor HOLDS on clean.** · detection-v1: 130P/5F — cycles AP (R3,
  known) + FOUR new vs baseline: referenced_recall bank_transactions.account_id (FK IS
  confirmed this run yet cell missing → referenced-identity path suspect, possible Lane-S
  regression), relationship_recall bank_transactions.payment_id→payments (judge "declined"
  at conf 1.0 w/ perfect evidence — smells like judge OUTPUT OMISSION), injection_detected
  + intent_readiness relationship_entropy:payments.invoice_id 0.000 (no node evidence;
  run #1 detected it). · clean-flat (FIRST run, no baseline): 124P/3F — **witness fires
  2/3 rollups (doc predicted this; was 0/20)**, only balance_sheet folded cell missing;
  column meanings exist ONLY for trial_balance (5 tables unannotated — plausibly
  pre-existing clean-flat state); reconciliation misses balance_sheet.ending_balance.
  **STOP-THE-LINE: forensics dispatched on A1 (referenced cell), A2 (judge omission),
  A3 (relationship_entropy zero) before any further lane.**

## Decisions
- 2026-07-19 · scorecard "correct vs specified" → Layer-1 oracles built first, full sweep as
  floor · lead's Socratic pointer + /own oracle-first rule.
- 2026-07-19 · borderlines 788/793/809 IN · lead: "this is not planning but deferring".
- 2026-07-19 · evidence run = budget run #1 · lead pick.
- 2026-07-19 · **slicing rescope (Downloads/slicing-existence-determinism.md) ruled INTO the
  slice** — lead confirmed; same class as DAT-791 (og_has_dimension stands on
  current_slice_definitions). clean-flat folds into gate sessions (witness 0/20 → expected
  to fire). DAT-800 (lineage pools on conformed_group) stays LATER per the doc's own
  ordering. R1–R3 absorb classification stood unobjected → proceed as slice work.
- 2026-07-19 · **reconciles_with shape = ConceptEdge SELF-LOOP rows at concept grain**
  (owner confirm of P2 checkpoint; conforms to DD edge table). Decisive: witness event side
  is never a Grounding (nothing mints the MAL rollup snippet) so Grounding→Grounding cannot
  represent it; pair grain stays derivable from grounded_by fan-out (one home). CHECK widens
  source IN ('seed','derived'); idempotent end-of-metrics derivation; supersede on vanished
  support; seed rows untouched. og_uses → current_enriched_columns for enriched relations
  (post-DAT-811) approved.

## Checkpoint log
- 2026-07-19 · spec contract locked (this ledger); slice opened on epic/dat-725-graph-context.
- 2026-07-19 · checkpoint: milestone 1 (oracle-first) landed; baseline + evidence 0/13 reported; G+E1 integrated, P2+S running; d2-floor fork put to lead; budget 1/6. Jira comment 17132.
