# DAT-853 wild-corpus stabilisation — owner ledger

Objective: All twelve wild-corpus findings closed at their four failure-mode roots, verified
on the corpora that evidenced them, with the measuring rig fixed FIRST — calibration green on
the fixed gauges, never on the old ones.

Scorecard (source: DAT-853 epic description, locked by lead 2026-07-21):
1. Every open finding's repro closed on its evidencing corpus (wave 2 → rel-f1, wave 3 →
   clean-flat), per-ticket acceptance.
2. DAT-851 proven, not asserted: degeneracy lint red-teamed — a planted constant-output
   detector is caught.
3. DAT-849: oracles grade (conflict, ignorance) bands for every surface the product bands on;
   a 0.0-score band-driver can no longer pass ungraded.
4. Abstention end-to-end: "not measured" renders as abstention at every consumer
   (845/843/844/846 are applications); an undeclared vertical cannot silently promote a
   green head.
5. Full calibration green on `clean`, 3 seeds, on the fixed gauges — only AFTER waves 1–3;
   coordinate with DAT-736 (owns the post-DAT-725 rebaseline).

Non-goals: DAT-714 (DAT-671 package) · DAT-725 phases (start after waves 1–3) · DAT-581
(postponed) · cockpit/drill work · tuning anything to the finance corpus.

e2e budget: none pre-granted — deterministic tiers per integration; full real-LLM
calibration only at wave boundaries, EACH run on explicit lead go · spent: 0

## NEXT ACTION
Wave-2 lanes W2-A (abstention primitive+843+851det, fable) · W2-B (845 promote gate,
opus) · W2-C (846 role filter, sonnet) · W2-D (844/808 effect guard, sonnet) spawned in
ENGINE worktrees off the epic branch. Integrate as they land (order: any; C/D trivial).
After all four: wave-2 boundary = rel-f1 verification → needs LEAD GO (real-LLM run).

## State
WAVE 1 COMPLETE + PUSHED: eval main origin = 6e9619f (851 lint fix, 849 band oracles,
probe dirs). Scorecard: band 2 GREEN (lint red-teamed, planted constants caught) ·
band 3 GREEN at unit tier (band oracles, 20 tests, score-0.0/investigate proof; Tier-3
live oracle armed, skips honestly until next calibration run) · bands 1/4/5 red.
Lanes: W2-A/B/C/D open (engine). Epic branch from main@890e88a3d.
Eval repo ABSOLUTE path: /Users/philipp/Code/dataraum/dataraum-eval.
Gotcha: eval worktrees can't `git worktree remove` (vendor submodules) → rm -rf + prune.

## Open forks
- W2-A may hit: does abstention need a product-visible readiness state beyond
  ready|investigate|blocked? Pre-registered: that is a SPEC question → lane stops, owner
  escalates to lead. 0 reversals.

## Parked
- spikes/dat-544/driver_spike.py tracked in repo — delete with next driver-touching PR
  (also noted on DAT-543).

---

## Substrate map
(pending grounding fan-out; carried over from the planning session:)
- Pipeline: add_source (10 phases, fan-out per table) / begin_session (14, sequential) /
  operating_model (6) — worker/workflows.py orders them; phases self-register via
  @analysis_phase (pipeline/registry.py). No pipeline.yaml.
- Eval: /Users/philipp/Code/dataraum/dataraum-eval — runner `python -m calibration.run`
  (make calibrate), pytest only reads persisted results (`--strategy`, sidecar
  calibration_run.json); strategies/*.yaml incl. clean, clean-flat, detection-* family;
  wild corpora gitignored under corpora/relbench (rel-f1 …), staged via
  scripts/stage_wild_corpus.py. Real-LLM runs.
- DAT-853 assets: calibration/phase_contract.py + scripts/stage_wild_corpus.py COMMITTED;
  scripts/probes/add-source-audit/ + scripts/probes/dat826-rebaseline/ UNTRACKED in
  dataraum-eval → wave-1 hygiene: commit or delete.
- Fixed already: DAT-834 (cycle-space description, PR #529), DAT-835 (core/duckdb_types
  shared predicate, PR #535), eval strategy-scoping (faba11e, 39db4b7 in dataraum-eval).

### Eval-side substrate (grounded 2026-07-21, sonnet reader; paths in dataraum-eval)
- **DAT-851 rig half — exact defect:** scripts/build_clean_bands.py:75
  `if max(values) <= _FLOOR: continue` (_FLOOR=0.1 line 34) runs BEFORE the constancy
  check (83-90), so a detector constant at exactly 0.100 never trips the degeneracy lint.
  Lint is a hard build-break (SystemExit 136-139) with _DISCRETE_BY_DESIGN exemptions
  (44-58). Tier-1 tests: calibration/unit/test_clean_bands_lint.py. Fix: constancy check
  over ALL emitted keys independent of floor. Repro tool:
  scripts/probes/dat826-rebaseline/emission_check.py join_path_determinism.
- **DAT-849 — mechanism + existing pattern:** engine persists BOTH surfaces:
  EntropyObjectRecord.score (conflict-only; entropy/loss.py:114-121 reads ignorance from
  evidence) and EntropyReadinessRecord.band/intents (entropy/db_models.py:118-174,
  bands ready|investigate|blocked, loss.py:74-83). Score-only oracles:
  test_detector_recall.py (DETECTION_THRESHOLD=0.3, :439-471), test_detector_precision.py
  (clean_bands score-only), test_adjudication_recall.py. Band-grading pattern ALREADY
  EXISTS: conftest.py:359-488 intent_readiness fixtures + test_intent_readiness.py vs
  hand-authored intent_readiness.yaml floors. Fix shape: extend band oracles across the
  detector surface + expose (conflict, ignorance) as first-class fixture output.
  clean_bands.yaml = score-only [min,max,seen] per (table.column, detector), seeds 46-48.
- **Corpora/repro:** clean-flat = Tier-A strategy (strategies/clean-flat.yaml,
  scenario month-end-close-flat, no injections, full truth). rel-f1 = Tier-B, NO strategy
  yaml — staged by scripts/stage_wild_corpus.py (declared-structure truth only:
  relationships + semantic_roles.timestamp, tier:wild); vertical stand-in via
  scripts/frame_wild_vertical.py (hand-authored MOTORSPORT concepts, ungraded).
  is_wild() gate (calibration/metadata_truth.py:674-689) makes absent-truth oracles stand
  down on Tier B. rel-f1 IS graded today: FK recall/precision 13/13
  (test_relationships_e2e) + phase_contract.py (standalone CLI
  `python -m calibration.phase_contract rel-f1`, 7 add_source phases, engine-independent
  expectations; NOT wired into pytest).
- **3-seed mechanics:** scripts/sweep_clean_seeds.py (--seeds 46 47 48; throwaway
  strategy+workspace per seed; --reuse to fold in a completed run) →
  output/seed_sweep/seed_*.yaml → build_clean_bands.py aggregates + lints + writes
  clean_bands.yaml. Interim rebaseline ALREADY LANDED post-835 (DAT-826, commit 5c10965,
  2026-07-21).
- **DAT-736:** In Progress in Jira; no eval-repo branch; its real progress = commits
  9114ee7/8b36032/39db4b7/5c10965. Stays open until after DAT-725; final gate = P4-P8
  metadata_truth consolidation + v0.3.4 bands. Coordination honored: full rebaseline only
  after waves 1-3.
- **Probe dirs (wave-1 disposition):** untracked add-source-audit/ (6-file census
  toolkit) + dat826-rebaseline/ (diff_bands, emission_check) — epic earmarks
  commit-or-delete; dat850-edge-kind/ (validation_sql.py one-off) → wave-3 input, then
  delete per probe policy.

### Engine detector-rig substrate (grounded 2026-07-21; paths in packages/engine)
- **DAT-851 detector half — WHY constant:** JoinPathDeterminismDetector
  (entropy/detectors/structural/relations.py:16-129): ambiguous branch needs >1 distinct
  col-path between a table pair, but loaders.py:548-577 + engine.py:173-220 deliberately
  exclude candidates (DAT-405) and the LLM confirms ~one rel per pair → branch
  structurally unreachable → every rel emits score_deterministic=0.1
  (dataraum-config entropy/thresholds.yaml:47-50). Unit tests inject analysis_results
  directly, bypassing the loader — can't see it. loss.yaml:196-198 weight 0.8×0.1=0.08 →
  can never leave "ready". DISPOSITION: wave-2 abstention application (abstain when ≤1
  path — the question is unanswerable), NOT wave 1. Wave 1 = rig half only. Testdata gap
  for a REAL ambiguous fixture: two distinct FKs same pair (DAT-419 note in
  test_detector_recall.py:120-124).
- **Lint boundary detail:** floor check is `<=` so a detector constant EXACTLY at
  _FLOOR=0.1 is skipped; no test covers values==[0.1,0.1,0.1]. Also NOISE_FLOOR=0.15 in
  test_detector_precision.py:56 vs _FLOOR=0.1 — unaligned floors (secondary; note only).
- **DAT-843 — TWO Benford paths, both ungated for decade span:** (1)
  analysis/statistics/quality.py:49-131 check_benford_law — only gate is n>=100; result →
  statistical_quality_metrics.benford_compliant → graphs/context.py:1538-1574 appends
  "benford_violation" flag UNGATED into LLM context; cockpit column-profile.tsx +
  look-profile.ts render it. (2) entropy/detectors/value/benford.py:74-152 — gates
  role=measure, non-dimensionless, n>=100; loss.yaml EXCLUDES benford from loss (DAT-442,
  informative only). Fix = decade-span applicability gate at BOTH sites → abstention.
  Existing tests reinvent chi-square inline and never call check_benford_law().
- **Banding surface (849 engine side):** loss.py band() 0.3/0.6; _PRIMARY_SIGNALS
  score|conflict|surprise read obj.score, others (ignorance) read evidence-max
  (loss.py:114-121). entropy_objects (raw) vs entropy_readiness (band, CHECK
  ready|investigate|blocked) → current_entropy_readiness → 9 cockpit consumers
  (why-*/look-*/list-tables/readiness-grain/grounding-readiness/briefing).
- **DAT-847 — the three disagreeing surfaces (reconstructed, verify vs ticket):**
  (1) entropy_readiness band — delete+insert per run, always fresh;
  (2) ColumnConcept.temporal_behavior — entropy/resolve.py:72-130 UPDATE-only, total
  ignorance LEAVES STALE PRIOR (pinned by test_resolve_temporal_behavior.py:131-138!) —
  consumed by graphs/additivity_resolver.py:194-215 AND cockpit drill-axes.ts:236-280
  (independent TS flow-gate);
  (3) SemanticAnnotation.temporal_behavior_claim — raw pre-adjudication LLM claim, fed
  back into per-table synthesis via semantic/utils.py:80-150.
  Structural witness overrules LLM when both opine (measurements/temporal_behavior.py:
  251-268). No cross-surface consistency test exists.

### Drivers + operating-model substrate (grounded 2026-07-21)
- **DAT-846:** _measure_columns (analysis/drivers/persistence.py:77-113) selects every
  semantic_role='measure' column with NO join to TableEntity.table_role (zero table_role
  refs in drivers pkg); dim tables have no EnrichedView (only FACT/PERIODIC_SNAPSHOT get
  one, enriched_views_phase.py:156-158) → processor.py:409-414 returns empty
  DriverRanking(n_rows=0); persistence.py:141-171 appends UNCONDITIONALLY ("born loud"
  docstring) → empty artifact reaches graphs/context.py:638-653 + cockpit look-drivers.
  Missing regression test: DIMENSION table with measure-labeled column.
- **DAT-844/808:** `group_mean/baseline − 1` with exact-zero guard `if baseline else 0.0`
  in THREE targets (analysis/drivers/targets.py:85,133,199); EntityDemeanedRatioTarget
  :244-247 already returns [] — the correct pattern to generalize. Blow-up path:
  FlowTarget fed within-entity residual (processor.py:612-629, wired :841-845 under high
  ICC) → baseline ≈ tiny float → ±1e18. No clamp in tree.py/persistence; raw effect
  reaches LLM context (graphs/context.py:2089-2097 `+.2f`) + cockpit query-context.ts
  formatDrivers; driver-list.tsx does NOT render slices (unaffected surface).
- **DAT-845:** validation/business_cycles/metrics phases each early-return
  PhaseResult.SUCCESS with outputs {outcome, declared: 0} (validation_phase.py:109-123,
  business_cycles_phase.py:95-107, metrics_phase.py:148-161); OperatingModelWorkflow
  (worker/workflows.py:799-889) never inspects outcomes; promote_operating_model_run
  (worker/activity.py:746-770) flips head unconditionally. require_known_vertical
  (core/vertical.py:138-162) only rejects typos — PLACEHOLDER (_adhoc) always valid,
  FRAMED after ONE concept row. No OperatingModelWorkflow-level test exists at all.
  Fix seam: promote gate reads the three phases' declared/outcome counts.
- **DAT-848:** Concept deliberately NOT run-versioned (db_models.py:128-201, keyed
  (vertical,name), superseded_at only lifecycle); seeded per-run from
  ctx.config["vertical"] (semantic_per_column_phase.py:136,159 → concept_store.py:37-86
  ON CONFLICT DO NOTHING); wrong-vertical rows NEVER superseded; og_concepts
  (property_graph.py:249-259) + cockpit currentConcepts (schema.ts:55, no head join —
  contrast column_concepts :119 which IS head-joined) serve ALL verticals ever seeded.
  ColumnConcept shows the run-scoped pattern the fix can mirror.
  eval's frame_wild_vertical.py stand-in is a live contamination source in wild testing.
- **DAT-836: ALREADY FIXED on main** — 0e0cf9996→`0e0cf5996` "docs(config): _adhoc no
  longer promises engine-side concept induction" (Refs DAT-836/833), ancestor of HEAD;
  no other stale induction adverts found. Only Jira transition remains (lead, closeout).
  Adjacent open fact: non-finance e2e needs cockpit frame or the eval stand-in (DAT-692
  is the real fix — OUT OF SCOPE, note only).
- **Abstention contract (wave-2 primitive must fit ALL of):**
  (1) EntropyObject.score plain float, no NA sentinel (entropy/models.py:34-69);
  (2) detector contract: detect()->list only; can_run() advisory (detectors/base.py:94-166);
  (3) snapshot.py:186-250 _run_detectors: can_run False = bare continue (no log/record),
      exception = log-only; [] indistinguishable from both (DAT-405 precedent bit once);
  (4) EntropyObjectRecord.score NOT NULL, no status column (db_models.py:43-103);
  (5) loss.py:137-144 `merged.get(intent, 0.0)` → zero-object intent = risk 0.0 = READY —
      the exact missing→green line.
  Same absence→green pattern instances: drivers persistence (846), promote gate (845),
  unscoped concept reads (848) — primitive judged against closing all of them.

## Plan
Wave 1 — instruments: DAT-851 (join_path_determinism constant + lint blind spot) ·
  DAT-849 (band-grading oracles) · probe-dir hygiene. Gate: red-teamed lint + bands graded.
Wave 2 — abstention contract: primitive once, then DAT-845 / 843 / 844+808 / 846.
  Verify on rel-f1.
Wave 3 — catalog invariants: DAT-850 edge kind (one owner, write-site enforcement) pulls
  852/847/839; candidate-lifecycle bugs 823/824; judge over-confirm 723. Verify on clean-flat.
Wave 4 — hygiene: DAT-848 (concepts run-scoping) · DAT-836 (_adhoc/ontology.yaml).
Close: 3-seed clean calibration on the fixed gauges (lead go required per run).

### Catalog substrate (grounded 2026-07-21)
- **DAT-850 — SIX edge-kind deciders, three bases, zero coordination:**
  A og_references NOT-EXISTS same-dim exclusion (property_graph.py:305-333, table-grain);
  B og_conformed_dimension shared (dim_table_id, attribute) (:379-419, attribute-grain);
  C enriched_views grain probe COUNT LEFT JOIN (enriched_views_phase.py:1058-1128);
  D slicing dim_table_by_fk_col via C's verified rel ids (slicing_phase.py:744-750);
  E LLM stamps relationship_type foreign_key|hierarchy on the row itself
    (semantic/processor.py:928-949);
  F bus_matrix conform judge for folded dims (bus_matrix.py:338-505) — its referenced leg
    recomputes B independently.
  Cycles (cycles/context.py:156-182) + validation (validation/resolver.py:150-178) both
  read load_defined_relationships (relationships/utils.py:34-78) = detection_method !=
  'candidate', NO conformed exclusion → judge-stamped FK served as genuine FK verbatim.
- **Contradiction writable:** relationships/db_models.py:52-106 has three per-column
  CHECKs, NO compound (type=fk ∧ cardinality=m2m). Writers: judge-confirm
  (processor.py:895,935-949 — _resolve_cardinality independent of type) and overlay
  materialize (materialize.py:309-327, fk hardcoded) both unguarded; surrogate mint IS
  guarded (surrogate_mint_phase.py:569-579 refuses m2m). test_oriented_row.py:280-283
  ASSERTS the contradictory row is admitted — test follows design, adapt it with the fix.
- **DAT-839:** confidence = max(jaccard, containment) (joins.py:362-384) persisted raw
  (detector.py:230); containment rescue fires at REF_UNIQUENESS_MIN=0.95; consumers gate
  >0.7 (detector.py:115-116, relationships_phase.py:122-124). Correct pattern in-tree:
  relationship_discovery pools it as ONE witness at 0.7 reliability. Complete-graph
  mechanism: TYPE_GROUPS lumps all int widths (joins.py:52-101); _should_compare_columns
  waives the cardinality cap when near-unique (:277-292); finder.py:48-65 full
  upper-triangle → sequential surrogate ranges overlap by coincidence → 36/36.
- **DAT-852:** validation_sql.yaml:47-81 forces ONE result row ("deviation = the rate")
  vs orphan_transactions.yaml:32-49 sql_hints wanting per-leg rows → LLM must pool;
  resolver serves ungated legs (same read as cycles gap); evaluate.py:139-196 _judge
  reads result_rows[0] only. No orphan_transactions test exists.
- **DAT-824:** decline path upserts on the SAME unique key as the detector candidate
  (processor.py:927-971, index run_id+from+to+detection_method; upsert.py:101-106
  replaces every non-key column) with impoverished evidence (agent.py:277-299 carries
  only reasoning; _build_candidate_metrics_lookup restores a subset, never
  join_confidence/algorithm/source) → measured overlap evidence destroyed on decline.
- **DAT-823:** persist_column_concepts (processor.py:244-314) is the ONLY
  ColumnConcept.meaning writer, authored in the single semantic_per_table turn; bus
  matrix/lineage/cycles are later read-only consumers — no revision channel.
- **DAT-723:** semantic_per_table.yaml:97-119 "containment … at ANY cardinality" +
  caution list (:126-129) names dates but not period identifiers; candidate feed
  (utils.py:148-217) has no temporal/period-role pre-filter.
- **DAT-777 residue:** surrogate mint hand-rolls its orientation swap
  (surrogate_mint_phase.py:538-660, own comment "can't route through oriented_row"),
  CHECK constraint is the only backstop; drift risk documented, accepted for now.

## Lane briefs
### W1-A — degeneracy-lint boundary + probe disposition · sonnet · eval repo
Goal: the lint catches constant detectors regardless of the floor; probes dispositioned.
Surface: dataraum-eval scripts/build_clean_bands.py, calibration/unit/
test_clean_bands_lint.py, scripts/probes/* (git add two, leave dat850-edge-kind).
Approach: constancy check over ALL keys with >=2 values, independent of _FLOOR (floor
keeps gating band RECORDING only); keep _DISCRETE_BY_DESIGN; tests for the exact-floor
constant, below-floor constant, and planted-constant red-team. Do NOT touch conftest.py,
test_detector_*.py, clean_bands.yaml, engine repo. No pipeline/LLM/docker runs.
### W1-B — band-grading oracles (DAT-849) · opus · eval repo · INTEGRATED 6e9619f
Goal: eval grades the (conflict, ignorance)→band surface, not only score.
Outcome: calibration/band_grading.py seam (grades via the ENGINE's own
loss_risk_for_object/LossConfig.band + _signal_value — no reimplementation), additive
banded_measurements fixture, Tier-1 proof (20 tests incl. score-0/ignorance-0.811/
investigate signature + 14-detector loss-table sweep), Tier-3 live oracle (honest skip
without a run). Fable reviewer: 3 Majors found+fixed. No hand-authored floors needed.

### W2-A — abstention primitive + entropy applications · fable · engine
Goal: a typed "I did not measure this" outcome exists end-to-end in the entropy layer;
join_path_determinism (851 detector half) and Benford (843, BOTH sites) become its first
applications. Surface: entropy/models.py, db_models.py, detectors/base.py, snapshot.py,
loss.py, readiness persistence; detectors/structural/relations.py,
detectors/value/benford.py, analysis/statistics/quality.py, graphs/context.py benford
flag gate; dataraum-config entropy/* if contracts move. Approach: typed status (CHECK +
strict models, v4 enforcement standard), NOT a magic score; loss rollup gains a third
outcome distinct from measured-clean (`merged.get(intent, 0.0)` line is THE target);
snapshot's silent can_run-False/exception paths become persisted abstentions. Must NOT:
change the ready|investigate|blocked band vocabulary without stopping to ask (spec
fork); touch drivers/ (W2-C/D), worker/workflows.py (W2-B), relationships write paths
(wave 3). Full engine CI + both reviewers (fable).
### W2-B — 845 promote gate · opus · engine
Goal: an operating_model run where validation+cycles+metrics ALL declared 0 cannot
silently flip the head. Surface: worker/workflows.py OperatingModelWorkflow,
worker/activity.py promote_operating_model_run (+resolve if needed), tests (first
OperatingModelWorkflow-level test). Temporal determinism/replay safety mandatory —
senior-code-reviewer required. If a legit flow depends on empty-promote (grep cockpit +
smokes first), STOP and ask. Must NOT touch entropy/* (W2-A) or phase internals beyond
reading their outputs.
### W2-C — 846 drivers role filter · sonnet · engine
Goal: dimension-table attributes are not driver measures. _measure_columns joins
TableEntity.table_role, restrict to FACT|PERIODIC_SNAPSHOT; regression test: DIMENSION
table with measure-labeled column → NO ranking row; born-loud empty rows for real facts
stay. Surface: analysis/drivers/persistence.py + tests. Must NOT touch targets.py (W2-D).
### W2-D — 844/808 effect guard · sonnet · engine
Goal: no ±1e18 effects. Generalize EntityDemeanedRatioTarget's correct pattern
(targets.py:244-247): group_effects on a near-zero/de-meaned baseline returns no effect
for that slice (omission, not clamp) across FlowTarget/RatioTarget/EntityMeanTarget;
unit tests with near-zero-baseline residual fixtures. Surface: analysis/drivers/
targets.py (+processor wiring read-only) + tests. Must NOT touch persistence.py (W2-C).

## Eval history
- 2026-07-21 · W1-A integration · `pytest calibration/unit` on merged eval main:
  106 passed · deterministic gate GREEN (no LLM run; budget untouched).

## Decisions
- Eval-repo write discipline: its CLAUDE.md is silent on own-repo branching (only the
  engine-submodule case is specified); observed history = direct main commits →
  RESOLVED: ff-merge lanes to LOCAL eval main, hold push until the wave-1 checkpoint
  (lead visibility before anything reaches origin). 0 reversals.
- W1-A in-lane fork (adapt the existing below-floor lint test vs add-only): the test's
  fixture WAS the bug's pin — adapted per brief bullet 5's omission from the keep-passing
  list. Resolved in-lane, correctly, no escalation needed.
- W1-A integrated 2026-07-21: 5503a7f (lint decouple + 5 tests) + c2328d9 (probe dirs,
  byte-identical, untracked originals cleared from main checkout pre-merge).

## Checkpoint log
2026-07-21 — slice opened. Spec from the planning conversation with the lead (phases plan +
three confirmations); five epics re-grounded in Jira; DAT-855 created as Phase-4 home.
