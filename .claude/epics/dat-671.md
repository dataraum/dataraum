# DAT-671 Phase 3 Lane B — owner ledger

Objective: Close stabilisation Phase 3 (RFC 3 B1) — drill UX finished, slice catalog scored,
additivity next cut, cross-fact drill-across live, config→DB complete, correctness bugs fixed —
eval-green on `epic/dat-671-phase3`. Spec agreed with lead 2026-07-28 (in-session).

Scorecard:
- Browser-verified (real browser, screenshot-judged where new UI): drill from answer surface
  AND report detail · drill steps survive reload · drilled result mints child report with
  `parentId` · cross-fact drill-across numbers verified correct on the conformed-dimension
  fixture · bus matrix consumed by a real UI surface.
- RFC 3 B1 engine exit: engine's own SQL computes a predicate-carrying per-entity unit metric,
  drilled one level down, additivity-gated; `sql_snippets` substrate in place for DAT-687
  (graded from eval repo — not this slice's gate).
- Deterministic gates green at every integration (full CI mirror both packages).
Non-goals: Lane A (DAT-884/885/687, DAT-680 incl. DAT-724/861 — eval repo) · Phase 4 / DAT-855
strands (549/573/738/837) · DAT-864 (parked, consumer-gated) · DAT-674 (stretch, not close-gating)
· no run-model surgery · no second eval harness · NO finance-seed retirement this slice (lead,
2026-07-28: finance stays the shipped vertical/induction seed AND the eval substrate until
Phase 4 strand 2; no B3-alpha pull-forward).
e2e budget: none standing — ask lead per real-LLM run, at wave boundaries only (lead-locked
cadence, re-approved 2026-07-28). Deterministic tiers per integration. spent: 0.
EVAL-REPO LOCK (lead, 2026-07-28): Lane A is running concurrently IN dataraum-eval — do not
touch that repo AT ALL (no calibrate, no Tier-1/2 there, no submodule checkout flip) without
checking in with the lead first; he updates on Lane A state at that point. Per-integration
deterministic gates are in-repo only (engine + cockpit CI mirror).
EVAL BLOCKERS (lead, 2026-07-28): DAT-886 (sign on metric-extract path → extends W1-d, both
consumers of stored_sign) · DAT-887 (period axis unbound → new W3-c) · DAT-874 (stock↔flow in
induction, Jira-Done but flagged live → new W1-e, verify-first, after W1-d). All found by
DAT-687's first read on CLEAN data @ engine bce2256a — they are the eval-green critical path.
Also from that read: DAT-709's historical repro did NOT manifest (current_ratio=13.8058) —
W1-b's guard stays as class guard; the 2/3 baseline fails are DAT-874's signature.

## NEXT ACTION
Await W1-c rework + W1-c spec verdict + W1-d pair. On W1-c rework green: owner verify →
merge. On W1-d pair clean: merge + cockpit mirror regen (`bun run db:pull:metadata`, needs
compose stack). Then W1 wave CLOSED → checkpoint to lead + Lane A eval sync BEFORE any eval
touch. Then W1-e (DAT-874 verify-first) + W2 lanes (W2-a unblocked once W1-c merges).

## State
INTEGRATED (epic gates green after each): W2-d1 DAT-840 (c47e013ad) · W1-b DAT-709
(8b87c5799) · W1-a DAT-847 (last epic gate run: 3063P/8s engine, 200f/1970t cockpit).
W1-c IN REWORK, RE-SCOPED (senior: annotation leak + live-type ruling + couldn't-check split;
spec: NON-COMPLIANT on 878's original claim — 11-site sweep shows per-call-site filtering
doesn't compose. Owner salvage split: 878 narrows to catalogue-prompt-complete with honest
reworded commit; full seam = new W1-f (/refine first); 748 unchanged, independently sound).
W1-d IN FOLD-INS (spec PASS-with-fold-ins: loss-trace docstring honesty — stored_sign has NO
loss.yaml entry so abstentions don't surface in readiness/coverage yet (deliberate, entry owed
at calibration); stored_sign added to PGQ column_node PROPERTIES; 'unsure' comment fix;
relative-vs-absolute label caveats documented. Both prose surfaces + prompt Fable-APPROVED.
Senior verdict pending — reconcile on arrival). CHECKPOINT watch items (sharpened): (a) the partition is measured RELATIVE to the event
table — family-normalized event amounts invert BOTH labels (confidently backwards, not
merely uninformative); no defence possible today, the account-class axis is not machine-
readable anywhere. (b) all-debit-normal populations mint a confident ledger_signed that
will read as measured in any label-counting eval. Fold-ins delivered (5497719d7, 3062P/8s;
abstentions ARE persisted to entropy_objects — the gap is only the readiness rollup until
the loss.yaml entry lands at calibration). Branch holding for senior verdict.
INTEGRATION STEP OWED at W1-d merge:
cockpit `bun run db:pull:metadata` regen (currentColumnConcepts + currentMeasureAggregation-
Lineage enumerate columns; schema-drift CI fails without it; needs compose stack).
Checkpoint items for Lane A sync: stored_sign runs on documented placeholder reliabilities
(calibration owed in eval) · W1-a near-tie margin is a recall trade only calibration grades.
W1-b noted edges (one-liners, no rework): guard scope = this run's warm DAG (declared set is
deterministic per vertical — stable); sorted re-ground order means members see different retry
evidence (both demoted — not a pick); transitive A⊥B⊥C over-reach untested (symmetric, likely
correct); test_unseeded_vertical name misleads.
Reviewer insight (recorded): the finance corpus exports 8 flat tables — NO dim_* table, no
calendar dim, no surrogate date key. It cannot exercise the period-FK logic at all; the unit
fixtures are the whole evidence base there. And the reconcile near-tie margin is a recall
trade the unit suite cannot grade — needs the calibration read at the wave boundary.
Merge-order note: W1-b and W1-c both touch graphs/ (agent.py vs context.py — disjoint files);
W2-a (879 context split) stays HELD until both integrate. Jira hygiene DONE
(853/714/701/716/717/718→Done, 715→Duplicate, residue → DAT-868). Scorecard: all bands red
(nothing integrated yet). Reviewer note: nesting default-OFF — OWNER runs both reviewers at
integration (running now for W1-a + W2-d1, Opus tier).
W1-a eval-relevant note: near-tie abstention may make the aggregation-lineage witness abstain
on marginal measures it previously ruled on — watch at first calibration.

## Open forks
(none)

## Parked
- Stale merged-epic worktrees `.claude/worktrees/epic-dat-725`, `epic-dat-853` in the shared
  checkout — remove with lead ack at close (not mine to delete mid-flight).
- DAT-718 oracle half lives in dataraum-eval — Lane A's; note in closeout.
- `reporting_intent` persisted `entropy_readiness.intents` blobs keep the old string until a
  re-run — acceptable, note in rename lane brief.
- `analysis/lineage/__init__.py` stale `__all__` (re-exports nothing used, omits processor's
  imports; nothing imports the package) — W1-a discovery, trivial cleanup at close.
- Smart-integer calendar keys (dim_date.date_key=20240131) still resolve FACT — W1-a deliberate
  under-claim, fails safe; closing it = an LLM-contract change (RelationshipOutput can't say
  "target is a calendar"), NOT a name heuristic. Candidate future ticket at closeout.
- `semantic/agent.py::_load_profiles` uses profiled_at-latest, not run-scoped reads — W1-c
  discovery, pre-existing; relevant to any future run-versioning pass.
- Period-dimension identity residual (W1-a re-review): the cardinality witness is necessary
  but NOT sufficient — a small dimension with a genuinely unique event date (launch_date/
  opened_at class) still false-fires; the smart-integer calendar still under-claims. Closing
  BOTH = one LLM-contract change: the model names its period dimension, witness stays as the
  soundness gate. Future ticket at closeout; never a name heuristic.
- Generic retained-failure steer serves a NULL-aggregation tail irrelevant to
  provenance_invalid failures too — W1-b discovery, small prompt honesty cleanup.
- DAT-809 ticket cites stale file:line evidence (module reshaped since 2026-07-17) — refresh
  description at checkpoint.
- DAT-840 note: canvas node renders only the OUTPUT step's checks; an overlay-authored check on
  an extract step shows in the step list only — revisit when overlay authoring goes live.

---

## Substrate map (grounded 2026-07-28, two Explore passes @ main bce2256ab)
**Drill/parts:** DAT-714 FIXED on main (PR #462; COALESCE carrier spine `cockpit/src/duckdb/parts.ts:722-742`).
`parts.ts` greps as binary — use `grep -a`. Additive path = GROUP BY (`:720`). Local categorical
judge `flattenAdditive` `parts.ts:229` (consumed `:299,:583`) — DAT-868 must delete it.
**Additivity:** engine classifier `graphs/additivity.py` (457L) + resolver `graphs/additivity_resolver.py`
+ persistence `pipeline/phases/metrics_phase.py:456` (`metric_additivity`, unique (target_kind,target_key,run_id))
+ `current_metric_additivity` (`storage/read_views.py:96`) + PGQ og_additivity (`storage/property_graph.py:194,986-1025`).
Cockpit time gate reads boolean `time_additive` (`tools/drill-axes.ts:684-711`, helper `:307-329`);
categorical axis deliberately unread (`:244-250`); `categoricalAdditive` mirrored in Drizzle
(`db/metadata/schema.ts:425`), zero consumers. SQL-AST gate fails closed on window/FILTER
(`duckdb/sql-ast.ts:84-90`) — the DAT-715 residue, now DAT-868's.
**DAT-857/868: zero code on main.** Only typed-abstention precedent: driver-ranking status/abstain_reason
(`analysis/drivers/models.py:201`).
**DAT-847 (live, both):** discriminator bare `<` no margin `analysis/lineage/reconcile.py:106`
(guardrails `:100-103` abstain on bad fit, near-tie still picks); `derive_table_role`
(`analysis/semantic/db_models.py:137-153`) needs grain∩TimeColumn, call site passes event-role
date cols only (`semantic/agent.py:259-267`) → period-FK snapshot ⇒ FACT ⇒ over-permissive
`snapshot_count` verdict via `additivity_resolver.py:222`. Merged 847 commits fixed a different
surface (`entropy/resolve.py:72-140` None-through).
**DAT-709 (live):** grounding = 1 LLM call/concept (`dataraum-config/llm/prompts/graph_sql_generation.yaml`);
no cross-concept pass; snippet unique key INCLUDES standard_field (`engine/schema.sql:245`) so
identical statements dedup-invisible; `normalized_expression` matched only for formula type
(`query/snippet_library.py:303-313`); within-output uniqueness only (`graphs/models.py:451-470`);
finance ontology declares NO disjoint block — disjointness only via convention concept_groups
(`analysis/semantic/concept_edge_store.py:77-81`).
**DAT-748 (live):** only VARCHAR sampled (`analysis/typing/inference.py:79-84`); raw_type from one
DESCRIBE of DuckDB CTAS (`sources/backends.py:208-211`); strongly-typed heuristic
`pipeline/phases/typing_phase.py:84-88`, verbatim resolve `:189`.
**DAT-878 (live):** column load unfiltered `analysis/catalogue/context.py:758-764`; `is_surrogate_column`/
`SURROGATE_PREFIX` (`analysis/relationships/surrogate.py:32`) never used by prompt builders.
**DAT-739 (premise part-outdated):** producer EXISTS — `analysis/semantic/reconciles_with.py::derive_reconciles_with`
wired `metrics_phase.py:376-384` (SAVEPOINT); tolerance always NULL (`reconciles_with.py:33-36`);
consumers = renderers only (`graphs/context.py:1493,1514,1963-1968`, `validation/induction.py:394`,
`property_graph.py:518-523`). Missing: executor computing the two sides.
**DAT-673 remainder:** driver ordering shipped invisible (`tools/drill-axes.ts:193-224,638-642`);
`DrillAxis` (`duckdb/drill.ts:39-54`) has no gain field; no drill-UI hierarchy consumer
(`current_dimension_hierarchies` only in `tools/query-context.ts:319-439`, `teach-routing.ts:26-29`);
no Haiku on drill path (Haiku = NAV_MODEL/SUMMARY_MODEL `llm.ts:27,33`).
**Config→DB:** DAT-880 `_normalize_legacy_check_fields` = model_validator on every ValidationSpec
(`analysis/validation/models.py:116`, docstring names DAT-447 overlay `core/overlay.py:222-233` as
last producer; `check_type` bare str `models.py:84-90`; entropy reader branch `entropy/detectors/loaders.py:748`).
DAT-881 no typed cycle_types table (families yes `schema.sql:70`); readers `cockpit/src/tools/teach-cycle.ts:65-102`
(errors→[] swallowed) + `frame.ts:66,420` seed; engine `analysis/cycles/config.py:70-82`.
DAT-882 `teach-metric.ts:70-95` rglobs shipped YAML; DAG body via `metric-spec.ts:307-324`; 3 call
sites (teach-metric:109, frame.ts:67/467, routes/api/shipped-metric-dag.ts:19/50).
DAT-883 envelope re-parsed per call (`analysis/semantic/concept_store.py:216`, no-cache by design
`ontology.py:171-175`, linters re-run each call); fabricated "1.0.0" one layer down
(`core/vertical_loader.py:56` empty_base → pydantic default `ontology.py:85`); hot-path callers:
semantic/agent.py:142, column_agent.py:112, catalogue/agent.py:100, graphs/context.py:871,
semantic_per_column_phase.py:192. Constraint: do NOT run while DAT-724 runs (eval-side).
**reporting_intent:** config-only — `dataraum-config/entropy/loss.yaml` (14 hits); engine iterates
generically (`entropy/loss.py:67-68,139-152`); opaque string to cockpit (`why-table.ts:52,154`,
`look-table.ts:47`); literals only in 7 test-fixture lines + prose `why-table.ts:371`, `look-table.ts:6`.
**DAT-875:** NOT yet verified — W1 lane verifies first (claim: one-sided sign normalization survives
sign_natural_balance; deviation exactly 2×|ending_balance| on clean-flat).
**W2 cockpit surfaces (grounded 2026-07-28, Sonnet reader):** Only ONE drillable mount:
`AnalyseModal` → `DrillableGrid` w/ nodeRef (`analyse-overlay.tsx:296`). Answer
(`answer-result.tsx:267`), run_sql (canvas kind result-grid, `canvas-registry.ts:45`), report
detail (`reports/$reportId.tsx:225`) all mount plain `ResultGridWidget` — gap = no
`axesRequest`/`nodeRef` (required prop `drillable-grid.tsx:271`). DAT-678 seam: answer =
`tools/query.ts` (`querySubAgent`:581, capture `:187-210` composedSql+components); canvas boundary
DROPS components (`tool-result-to-canvas.ts:279-303` projects only {sql,summary,confidence});
`saveQuerySnippet` INSERT never populates `parts` (`db/metadata/snippet-writer.ts:108-123`);
`SnippetParts`/`sql_snippets.parts` is the join point `parts.ts:85,99` expects; tier B deleted
(`duckdb/drill.ts:16`); tier A `composeTierA` (`drill.ts:98`, `/api/drill/compose`) works on any
flat SQL = the honest fallback. DAT-676: drill state 100% useState (`drillable-grid.tsx:298-303`,
`analyse-overlay.tsx:179-187`); zero search-param usage in reports routes; reload loses all.
DAT-627: `reports.parentId` exists (`db/cockpit/schema.ts:405`) + threaded through
`CreateReportInput`/`ReportRow`; `MintBody` (`routes/api/reports/mint.ts:16-24`) has NO parentId;
sole mint caller `answer-result.tsx:189-214`; zero lineage UI. DAT-840: both DAG paths drop
`validation` — `parseMetricDag`/`MetricStep` (`tools/operating-model-graph.ts:162-198`) and
`narrowSteps`/`DagStep` (`lib/metric-dag.ts:10-67`); authorable field exists
(`metric-spec.ts` GraphStepSchema.validation:111-141, enforced by engine graphs/verifier.py);
render homes: `operating-model/nodes.tsx` (subtitle:70, badge:38), `widgets/metric-dag-steps.tsx:15-44`,
equation header (`analyse-overlay.tsx:269-277`). No component named ModelFrame exists (language only).
Design tokens: `ui/theme.ts` (canonical) + `styles.css` Tailwind mirror; UI quality bar in cockpit
CLAUDE.md. Cockpit baseline GREEN on main: 197 files / 1947 tests.
**W4 substrate (grounded 2026-07-28):** `current_bus_matrix` = `schema_read.sql:9-17`, computed
`analysis/hierarchies/bus_matrix.py::derive_bus_matrix` in dimension_hierarchies phase. Cell:
`attachment` referenced|folded · `confirmation_source` unconfirmed|judge|user|keeper ·
`conformed_group` (nullable = the identity signature) · `needs_confirmation`. DAT-809 filter =
folded ∧ `conformed_group IS NOT NULL` ∧ ¬needs_confirmation; precedent
`analysis/lineage/processor.py:435-479` `_shared_dimension_groups`. Referenced cells ALREADY flow:
`og_conformed_dimension` (`schema_graph.sql:156-170`) → `_read_conformed`
(`graphs/context.py:1323-1361`) → prompt. Folded cells: zero paths. Cockpit `currentBusMatrix`
mirrored (`schema.ts:83-99`), zero consumers. DAT-809 ticket cites stale line numbers — conclusion
holds, evidence needs refresh (parked). DAT-740: multi-fact FULL-JOIN precedent = `parts.ts:708-742`
(one metric's DAG only, joins by name not conformance, no per-side rollup); needs conformance-derived
join + additivity-aware grouped merge + loud abstain. DAT-730 (common grain/P5): reader claimed
"retired, redo needed" — CORRECTED: merged via `46b7bbb3f`, Jira Done. W4 blocker in-slice = DAT-857
only (W3-a, already sequenced first). DAT-737 delta: `concepts`/`conceptEdges` mirrored zero
consumers; `og_*` views have NO Drizzle mirror; model route builds its own parallel metric-DAG graph
(`tools/operating-model-load.ts:17-35`, DAT-591) never touching concepts/og_* — "faithful lens"
doesn't exist; config seam still fs-based.
**DAT-838 seam:** grounding-time substrate complete (`ExtractGroundingOutput.where` models.py:626-631,
`compose_extract_sql`/`compose_where_predicate` formula_composer.py:35-68, `compose_scoped_where`
validity_scope.py:127-177). Declaration chain (NO predicate anywhere): finance metric YAML →
`metric-induction.ts:104-128` extractShape → `metric-spec.ts:35-100` GraphStepSchema.source →
`teach-metric.ts:116-121` config_overlay JSONB → `graphs/config.py` → `loader.py:209-247` →
`StepSource`/`GraphStep` (models.py:85-136) → agent.py call sites (~1352,1578,1655,1797,1897).
Pure JSON-shape + prompt plumbing, NO migration. HARD CONSTRAINT: predicate = field on existing
extract branch, never a new step variant (`induction-schema.contract.test.ts` pins branch count vs
constrained-decoding ceiling).
**Per-entity grain (B1 exit) — DISCOVERED GAP, absorbed:** metrics are scalar-only end-to-end;
`OutputType.SERIES/TABLE` has zero consumers; composers hard-code one-row scalar. Needs: grain/entity
declaration on `OutputDef` (models.py:139-145) + grouped composition mode in
`compose_extract_sql`/`compose_formula_sql` + induction/spec mirrors. Absorbed into W3-b.
**DAT-875 (verified LIVE 2026-07-28):** no deterministic sign code — `sign_natural_balance` is
prompt text (`finance/ontology.yaml:272-290`, DAT-870 clause `4a6b39a36`); routed via
`relevant_conventions` (induction.py:100,206,364-406; validation_phase.py:225-256). Generator stores
`ending_balance` raw debit−credit ⇒ one-sided normalization = exactly 2×|ending_balance|, arithmetic
certainty. Lead-endorsed design on ticket: typed `stored_sign` fact on ColumnConcept, DAT-657
temporal_behavior template (LLM-authored + data-reconciled + served via grain_facts). No
stored_sign/SignConvention symbol exists yet.
**Eval mechanics:** deterministic gate = `uv run pytest calibration/unit -q` (Tier 1/2, no docker/LLM;
`-m 'not llm'` equivalent; 33 unit files + frozen sqlite fixture). Real-LLM = `make calibrate`
(docker+LLM) — wave boundaries, lead go only. BASELINE (pre-existing, do not attribute to slice):
clean pass `439ed742a193` 2026-07-23 engine `aeb2421b`: 47P/**3F**(clean_scores_within_measured_bands,
pooled_llm_detectors_below_measured_ceiling, clean_readiness_no_regression)/6xf/31s; clean-flat
`add243032d75`: 1F (cycle_recall). Verdict store `calibration/results/verdicts.jsonl`; engine_commit
computed live from `vendor/dataraum-context` checkout (editable install) → to eval the branch: push
it, `git -C vendor/dataraum-context fetch && checkout epic/dat-671-phase3`. Conformed fixture =
clean-flat (account axis folded into general_ledger/trial_balance/balance_sheet;
`schema_transforms.py:373-419`; numbers correct-by-construction; folds declared in metadata_truth).

## Plan (living)
W1 bugs → W2 drill UX + catalog → W3 additivity + grammar → W4 cross-fact → W5 config→DB → W6 739.
Lanes per wave, independently mergeable into epic branch:
- W1-a DAT-847 (Opus·high) · W1-b DAT-709 (Opus·high) · W1-c DAT-878+748 (Sonnet·high)
  · W1-d DAT-875 verify→fix (Sonnet·high)
- W2-a DAT-879 + graphs/context.py 3-way split (Opus·high; HOLD until W1-c integrates — W1-c
  touches graphs/context.py:509-522; W1-b touched zero lines there, hold re-based 2026-07-28) · W2-b DAT-673 remainder (Sonnet·high) · W2-c DAT-678 (Opus·high;
  before W2-b or serialize — both touch drillable-grid.tsx) · W2-d1 DAT-840 (Sonnet·medium,
  UNBUNDLED — zero deps, spawned 2026-07-28) · W2-d2 DAT-676+627 (Sonnet·medium; after DAT-678
  — the surfaces they persist/mint from aren't drillable until 678 lands)
- W3-a DAT-857+868+715-residue (Opus·high; Fable review — semantic verdict surface)
  · W3-b DAT-838 + per-entity metric grain (discovered, absorbed — B1 exit needs it; Opus·high,
  raised from Sonnet: grouped-composition design spans models+composers+mirrors, same declaration
  chain as the predicate so one coherent cut)
  · W3-c DAT-887 (ABSORBED 2026-07-28, lead-flagged eval blocker): point-in-time extracts bind
  naked MAX(period), unbound to fiscal window — AP 37.6% off, AR right by luck. Fix = period/window
  binding on point-in-time extracts riding DAT-730's calendar/period-grain substrate + the RESOLVED
  period recorded on the snippet/artifact (the ticket's required observable). Opus·high; after/with
  W3-b (same declaration chain).
- W1-f surrogate serving seam (DISCOVERED 2026-07-28, W1-c spec sweep): ~11 LLM prompt sites /
  ~10 column sources; two physical DESCRIBE paths unfilterable by ORM (graphs/agent.py:1261 —
  which ALSO feeds validate_grounding_basis, so contract-v2 ACCEPTS surrogates while
  served_membership rejects them; hierarchies/processor.py:245); unfiltered: validation/
  resolver.py:411, cycles/context.py:322, enriched_views_phase.py:226, slicing_phase.py:805,
  semantic/utils.py::load_persisted_annotations, og_columns projection (which _read_references
  legitimately needs surrogates from — wholesale view filter would break relationship evidence).
  /REFINE FIRST — candidate cuts: exclude _sk__* from the enriched-view PROJECTION (joins keep
  using them; DESCRIBE class clean by construction; cockpit-visible change) vs a single shared
  column-serving seam. Opus·high. After W1 closes; does not gate the B1 scorecard.
- W1-e DAT-874 (ABSORBED 2026-07-28, lead-flagged eval blocker; Jira says Done — VERIFY FIRST):
  validation induction compares stock to flow while column temporal_behavior is determined for both
  sides; repro signature = 2 of the 3 pre-existing baseline eval fails (clean_scores_within_measured_
  bands + clean_readiness_no_regression, 12×CTC 0.500). Fix direction: column-level temporal_behavior
  served/honored at induction so a stock↔flow comparison is impossible or abstains. SERIALIZED after
  W1-d (same serving seam). Opus·high.
- W4-a DAT-737 (Sonnet·high) · W4-b DAT-809+740 (Opus·high)
- W5-a DAT-880 (Sonnet·medium) · W5-b DAT-881+882 (Sonnet·medium) · W5-c DAT-883 + reporting_intent
  rename (Sonnet·high; rename needs lead approval at checkpoint)
- W6-a DAT-739 executor (Opus·high)
Sequencing: 847 before W3 (role fix feeds verdicts). 879 lands before/with 678 (axes). W2-d after
W2-c (676/627 depend on 678 per dep graph). Reviewers: senior-code-reviewer + spec-compliance
at every integration, Opus·high floor; Fable on prompt/evidence-surface diffs (709, 878 prompt,
857/868, 739, 673 chips prompt if any).

## Lane briefs

### W1-a DAT-847 — stock/flow honesty + period-FK snapshot role · Opus·high
Goal: (1) `reconcile.py:106` tie-break gets an abstention margin — near-tie between r_stock/
r_flow abstains (typed, existing abstention shapes) instead of picking a side; margin justified
from the residual statistics themselves, NEVER tuned to clean-flat numbers. (2) `derive_table_role`
(`semantic/db_models.py:137-153`) recognizes a snapshot grained on a period FK: grain column that
FK-references a period/date dimension counts as time-in-grain → PERIODIC_SNAPSHOT. Call site
`semantic/agent.py:259-267` passes event-role date cols only (DAT-780) — understand why before
widening; the fix must not regress DAT-780. Downstream check: `additivity_resolver.py:222`
snapshot_count rule now sees the corrected role (assert in an integration test).
Surface: analysis/lineage/reconcile.py · analysis/semantic/db_models.py · semantic/agent.py ·
their tests. Do NOT touch: graphs/additivity*.py (W3), entropy/resolve.py (done), cockpit.

### W1-b DAT-709 — cross-concept grounding disambiguation · Opus·high
Goal: two concepts silently grounding to the IDENTICAL extract becomes impossible — detected
→ disambiguate (re-ground with sibling-extract context served) or typed abstention. NO
deterministic override of the LLM judgment (locked); the guard detects and re-prompts/abstains,
never hardcodes a pick. Compare on normalized statement across concepts sharing a disjoint
group/family (disjointness today only via convention concept_groups —
`concept_edge_store.py:77-81`; finance ontology has no disjoint block — check coverage).
Surface: engine graphs/ grounding path + persistence lookup (`query/snippet_library.py`),
graphs/context.py disjoint serving, `dataraum-config/llm/prompts/graph_sql_generation.yaml`
(prompt diff ⇒ Fable review at integration). Do NOT touch: additivity, cockpit.
Acceptance: test where two disjoint concepts produce identical normalized statements → guard
fires (re-ground or abstain); no silent identical pair persists.

### W1-c DAT-878 + DAT-748 · Sonnet·high
Goal 878: minted `_sk__*` surrogate columns stop reaching the meaning prompt — filter via
`is_surrogate_column`/`SURROGATE_PREFIX` (`relationships/surrogate.py:32`) at the catalogue
context load (`catalogue/context.py:758-764`); check the other prompt builders (semantic/,
graphs/context.py) for the same leak. Evidence-surface change ⇒ Fable review at integration.
Goal 748: typing stops blindly trusting catalog raw_type for DB sources — minimal honest
cross-check (refine decides: TRY_CAST sample or DESCRIBE re-verify) for non-VARCHAR columns;
do not over-build (`typing/inference.py:79-84`, `typing_phase.py:84-88,189`).
Do NOT touch: relationships/surrogate.py logic, prompts YAML wording, cockpit.

### W1-d DAT-875 — typed stored_sign fact · Opus·high (raised from Sonnet: evidence-surface,
cross-layer typed-fact design; spawned 2026-07-28 after verification confirmed the claim live)
Goal: implement the lead-endorsed design from the ticket comment — typed `stored_sign` on
ColumnConcept per the DAT-657 temporal_behavior template (LLM-authored + data-reconciled + served
via grain_facts), so `sign_natural_balance` can be applied two-sidedly and the BS-vs-GL one-sided
normalization (deviation ≡ 2×|ending_balance|) becomes impossible or abstains. Serve the fact,
never post-process the SQL (agentic ≠ deterministic). Engine + schema.sql only; cockpit mirror
regen is an OWNER integration step, do not hand-edit generated files.

## Eval history
(none yet)

## Decisions
- 2026-07-28 · W1-a composite-calendar fork: reviewer's "keep composite via jointly-unique"
  proved impossible (per-column profiles carry no joint cardinality — probed) · call: narrow
  to grain∩unique, composite calendars under-claim to FACT (fails safe: COUNT keeps its axis;
  over-claim denied a real axis) · evidence: lane's 3-shape probe table · reversals: 0.
- 2026-07-28 · W1-a period-FK fork: grain-membership predicate → CARDINALITY-WITNESS widening
  (dimension's event date one-row-per-period, exact structural uniqueness, no tuned threshold;
  missing witness → FACT) · criterion: must fire on the surrogate-keyed calendar (the DAT-847
  shape) while dim_customer stays out on DATA, not on the LLM's grain choice · evidence: senior
  review trace — old predicate fired only where DAT-780 already covered · reversals: 0.
- 2026-07-28 · slice cut = one /own, six waves · lead-picked over two-owner splits.
- 2026-07-28 · closures 853/714/701/715-718 approved · grounded file:line · 715 residue → DAT-868.
- 2026-07-28 · reporting_intent: rename now; lane proposes, lead approves at checkpoint.
- 2026-07-28 · model mix as recorded in Plan · lead-approved.

## Checkpoint log
(none yet)
